# Vercel Blob — create a Blob store in the Vercel project dashboard.
# Vercel injects BLOB_READ_WRITE_TOKEN automatically on deploy.
BLOB_READ_WRITE_TOKEN=

# Optional: lock uploads/deletes behind Bearer token (reads stay open via the site API).
# Leave empty for fully automatic sync for all site visitors.
DXF_API_SECRET=
