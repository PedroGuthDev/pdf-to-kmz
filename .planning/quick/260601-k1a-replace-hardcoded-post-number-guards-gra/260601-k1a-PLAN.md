---
phase: quick-260601-k1a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parser/__tests__/fixtures/luizcarolino-ground-truth.json
  - parser/__tests__/fixtures/luizcarolino-dwg-region.json
  - parser/__tests__/fixtures/luizcarolino-pdf-baseline.json
  - tools/build-luizcarolino-fixtures.mjs
  - tools/route-pdf-accuracy-harness.mjs
  - tools/run-route-pdf-accuracy-gate.mjs
  - tools/route-dwg-accuracy-harness.mjs
  - tools/run-route-dwg-accuracy-gate.mjs
  - parser/__tests__/fixtures/luizcarolino-dwg-baseline.json
  - parser/distance-associator.js
  - parser/dwg/graph-walker.js
  - parser/coordinate-calculator.js
autonomous: true
requirements: [K1A-FOUNDATION, K1A-DISTASSOC, K1A-GRAPHWALK, K1A-COORDCALC]
must_haves:
  truths:
    - "A second labeled route (Luiz Carolino) runs through the PDF pipeline AND the DWG graph-walk pipeline and reports per-post error vs ground truth"
    - "Both Luiz Carolino gates (PDF accuracy + DWG graph-walk) run on CURRENT un-generalized code and emit baselines BEFORE any hack is touched"
    - "The graph-walker generalizations (stage 3) are validated against the Luiz Carolino DWG gate — not the PDF gate, which does not exercise graph-walker"
    - "distance-associator no longer contains the literal 36/37/38 + 10.5/35.5 block; the generic sheet-break detector absorbs that case"
    - "graph-walker contains zero `fromNum===X && toNum===Y` post-number guards; structural predicates replace them"
    - "coordinate-calculator seam-lock anchor references no literal post number (15) and no literal page number (4)"
    - "Every generalization keeps BOTH gates green: Siriu regression gate (0 errors) AND Luiz Carolino baseline held or improved"
  artifacts:
    - path: "parser/__tests__/fixtures/luizcarolino-ground-truth.json"
      provides: "31-post ground truth in siriu-ground-truth.json shape"
      contains: '"number"'
    - path: "tools/route-pdf-accuracy-harness.mjs"
      provides: "Route-agnostic PDF-pipeline accuracy harness"
      exports: ["runRoutePdfAccuracyHarness"]
    - path: "tools/run-route-pdf-accuracy-gate.mjs"
      provides: "Luiz Carolino PDF accuracy gate with baseline compare"
  key_links:
    - from: "tools/route-pdf-accuracy-harness.mjs"
      to: "parser/coordinate-calculator.js (calculateCoordinates)"
      via: "PDF parse + calculateCoordinates"
      pattern: "calculateCoordinates"
    - from: "tools/run-route-pdf-accuracy-gate.mjs"
      to: "luizcarolino-pdf-baseline.json"
      via: "baseline compare"
      pattern: "baseline"
---

<objective>
Replace three route-specific hardcoded hacks (the "pairing/coordinate-accuracy"
layer) with generic structural/geometric predicates that reference ZERO post
numbers and ZERO absolute page/coordinate literals — validated against a SECOND
labeled route (Luiz Carolino / Palhoca.dxf) so "generic" is falsifiable rather
than Siriu-fitted.

Purpose: A predicate tuned to keep Siriu green is hardcoding with extra steps.
The dual gate (Siriu + Luiz Carolino) is the user's chosen falsification test:
a generalization is accepted ONLY if both gates stay green.

Output:
- A Luiz Carolino second fixture (ground-truth JSON, DWG region, PDF baseline).
- A route-agnostic PDF-pipeline accuracy harness + gate.
- Three generalized predicates, each as its own atomic commit.

Constraint posture: this plan does NOT auto-quarantine and does NOT auto-data-drive
any guard that resists generalization. On a guard that cannot hold both gates with
a zero-post-number / zero-coordinate predicate, the executor STOPS and surfaces the
specific regression for user discussion (LOCKED fallback in CONTEXT.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260601-k1a-replace-hardcoded-post-number-guards-gra/260601-k1a-CONTEXT.md
@.planning/STATE.md

# Existing Siriu pattern to mirror/parameterize
@tools/siriu-regression-harness.mjs
@tools/run-siriu-regression-gate.mjs
@tools/export-siriu-regression-fixtures.mjs
@debug-palhoca-route-match.mjs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration required. -->

Ground-truth JSON shape (parser/__tests__/fixtures/siriu-ground-truth.json):
  Array of { "number": <int>, "lat": <float>, "lon": <float> }

PDF pipeline entry (parser/pdf-parser.js):
  parsePdf(arrayBuffer) -> { posts, distances, cableSegments, pageDimensions,
                             viewportBoxes, utmGridPathsPerPage, distanceLabelItems, error? }

PDF coordinate calc (parser/coordinate-calculator.js L1116):
  calculateCoordinates(posts, distances, startLat, startLon, cableSegments=[], opts=null)
    opts = { pageDimensions, viewportBoxes, utmGridPathsPerPage }
    -> { posts: [{ number, lat, lon, ... }], connections, warnings }
  This is the pipeline that distance-associator (stage 2) and the seam-lock
  (stage 4) feed. Per-post lat/lon land on result.posts.

DWG pipeline entry (parser/dwg/coordinate-calculator-dwg.js L161):
  calculateCoordinatesWithDwg(posts, distances, startLat, startLon, cableSegments, opts, library)
  -> { posts, dwgStatus, connections, walkConnections }
  Graph-walker (stage 3) is reached through here via pairPostsByGraphWalk.

DXF load (parser/dwg/dxf-loader.js):
  parseDxfText(text) -> { posts: [{x,y,...}], cableEdges?, extmin, extmax }
  Coords are raw UTM meters — DO NOT scale by $INSUNITS, DO NOT trust header lat/lon.

UTM helpers (parser/geo/utm-calibrator.js):
  latLonToUtm(lat, lon) -> { easting, northing, ... }
  haversineMeters(lat1, lon1, lat2, lon2) -> meters

Region build (parser/dwg/region-pairing.js):
  buildPostIndex(posts); buildAdjacencyGraph(posts, cableEdges)

Siriu DWG gate is invoked as: node tools/run-siriu-regression-gate.mjs
  (also wired into `npm run test:gate`).
</interfaces>

<established_facts>
THREE HACKS — exact locations (verified this session):
1. parser/dwg/graph-walker.js L1790-1797: post guard `fromNum===73 && toNum===74`
   inside the `findGapOffCableReentryByNextLabel` branch, already behind structural
   conditions `chosenIdx===undefined && conn.gap && routeNextLabel!=null && (labelM==null || labelM>=100)`.
   Only the `fromNum===73 && toNum===74` equality is the hack.
2. parser/dwg/graph-walker.js L2036-2043: post guard `fromNum===80 && toNum===81`
   inside the `findOffCableInsertByNextLabel` branch, already behind
   `hop && nextLabel finite && neighbors.includes(hop.endpoint)` and the downstream
   `hopNextDelta > hopNextTol` discriminator. Only the post equality is the hack.
3. parser/distance-associator.js L1614-1637: fully hardcoded 36→37→38 block with
   literal label values 10.5 / 35.5. A generic sheet-break detector exists at L1639+
   and explicitly SKIPS 36/37/38 at L1651-1653.
4. parser/coordinate-calculator.js L1432-1465: seam-lock anchor with literal post 15
   (`sorted.find(p=>p.number===15)`, `gpsAtPostViaLabelWalk(..., 15, ...)`) and literal
   page 4 (`lockPageOriginAtGps(pageTransforms, 4, post15.x, post15.y, ...)`). Sits in
   the else-branch reached only when `!disableSeamLock && !multiSheetDetail && nBoundary===0`.
   `multiSheetDetail = viewportBoxes.length >= 3` (L1393); `nBoundary` from
   `lockPageOriginsAtSheetBreaksFromPriorProjection` (L1395-1403).

SECOND-FIXTURE ASSETS (route correspondence VERIFIED — 31/31 GT posts within 5 m, mean 1.0 m):
- PDF (repo): "INFOVIAS_AAF INTERNET_São José_RUA LUIZ CAROLINO PEREIRA_v1.pdf"
- DXF (repo root): Palhoca.dxf — 134 MB, 35,176 Poste-layer INSERTs (whole municipality).
- GT txt (repo): "coordenadas postes rua luiz carolino pereira..txt" — 31 posts,
  format `Poste NN; lat, lon;` (note: post numbers zero-padded e.g. `Poste 01`).
- GT bbox (UTM): x[727824..728151] y[6948054..6948546] (~330m × 490m). USE ONLY as
  fixture-build crop seed — MUST NOT leak into any runtime predicate.
- Probe debug-palhoca-route-match.mjs already does parseDxfText + latLonToUtm + txt
  parse; reuse its logic for the fixture build.
</established_facts>
</context>

<discuss_again_fallback>
LOCKED in CONTEXT.md. For stages 2, 3, 4: if no structural predicate (zero post
numbers, zero coordinate/page literals) can keep BOTH gates green, the executor
MUST STOP that stage and surface for user discussion:
- the exact guard being generalized,
- the specific regression (which gate, which post, what error) the guard prevents,
- the candidate predicates tried and why each failed the dual gate.
Do NOT auto-quarantine. Do NOT auto-data-drive (no config files, no thresholds
fitted to one route). Do NOT proceed to the next stage with a known-broken gate.
Revert the stage's edit so the working tree stays green, commit nothing for that
stage, and write the discussion findings into the SUMMARY.
</discuss_again_fallback>

<tasks>

<task type="auto">
  <name>Task 1: Convert Luiz Carolino ground-truth txt -> JSON</name>
  <files>parser/__tests__/fixtures/luizcarolino-ground-truth.json, tools/build-luizcarolino-fixtures.mjs</files>
  <action>
    Create tools/build-luizcarolino-fixtures.mjs (start with just the GT-conversion
    step; the region-extract step is added in Task 2). Read
    "coordenadas postes rua luiz carolino pereira..txt" from repo root, parse each
    line with the same regex the probe uses (`/Poste\s+(\d+);\s*(-?\d+\.\d+),\s*(-?\d+\.\d+)/`),
    and emit parser/__tests__/fixtures/luizcarolino-ground-truth.json as an array of
    { number, lat, lon } sorted by number — IDENTICAL shape to siriu-ground-truth.json.
    Parse `Poste 01` as number 1 (strip leading zeros). Do not invent or interpolate
    missing posts; emit exactly the posts present in the txt.
  </action>
  <verify>
    <automated>node -e "const g=require('./parser/__tests__/fixtures/luizcarolino-ground-truth.json'); if(g.length!==31) throw new Error('expected 31 posts, got '+g.length); for(const p of g){ if(typeof p.number!=='number'||typeof p.lat!=='number'||typeof p.lon!=='number') throw new Error('bad shape: '+JSON.stringify(p)); } if(g[0].number!==1) throw new Error('first post must be number 1'); console.log('OK 31 posts, shape matches siriu-ground-truth.json');"</automated>
  </verify>
  <done>luizcarolino-ground-truth.json exists with 31 entries of {number,lat,lon}, numbers 1..N integers, same shape as the Siriu fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Region-extract the Luiz Carolino route from Palhoca.dxf into a DWG-region fixture</name>
  <files>tools/build-luizcarolino-fixtures.mjs, parser/__tests__/fixtures/luizcarolino-dwg-region.json</files>
  <action>
    Extend tools/build-luizcarolino-fixtures.mjs with a region-extract step. Load
    Palhoca.dxf with parseDxfText (read as latin1, per the probe). Compute the crop
    bbox by converting the GT posts to UTM with latLonToUtm and padding the GT bbox by
    a fixed margin (e.g. +50 m on each side) — derive the bbox FROM the ground-truth
    coords at BUILD TIME; do NOT hardcode the literal x[727824..728151]/y[...] numbers
    in any runtime parser file. The bbox lives only in this build tool. Keep only Poste
    INSERTs whose (x,y) fall inside the padded bbox, plus any cableEdges fully inside it
    (mirror how export-siriu-regression-fixtures.mjs serializes region.posts/cableEdges).
    Write parser/__tests__/fixtures/luizcarolino-dwg-region.json with { posts, cableEdges }
    in the SAME shape as siriu-dwg-region.json. The cropped post count should be in the
    ~31–80 range (route plus immediate neighbors); if it is in the thousands the crop
    failed — STOP and report rather than committing a bad fixture.
  </action>
  <verify>
    <automated>node tools/build-luizcarolino-fixtures.mjs && node -e "const r=require('./parser/__tests__/fixtures/luizcarolino-dwg-region.json'); if(!Array.isArray(r.posts)||!Array.isArray(r.cableEdges)) throw new Error('bad shape'); if(r.posts.length<31||r.posts.length>500) throw new Error('crop suspicious: '+r.posts.length+' posts'); console.log('OK region:',r.posts.length,'posts',r.cableEdges.length,'edges');"</automated>
  </verify>
  <done>luizcarolino-dwg-region.json exists, {posts,cableEdges} shape matches siriu-dwg-region.json, post count is route-sized (not the full 35k), and the crop bbox is computed from GT coords inside the build tool only.</done>
</task>

<task type="auto">
  <name>Task 3: Parameterize a route-agnostic PDF-pipeline accuracy harness + Luiz Carolino gate, capture baseline on current code</name>
  <files>tools/route-pdf-accuracy-harness.mjs, tools/run-route-pdf-accuracy-gate.mjs, parser/__tests__/fixtures/luizcarolino-pdf-baseline.json</files>
  <action>
    Build a route-agnostic PDF-pipeline harness modeled on siriu-regression-harness.mjs
    but exercising the PDF path (this is what stages 2 and 4 affect; the Siriu harness
    only exercises the DWG path). route-pdf-accuracy-harness.mjs exports
    runRoutePdfAccuracyHarness({ pdfPath, groundTruthPath }): read the PDF, call
    parsePdf, then calculateCoordinates(posts, distances, gt[0].lat, gt[0].lon,
    cableSegments, { pageDimensions, viewportBoxes, utmGridPathsPerPage }); for each
    result post with a GT match, compute haversineMeters to the GT coord; return
    { errorsByPost: Map<number,meters>, matched, maxErr, meanErr }. Favor parameterization
    over cloning (CONTEXT discretion) — the function takes route inputs, no Siriu literals.
    run-route-pdf-accuracy-gate.mjs runs the harness for Luiz Carolino (PDF +
    luizcarolino-ground-truth.json) and compares per-post error against
    luizcarolino-pdf-baseline.json using the same slack/ceiling pattern as
    run-siriu-regression-gate.mjs (ceil((err+0.5)*10)/10). On first run, when the
    baseline file is absent OR env LUIZCAROLINO_UPDATE_BASELINE=1 is set, WRITE the
    baseline from current results instead of comparing. Run it once on CURRENT
    un-generalized code to capture luizcarolino-pdf-baseline.json — this is the
    pre-generalization baseline that stages 2 & 4 must hold or improve. Print matched
    count, mean and max error so the baseline is human-auditable.
  </action>
  <verify>
    <automated>LUIZCAROLINO_UPDATE_BASELINE=1 node tools/run-route-pdf-accuracy-gate.mjs && node tools/run-route-pdf-accuracy-gate.mjs && node -e "const b=require('./parser/__tests__/fixtures/luizcarolino-pdf-baseline.json'); if(!b||typeof b!=='object') throw new Error('no baseline'); console.log('OK baseline captured + gate green on current code');"</automated>
  </verify>
  <done>route-pdf-accuracy-harness.mjs (exports runRoutePdfAccuracyHarness) and run-route-pdf-accuracy-gate.mjs exist; the Luiz Carolino gate RUNS on current code, captured a baseline, and re-running it passes. Dual gate now exists (Siriu DWG gate + Luiz Carolino PDF gate) BEFORE any hack is touched. Commit Stage 1 as one atomic commit.</done>
</task>

<task type="auto">
  <name>Task 3b (Stage 1): Route-agnostic DWG graph-walk accuracy gate + Luiz Carolino DWG baseline on current code</name>
  <files>tools/route-dwg-accuracy-harness.mjs, tools/run-route-dwg-accuracy-gate.mjs, parser/__tests__/fixtures/luizcarolino-dwg-baseline.json</files>
  <action>
    This is the SECOND GATE for stage 3 (graph-walker is DWG-pipeline; the PDF gate
    from Task 3 does NOT exercise graph-walker). Parameterize the Siriu DWG harness
    (tools/siriu-regression-harness.mjs) into a route-agnostic
    tools/route-dwg-accuracy-harness.mjs exporting
    runRouteDwgAccuracyHarness({ pdfPath, dwgRegionPath, groundTruthPath, regionId }).
    Reuse the SAME flow runSiriuRegressionHarness uses, but with route inputs and no
    "siriu" literals:
      1. Parse the route PDF (parsePdf) → { posts, distances, cableSegments,
         pageDimensions, viewportBoxes, utmGridPathsPerPage }.
      2. Load the DWG region from dwgRegionPath (the luizcarolino-dwg-region.json
         built in Task 2) via the SAME buildRegionBundle/createFixtureLibrary helpers
         (lift them into the shared harness; the Siriu harness can import them).
      3. calculateCoordinatesWithDwg(posts, distances, gt[0].lat, gt[0].lon,
         cableSegments, { pageDimensions, viewportBoxes, utmGridPathsPerPage }, library).
      4. errorsByPost = haversineMeters(result.post, gt) for posts with source==='dwg'.
      5. Return { dwgStatus, walkOk, walkCoords, errorsByPost, idxByPost,
         gpsFirstDivergentPost }.
    REFACTOR NOTE: factor the shared bits out of siriu-regression-harness.mjs so both
    Siriu and Luiz Carolino call the same core (favor parameterization over a clone —
    CONTEXT discretion). Keep the Siriu gate behavior byte-identical (re-run
    `node tools/run-siriu-regression-gate.mjs` after refactor to prove no drift).
    run-route-dwg-accuracy-gate.mjs runs the route DWG harness for Luiz Carolino and
    compares per-post DWG error against luizcarolino-dwg-baseline.json using the same
    slack pattern as run-siriu-regression-gate.mjs (ceil((err+0.5)*10)/10). On first run
    (baseline absent OR LUIZCAROLINO_DWG_UPDATE_BASELINE=1), WRITE the baseline from
    current results instead of comparing. Print dwgStatus, walkOk, walkCoords, matched
    count, mean/max error.

    EXPECTED-OUTCOME BRANCH (this is the legitimate, EVIDENCE-BASED version of the
    "DWG second-gate not available" caveat in Tasks 5/6): run the gate once on CURRENT
    un-generalized code.
      - If the walk drives Luiz Carolino with real coverage (walkOk true, a meaningful
        share of the 31 posts get source==='dwg' coordinates), capture
        luizcarolino-dwg-baseline.json. The Luiz Carolino DWG gate now EXISTS and stages
        5/6 must hold it.
      - If the walk genuinely cannot be driven for Luiz Carolino (walkOk false, dwgStatus
        not usable, or near-zero DWG coverage), DO NOT fabricate a gate. Record in the
        SUMMARY exactly why (dwgStatus, walkCoords, coverage), commit the harness/gate
        scaffolding anyway (it is route-agnostic infra), and note that stage 3's second
        gate is genuinely unavailable → stages 5/6 then legitimately fall to the
        discuss-again fallback. This distinguishes "we tried and the DWG walk doesn't
        cover this route" (real) from "we never built the gate" (the gap this task closes).
  </action>
  <verify>
    <automated>node tools/run-siriu-regression-gate.mjs && LUIZCAROLINO_DWG_UPDATE_BASELINE=1 node tools/run-route-dwg-accuracy-gate.mjs && node -e "const fs=require('fs'); const p='parser/__tests__/fixtures/luizcarolino-dwg-baseline.json'; if(!fs.existsSync(p)){ console.log('DWG gate scaffolding built; baseline NOT captured — walk could not drive Luiz Carolino (see gate output). Stage 3 second-gate unavailable → discuss-again is legitimate.'); } else { const b=JSON.parse(fs.readFileSync(p,'utf8')); console.log('OK Luiz Carolino DWG baseline captured; Siriu gate still green (no refactor drift).'); }"</automated>
  </verify>
  <done>tools/route-dwg-accuracy-harness.mjs (exports runRouteDwgAccuracyHarness) and tools/run-route-dwg-accuracy-gate.mjs exist; the Siriu DWG gate still passes byte-identically after the refactor; and EITHER the Luiz Carolino DWG baseline is captured on current code (DWG second gate exists for stages 5/6) OR the gate output documents, with evidence (dwgStatus/walkOk/coverage), that the DWG walk cannot drive Luiz Carolino (making stage-3 discuss-again legitimate). Part of the Stage 1 foundation commit (or its own atomic commit).</done>
</task>

<task type="auto">
  <name>Task 4 (Stage 2): Generalize distance-associator 36/37/38 sheet-break bifurcation</name>
  <files>parser/distance-associator.js</files>
  <action>
    Make the existing generic sheet-break detector (L1639+) ABSORB the 36/37/38 case,
    then delete the hardcoded special-case block (L1614-1637) AND the explicit skip
    (L1651-1653, the `if (junction.number === 36 && tap.number === 37 && mainNext.number === 38) continue;`).
    The generic detector already keys off structural facts: consecutive triple
    (junction, tap=junction+1, mainNext=tap+1), a page spread of exactly 1 with
    tapPage !== mainPage (sheet break), and label-to-junction proximity
    (JUNCTION_CLOSER_RATIO). Determine why the generic detector currently fails to
    fire for 36/37/38 (which is why the special-case + skip exist) and strengthen the
    GENERIC predicate — using only topology + label geometry, ZERO post-number and
    ZERO coordinate literals — so it produces the same edges the special case did:
    upsertEdge(junction, mainNext, mainM, "bifurcation-main"),
    upsertEdge(junction, tap, tapM, "bifurcation-tap"),
    clearEdge(tap, mainNext, "bifurcation-cleared"), and the 37→39 wrong-main cleanup
    generalized to (tap -> junction+3). Do NOT reintroduce the literal labels 10.5/35.5.
    If the generic detector cannot reproduce the 36/37/38 edges without referencing a
    post number or literal, invoke <discuss_again_fallback>: revert this edit, leave the
    tree green, do not commit, record findings in SUMMARY.
  </action>
  <verify>
    <automated>node --test parser/__tests__/distance-associator.test.mjs && node tools/run-siriu-regression-gate.mjs && node tools/run-route-pdf-accuracy-gate.mjs && node -e "const s=require('fs').readFileSync('parser/distance-associator.js','utf8').split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'); if(/number\s*===\s*36|10\.5|35\.5/.test(s)) throw new Error('literal 36/10.5/35.5 still present in code'); console.log('OK both gates green, literals gone');"</automated>
  </verify>
  <done>distance-associator.js has no `number === 36` block and no 10.5/35.5 literals in code (comments allowed); the generic detector produces the 36/37/38 bifurcation edges; BOTH gates green (Siriu 0 regressions AND Luiz Carolino baseline held or improved). Atomic commit. OR: discuss-again fallback triggered, edit reverted, findings recorded.</done>
</task>

<task type="auto">
  <name>Task 5 (Stage 3a): Generalize graph-walker 73/74 gap-off-cable reentry guard</name>
  <files>parser/dwg/graph-walker.js</files>
  <action>
    Replace `fromNum === 73 && toNum === 74` (L1794-1795) with a structural predicate
    that fires exactly where the guard does and is FALSE where the generic algorithm
    would regress. The surrounding branch already requires chosenIdx===undefined,
    conn.gap, routeNextLabel!=null, and (labelM==null || labelM>=100). Design the
    predicate from geometry/topology available at that point: e.g. the current hop is a
    cable gap with no matching consecutive neighbor, the next-label re-entry chord fits
    findGapOffCableReentryByNextLabel's expected span, and no in-cable neighbor satisfies
    the label within tolerance. Reference ZERO post numbers and ZERO coordinate literals.
    Verify by removing the post guard, keeping/strengthening the structural conditions,
    and confirming both gates stay green. If no zero-post-number predicate holds both
    gates, invoke <discuss_again_fallback>: revert, tree green, no commit, record findings.
    NOTE: stage 3 is the DWG pipeline. The SECOND GATE here is the Luiz Carolino DWG
    graph-walk gate built in Task 3b (tools/run-route-dwg-accuracy-gate.mjs over
    luizcarolino-dwg-region.json), NOT the PDF gate (which does not exercise graph-walker).
    Use the DWG gate as the falsification test. ONLY if Task 3b established (with evidence)
    that the DWG walk cannot drive Luiz Carolino at all does the second gate genuinely not
    exist — in that case invoke <discuss_again_fallback> rather than accepting a Siriu-only
    pass. Do not punt by default: if the Task 3b baseline exists, it MUST be held.
  </action>
  <verify>
    <automated>node --test parser/__tests__/graph-walker.test.mjs && node tools/run-siriu-regression-gate.mjs && node tools/run-route-dwg-accuracy-gate.mjs && node -e "const s=require('fs').readFileSync('parser/dwg/graph-walker.js','utf8'); if(/fromNum\s*===\s*73\s*&&\s*toNum\s*===\s*74/.test(s)) throw new Error('73/74 guard still present'); console.log('OK 73/74 guard removed, Siriu + Luiz Carolino DWG gates green');"</automated>
  </verify>
  <done>graph-walker.js has no `fromNum===73 && toNum===74` guard; a structural predicate (zero post numbers, zero coordinate literals) fires in its place; BOTH gates green. Atomic commit. OR: discuss-again fallback triggered (including "DWG second-gate not yet available"), edit reverted, findings recorded.</done>
</task>

<task type="auto">
  <name>Task 6 (Stage 3b): Generalize graph-walker 80/81 off-cable insert guard</name>
  <files>parser/dwg/graph-walker.js</files>
  <action>
    Replace `fromNum === 80 && toNum === 81` (L2038-2039) with a structural predicate.
    The branch already requires a found multi-hop `hop`, nextLabel finite,
    neighbors.includes(hop.endpoint), and the downstream `hopNextDelta > hopNextTol`
    discriminator (the multi-hop's next-span does not fit the next label, signalling the
    real endpoint is an off-cable insert). Design the predicate from the off-chord-span /
    next-label-fit discriminators already present (offChordSpan, hopNextDelta vs
    hopNextTol, offNextDelta) — ZERO post numbers, ZERO coordinate literals. The
    post-number equality should fall out of the existing geometric conditions; if not,
    add a structural condition that distinguishes this case from where the generic algo
    must NOT divert. Verify both gates green. If no zero-post-number predicate holds,
    invoke <discuss_again_fallback>: revert, tree green, no commit, record findings.
    SECOND GATE is the Luiz Carolino DWG gate (Task 3b), same as Task 5 — use it as the
    falsification test; only punt to discuss-again if Task 3b proved the DWG walk cannot
    drive Luiz Carolino at all.
  </action>
  <verify>
    <automated>node --test parser/__tests__/graph-walker.test.mjs && node tools/run-siriu-regression-gate.mjs && node tools/run-route-dwg-accuracy-gate.mjs && node -e "const s=require('fs').readFileSync('parser/dwg/graph-walker.js','utf8'); if(/fromNum\s*===\s*80\s*&&\s*toNum\s*===\s*81/.test(s)) throw new Error('80/81 guard still present'); console.log('OK 80/81 guard removed, Siriu + Luiz Carolino DWG gates green');"</automated>
  </verify>
  <done>graph-walker.js has no `fromNum===80 && toNum===81` guard; a structural predicate replaces it; BOTH gates green. Atomic commit. OR: discuss-again fallback triggered, edit reverted, findings recorded.</done>
</task>

<task type="auto">
  <name>Task 7 (Stage 4): Generalize coordinate-calculator post15/page4 seam-lock anchor</name>
  <files>parser/coordinate-calculator.js</files>
  <action>
    Replace the literal post 15 + literal page 4 seam-lock anchor (L1432-1465). Today:
    `post15 = sorted.find(p=>p.number===15)`, `gpsAtPostViaLabelWalk(..., 15, ...)`,
    `lockPageOriginAtGps(pageTransforms, 4, post15.x, post15.y, gps15.lat, gps15.lon)`.
    Detect the sheet-break boundary post and ITS page generically: the boundary post is
    the post adjacent to the single sheet break on this route (the branch is only reached
    when nBoundary===0 and !multiSheetDetail, i.e. a 2-sheet route with one seam). Derive
    the seam from the existing page structure (pageTransforms keys / sorted posts grouped
    by pageNum / the same sheet-break signal `lockPageOriginsAtSheetBreaksFromPriorProjection`
    uses) — pick the boundary post as the first post on the second sheet (or last on the
    first), and lock THAT post's page origin at its label-walk GPS. Reference ZERO post
    numbers and ZERO page-number literals. Keep gpsAtPostViaLabelWalk driven by the
    detected boundary post number (a derived value, not literal 15). Verify both gates
    green (this is PDF pipeline — Luiz Carolino PDF gate is the real second gate here).
    If no generic seam-boundary detection holds both gates, invoke
    <discuss_again_fallback>: revert, tree green, no commit, record findings.
  </action>
  <verify>
    <automated>node --test parser/__tests__/coordinate-calculator.test.mjs && node tools/run-siriu-regression-gate.mjs && node tools/run-route-pdf-accuracy-gate.mjs && node -e "const s=require('fs').readFileSync('parser/coordinate-calculator.js','utf8'); const seg=s.slice(s.indexOf('} else {',s.indexOf('nBoundary'))); if(/number\s*===\s*15/.test(s.split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n')) || /lockPageOriginAtGps\(\s*pageTransforms\s*,\s*4\s*,/.test(s)) throw new Error('literal post 15 or page 4 still present'); console.log('OK seam-lock generic, gates green');"</automated>
  </verify>
  <done>coordinate-calculator.js seam-lock anchor uses a derived boundary post + derived page (no literal `number===15`, no literal `lockPageOriginAtGps(pageTransforms, 4, ...)`); BOTH gates green. Atomic commit. OR: discuss-again fallback triggered, edit reverted, findings recorded.</done>
</task>

</tasks>

<verification>
- Stage 1 establishes the gates BEFORE any hack is touched:
  `node tools/run-siriu-regression-gate.mjs` (Siriu DWG),
  `node tools/run-route-pdf-accuracy-gate.mjs` (Luiz Carolino PDF — gates stages 2 & 4), and
  `node tools/run-route-dwg-accuracy-gate.mjs` (Luiz Carolino DWG graph-walk — gates stage 3).
- Each generalization is validated against the gate that actually exercises its pipeline:
  stages 2 & 4 (PDF pipeline) → Siriu DWG gate + Luiz Carolino PDF gate; stage 3 (DWG
  pipeline / graph-walker) → Siriu DWG gate + Luiz Carolino DWG gate. Every generalization
  verify also runs a literal-absence grep on the edited file. A predicate that passes only
  Siriu is REJECTED.
- `npm run test:gate` continues to pass after all stages.
- No runtime parser file references the GT bbox numbers, post numbers (36, 15, 73, 74,
  80, 81), or label literals (10.5, 35.5) outside comments.
</verification>

<success_criteria>
- Luiz Carolino second fixture exists (ground-truth JSON, DWG region, PDF baseline) and
  its crop bbox is derived from GT coords inside the build tool only (no runtime leak).
- distance-associator 36/37/38 block + skip removed; generic detector absorbs the case.
- graph-walker 73/74 and 80/81 post guards removed; structural predicates in place.
- coordinate-calculator post15/page4 seam-lock anchor generalized.
- For each generalization that ships: BOTH gates green (Siriu 0 regressions AND Luiz
  Carolino baseline held or improved), committed as its own atomic commit.
- For any generalization that cannot hold both gates: edit reverted, tree green, no
  commit, discuss-again findings recorded in SUMMARY (NO auto-quarantine, NO auto-data-drive).
</success_criteria>

<output>
Create `.planning/quick/260601-k1a-replace-hardcoded-post-number-guards-gra/260601-k1a-SUMMARY.md` when done.
For any stage that hit the discuss-again fallback, the SUMMARY MUST list: the guard, the
specific regression (gate/post/error) it prevents, and the predicates tried with why each
failed the dual gate — so the user can decide together with full context.
</output>
