---
phase: quick-260603-jk7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [LC-LABEL-DIAGNOSE, LC-LABEL-EVALUATE]
must_haves:
  truths:
    - "We know which function/stage assigns each wrong LC label and why it diverges from truth"
    - "We have a documented decision: either a minimal gated fix lands, or a recommendation explains why not"
    - "All accuracy gates (LC PDF, Valmor, Siriu, DWG) stay green if any code change is made"
  artifacts:
    - path: ".planning/quick/260603-jk7-debug-lc-wrong-label-assignments-and-eva/260603-jk7-ROOTCAUSE.md"
      provides: "Written root-cause finding for LC wrong label assignments"
    - path: ".planning/quick/260603-jk7-debug-lc-wrong-label-assignments-and-eva/260603-jk7-DECISION.md"
      provides: "Evaluate-and-decide outcome (fix applied OR documented recommendation)"
  key_links:
    - from: "260603-jk7-ROOTCAUSE.md"
      to: "parser/distance-associator.js"
      via: "file:line citations of the diverging stage"
      pattern: "distance-associator\\.js"
---

<objective>
Debug WHY Luiz Carolino (LC) produces wrong `Distância_Poste` label assignments — which
directly corrupt the route — and EVALUATE whether the label-assignment code itself must
change. This is a DIAGNOSE-then-CONDITIONALLY-FIX task: the outcome may be a minimal gated
code fix, OR a documented recommendation if the fix is large/risky.

Purpose: Recent quick tasks (260602-lbl, 260602-decouple, 260603-acc) repeatedly touched LC
label assignment but the route is still wrong in the posts 1–20 cluster. We need a clean
root-cause statement of the live behavior and a decision on whether the associator code is
the right lever.

Output:
- `260603-jk7-ROOTCAUSE.md` — written root-cause finding (Task 1 exit condition)
- A minimal gated fix with green gates, OR `260603-jk7-DECISION.md` recommendation (Task 2 exit condition)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260603-acc-luizcarolino-joaoborn-route-accuracy/260603-acc-RESEARCH.md
@.planning/quick/260603-acc-luizcarolino-joaoborn-route-accuracy/260603-acc-RESEARCH-label-assignments.md

# Reusable debug scripts already in the tree (untracked — read & run, do NOT commit):
@debug-lc-label-assignments.mjs
@debug-lc-truth-vs-edges.mjs
@debug-lc-offset-vs-deform.mjs
@debug-lc-degree.mjs
@debug-lc-rehome-trace.mjs

<interfaces>
<!-- Label-assignment entry points (the ONLY places LC label→edge decisions are made). -->
<!-- All in parser/distance-associator.js unless noted. Line numbers from main @ 15f0c70. -->

parser/distance-associator.js:
  - associateDistancesRich (L852)            — top-level PDF entry: runs the whole label→edge pipeline
  - inferDistanceEdgesFromLabels (L1138)     — non-consecutive "inferred-label" edges (LC: 3→1, 9→11, 11→8)
  - associateDistances (L143)                — greedy "legacy-midpoint" N→N+1 (LC: short 13.8 wins 6→7 over true ~37.7)
  - refineSequentialWindows (L936)           — "window-refine" swaps (LC: 9→10, 10→11)
  - applyBifurcationJunctionLabelRehome (L1306) — FALSE bifurcations: clears 3→4 and 11→12, makes 2→4 / 10→12
      * calibrated re-validation loop (L1451) — reverts spurious pre-calibration tap edges (KEPT, GATED — Siriu-load-bearing)
      * main bifurcation loop (L1506)         — creates bifurcation-main/tap/cleared edges
      * findTapLegMeters (L1381)              — tap-leg corroboration (opt-in via requireTapLegCorroboration)
      * rehomeBranchArmLabels call (L1909)    — same-page stolen-arm rehome (260603-acc: produces NO rehome on LC)
  - applyJumpbackDistanceCleanup (L463)      — "jumpback-refill"/"-shift" (LC: 20→21 sheet hop = 29.8)
  - classifyBranchArmLabel (L50)             — bearing+on-cable discriminator (geometry only)

parser/dwg/graph-walker.js:
  - rehomedTopologyArmTo (L699), rehomedCrossPageArmTo (L715) — DWG-PATH ONLY; PDF path does not use these

LC accuracy gate (authoritative before/after measure):
  node tools/run-route-pdf-accuracy-gate.mjs
  # per-post err ceilings vs parser/__tests__/fixtures/luizcarolino-pdf-baseline.json
  # refresh ONLY on intentional improvement: LUIZCAROLINO_UPDATE_BASELINE=1 node tools/run-route-pdf-accuracy-gate.mjs

Other gates that must stay green if code changes:
  node tools/run-valmor-accuracy-gate.mjs
  node tools/run-route-dwg-accuracy-gate.mjs
  npm run test:gate        # graph-walker + distance-associator + coordinate-calculator + Siriu regression
</interfaces>

<prior_findings>
260603-acc live-trace already established (VERIFY against current tree, do not trust blindly):
  - LC error splits into TWO regimes: posts 1–20 branch-label DEFORMATION + posts 21–31 rigid ~179m offset.
  - The deformation is NOT caused by rehomeBranchArmLabels (it produces zero rehomes on LC).
  - Suspected real culprits (confirm in Task 1):
      1. FALSE bifurcations at posts 2 and 10 (applyBifurcationJunctionLabelRehome main loop L1506)
         clear true steps 3→4 and 11→12, make 2→4 / 10→12.
      2. Short label 13.8 wins greedy 6→7 over the true ~37.7m chord (associateDistances).
      3. inferred-label phantom edges 3→1, 9→11, 11→8 (inferDistanceEdgesFromLabels).
  - Tap-leg corroboration shipping GLOBALLY regressed post 31 — kept opt-in. Mid-street ratio guard regressed Siriu — not shipped.
</prior_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DIAGNOSE — reproduce and root-cause the wrong LC label assignments</name>
  <files>.planning/quick/260603-jk7-debug-lc-wrong-label-assignments-and-eva/260603-jk7-ROOTCAUSE.md</files>
  <action>
Run the existing debug scripts (do NOT modify or commit them) and capture live output:
  - `node debug-lc-truth-vs-edges.mjs`        — consecutive truth-meters vs associator edge + source; flags |delta|>6m
  - `node debug-lc-label-assignments.mjs`     — full per-label inventory (idx, page, meters, greedy phase, final edge)
  - `node debug-lc-offset-vs-deform.mjs`      — separates rigid offset (posts 21–31) from deformation (posts 1–20)
  - `node debug-lc-degree.mjs`                — label-graph degree per post (which posts are deg>=3 junctions)

For EACH wrong consecutive edge flagged by debug-lc-truth-vs-edges.mjs (e.g. 6→7=13.8 vs truth ~37.7;
3→4 / 11→12 nulled; any others the live run surfaces), trace it to the EXACT producing stage and cite
file:line in parser/distance-associator.js. Confirm or refute the three prior-finding hypotheses:
  (1) false bifurcation @ posts 2 & 10 — confirm via the bifurcation-main loop (L1506) and the
      "[distance-assoc] Bifurcation at post N" warning; note whether requireTapLegCorroboration would suppress it.
  (2) short 13.8 wins greedy 6→7 — confirm the candidate score (gap+ratioPenalty) at associateDistances (L143)
      and why the true long-chord label loses.
  (3) inferred-label phantoms (3→1, 9→11, 11→8) — confirm inferDistanceEdgesFromLabels (L1138) emitted them.

State, in one sentence each, the single mechanism behind each wrong assignment. Distinguish "wrong because
the SOURCE GEOMETRY/label is ambiguous in the PDF" from "wrong because the associator HEURISTIC mis-ranks a
correct-enough signal" — this distinction drives Task 2's go/no-go.

Write findings to 260603-jk7-ROOTCAUSE.md: per-edge table (edge | truth m | got m | source/stage | file:line |
ambiguous-source vs heuristic-bug), the confirmed/refuted hypotheses, and a one-paragraph root-cause summary.
Do NOT change any parser code in this task.
  </action>
  <verify>
    <automated>node debug-lc-truth-vs-edges.mjs 2>&1 | Select-String -Pattern "<=="</automated>
  </verify>
  <done>260603-jk7-ROOTCAUSE.md exists with a per-edge mechanism table citing distance-associator.js file:line for every flagged wrong edge, the three hypotheses each marked confirmed/refuted, and each wrong assignment classified as ambiguous-source vs heuristic-bug. EXIT CONDITION: written root-cause doc.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Does the label-assignment code need to change, and is a minimal contained fix justified?</decision>
  <context>
Task 1 classified each wrong LC edge as either "ambiguous-source" (the PDF itself is ambiguous — code change
risky/large) or "heuristic-bug" (the associator mis-ranks a correct-enough signal — a contained fix exists).
Prior tasks proved that GLOBAL changes here are dangerous: tap-leg corroboration regressed post 31; a mid-street
ratio guard regressed Siriu. So any fix must be minimal, gated, and verified against ALL gates.
  </context>
  <options>
    <option id="apply-minimal-fix">
      <name>Apply a minimal, contained fix (only if Task 1 found a clear heuristic-bug)</name>
      <pros>Improves LC route directly; small surface area; gated</pros>
      <cons>Risk of regressing Siriu/Valmor/post-31 — must verify all gates</cons>
    </option>
    <option id="document-recommendation">
      <name>Stop at a documented recommendation (if fix is large/risky or source is ambiguous)</name>
      <pros>No regression risk; preserves green gates; records the real constraint for a future targeted task</pros>
      <cons>LC route stays wrong for now</cons>
    </option>
  </options>
  <resume-signal>Select: apply-minimal-fix or document-recommendation</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EVALUATE & ACT — minimal gated fix OR documented recommendation</name>
  <files>parser/distance-associator.js, .planning/quick/260603-jk7-debug-lc-wrong-label-assignments-and-eva/260603-jk7-DECISION.md</files>
  <behavior>
    - If apply-minimal-fix: the targeted wrong edge(s) from Task 1 now resolve to truth (debug-lc-truth-vs-edges
      shows the previously-flagged edge within delta tol), AND no gate regresses.
    - If document-recommendation: no parser code changes; DECISION.md records why and the precise future fix shape.
    - In BOTH cases: LC PDF gate, Valmor gate, DWG gate, and npm run test:gate all PASS.
  </behavior>
  <action>
Honor the checkpoint decision.

IF apply-minimal-fix:
  - Make the SMALLEST change in parser/distance-associator.js that fixes ONLY the Task-1-identified
    heuristic-bug edge(s). Prefer a tightly-scoped, geometry-only predicate; NO post-number literals
    (project rule — see 260602-decouple). If the fix would need a new option flag, default it to the
    behavior that keeps existing gates green and enable it via opts for the LC path only if necessary.
  - Capture LC error BEFORE and AFTER: `node tools/run-route-pdf-accuracy-gate.mjs` (and debug-lc-truth-vs-edges.mjs).
  - Run ALL gates: LC PDF, Valmor, DWG, and `npm run test:gate`. If ANY regresses, REVERT the code change and
    fall back to document-recommendation (record the regression in DECISION.md).
  - Only if the change is a genuine intentional improvement AND all other gates pass, refresh the LC baseline:
    `LUIZCAROLINO_UPDATE_BASELINE=1 node tools/run-route-pdf-accuracy-gate.mjs`, then re-run the LC gate to confirm green.

IF document-recommendation:
  - Make NO parser code changes. Write the precise recommended fix shape (which function, what signal, why it's
    risky now, what prerequisite would unblock it) into DECISION.md.

In BOTH cases, write 260603-jk7-DECISION.md: chosen option, before/after LC mean+max error, gate results
(pass/fail for each of the 4 gate commands), and the rationale. This doc is the exit condition.
  </action>
  <verify>
    <automated>node tools/run-route-pdf-accuracy-gate.mjs; node tools/run-valmor-accuracy-gate.mjs; node tools/run-route-dwg-accuracy-gate.mjs; npm run test:gate</automated>
  </verify>
  <done>260603-jk7-DECISION.md exists recording the chosen option with before/after LC error and a pass result for all four gate commands. If code changed, the targeted wrong edge now matches truth and no gate regressed; if not, no parser code changed. EXIT CONDITION: passing gates + DECISION.md (fix) OR documented recommendation + still-green gates (no-fix).</done>
</task>

</tasks>

<verification>
- Task 1 exit: 260603-jk7-ROOTCAUSE.md with per-edge file:line mechanisms and ambiguous-source vs heuristic-bug classification.
- Task 2 exit: 260603-jk7-DECISION.md; ALL of LC PDF / Valmor / DWG / test:gate gates PASS.
- No debug-lc-*.mjs script is committed (they are untracked diagnostics).
- If code changed: zero post-number literals introduced (geometry-only predicate).
</verification>

<success_criteria>
- Root cause of LC wrong label assignments is written down with exact file:line per wrong edge.
- A clear decision exists: minimal gated fix landed (gates green, LC error improved) OR a documented
  recommendation explains why the fix is deferred and what would unblock it.
- All four accuracy/regression gates are green at the end of the task.
</success_criteria>

<output>
Create `.planning/quick/260603-jk7-debug-lc-wrong-label-assignments-and-eva/260603-jk7-SUMMARY.md` when done.
</output>
