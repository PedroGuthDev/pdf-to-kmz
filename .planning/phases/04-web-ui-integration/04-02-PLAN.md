---
phase: 4
plan: "04-02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - index.html
autonomous: true
requirements:
  - UI-03
  - UI-04
  - CUST-01
  - CUST-02
  - CUST-03
---

<objective>
Deliver the customize-and-calculate slice: Appearance block in Step 2 wired to `mergeOptions()`, collapsed second-anchor GPS expander, and KMZ options passed into `buildKml` on download (and stored on calculate for consistency).

After this plan, user can set Earth preset colors, line width, label scale, route note, then calculate and download KMZ reflecting those choices.
</objective>

<must_haves>
truths:
  - Appearance controls live in #coordForm above Calculate Route
  - Colors use PRESET_COLORS keys only (no hex picker)
  - lineDescription textarea is present and passed to mergeOptions
  - Last-post GPS is hidden behind Improve accuracy expander by default
  - Download and calculate use mergeOptions(readAppearanceOptions()) not {}
artifacts:
  - index.html appearance markup and readAppearanceOptions helper
</must_haves>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Download uses empty options | high | Single readAppearanceOptions(); use on download handler |
| Invalid lineWidth breaks KML | low | Clamp select values to 1–8 integers |
</threat_model>

<tasks>

<task id="T-04-02-01" type="execute">
<title>Appearance block markup and swatch grids</title>
<read_first>
- parser/kmz-defaults.js (DEFAULT_OPTIONS, PRESET_COLORS)
- .planning/phases/04-web-ui-integration/04-UI-SPEC.md (Appearance block)
- .planning/phases/04-web-ui-integration/04-CONTEXT.md (D-CUS-01..05)
- index.html (#coordForm, field-row, hint classes)
</read_first>
<files>index.html</files>
<action>
Inside `#coordForm`, after GPS fields and before Calculate Route button, add subsection:

**Heading:** `h3` **KMZ appearance**

Controls:
1. **Post icon color** — button swatches for each `PRESET_COLORS` key, `data-preset` + `data-target="iconColor"`, default selected = `green`
2. **Route line color** — swatches, default `red`
3. **Line width** — `select#lineWidthSelect` options 1–8, default selected 3
4. **Label color** — swatches, default `white`
5. **Label scale** — `select#labelScaleSelect` values 0.8, 1, 1.2, 1.5 default 1
6. **Route note** — `textarea#lineDescriptionInput` placeholder per UI-SPEC

Import `PRESET_COLORS` and `DEFAULT_OPTIONS` in module script (already imports mergeOptions).

CSS: `.swatch` per PATTERNS; selected `aria-checked="true"`; keyboard accessible (button type="button").

No icon size control (D-CUS-04).
</action>
<acceptance_criteria>
- All six control groups visible in #coordForm above Calculate Route
- Six preset swatches per color group matching PRESET_COLORS keys
- lineWidth select includes values 1 through 8 with 3 selected by default
- labelScale select has options 0.8, 1, 1.2, 1.5
- No hex color input elements in DOM
</acceptance_criteria>
</task>

<task id="T-04-02-02" type="execute">
<title>readAppearanceOptions and swatch interaction</title>
<read_first>
- parser/kmz-defaults.js (mergeOptions)
- .planning/phases/04-web-ui-integration/04-PATTERNS.md (download analog)
</read_first>
<files>index.html</files>
<action>
Implement `readAppearanceOptions()` returning object with keys: `iconColor`, `lineColor`, `labelColor`, `lineWidth` (number), `labelScale` (number), `lineDescription` (string).

Swatch click handler: for each `data-target` group, set one swatch `aria-checked="true"`, others false; store selection on `data-preset`.

`function resetAppearanceDefaults()` — reset to DEFAULT_OPTIONS values; call from `resetSession()`.

Ensure appearance inputs respect `setParsingUi` disabled state from 04-01.
</action>
<acceptance_criteria>
- readAppearanceOptions returns lineWidth as number 1–8
- readAppearanceOptions returns preset keys matching selected swatches
- resetSession restores default swatch selections and empty lineDescription
- Only one swatch per color group has aria-checked true
</acceptance_criteria>
</task>

<task id="T-04-02-03" type="execute">
<title>Second-anchor expander (D-STP-03)</title>
<read_first>
- .planning/phases/04-web-ui-integration/04-UI-SPEC.md (Second-anchor expander)
- index.html (#gpsInputLast)
</read_first>
<files>index.html</files>
<action>
Move `#gpsInputLast` into collapsible panel `#secondAnchorPanel` default hidden (`hidden` attribute or `display:none`).

Add trigger button **Improve accuracy (2nd anchor)** toggles panel visibility.

Keep label/hint for last post inside panel. Use `hidden` attribute pattern preferred over animated max-height (D-STP-03, reduced motion).

resetSession collapses panel and clears last post input.
</action>
<acceptance_criteria>
- Last post GPS field not visible on initial page load after parse
- Clicking Improve accuracy (2nd anchor) reveals gpsInputLast field
- Calculate Route still reads last post when expander open and field filled
- resetSession hides expander and clears last post value
</acceptance_criteria>
</task>

<task id="T-04-02-04" type="execute">
<title>Wire mergeOptions on download (and calc path)</title>
<read_first>
- index.html (downloadKmzBtn handler, calcBtn handler)
- parser/kml-builder.js (options parameter)
</read_first>
<files>index.html</files>
<action>
In download handler replace `mergeOptions({})` with `mergeOptions(readAppearanceOptions())`.

Optionally store `lastAppearanceOptions` on successful Calculate Route for debugging — not required if download always re-reads DOM.

Verify buildKml receives lineDescription, colors, lineWidth, labelScale.

Keep Phase 3 download UX: Building KMZ…, stats panel, [kmz] warnings, disabled states.
</action>
<acceptance_criteria>
- downloadKmzBtn handler calls mergeOptions(readAppearanceOptions())
- Changing line color swatch before download changes KML line color (manual spot-check or inspect kml string for style)
- lineDescription non-empty appears in generated KML LineString description when set
- calculateCoordinates path unchanged except appearance read not required for coords
</acceptance_criteria>
</task>

</tasks>

<verification>
- Manual: select blue line swatch, download KMZ, confirm line color differs from default red in doc.kml
- Manual: expand second anchor, enter coords, verify calc warnings behavior unchanged
</verification>
