---
phase: 4
plan: "04-03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - index.html
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04
  - UI-05
---

<objective>
Complete the product slice: optional KMZ filename, user-facing copy pass, developer-tools footer toggle, and Phase 4 human UAT checklist. Ensures UI-02 download polish and ROADMAP success criteria 4–6.

After this plan, the single-page app meets all Phase 4 success criteria for a field engineer workflow.
</objective>

<must_haves>
truths:
  - User can download KMZ with one click using custom or default filename
  - Page lede describes upload → GPS → appearance → download (not parser internals)
  - Debug and reference compare sections hidden until user toggles developer tools
  - Full workflow works in modern browser without backend
artifacts:
  - index.html filename input, footer toggle, updated copy
  - .planning/phases/04-web-ui-integration/04-HUMAN-UAT.md (checklist)
</must_haves>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Path traversal in filename | low | sanitizeKmzFilename strips slashes and illegal chars |
| Developer tools leak to normal users | low | default display none; no auto-show on parse |
</threat_model>

<tasks>

<task id="T-04-03-01" type="execute">
<title>KMZ filename input and resolveKmzFilename</title>
<read_first>
- .planning/phases/04-web-ui-integration/04-CONTEXT.md (D-DL-01)
- .planning/phases/04-web-ui-integration/04-RESEARCH.md (filename sanitization)
- index.html (#resultSection, download handler)
</read_first>
<files>index.html</files>
<action>
Add to Step 3 above Download KMZ:
- Label **KMZ filename (optional)**
- `input#kmzFilenameInput` type text, empty value, placeholder "Uses PDF name if empty"

Implement `resolveKmzFilename(userInput, pdfFileName)` per RESEARCH (strip illegal chars, default basename from `lastPdfFile.name`, ensure `.kmz` suffix).

In download handler set `a.download = resolveKmzFilename(kmzFilenameInput.value, lastPdfFile?.name)`.

resetSession clears kmzFilenameInput.
</action>
<acceptance_criteria>
- Empty filename input downloads file named from PDF basename plus .kmz extension
- User input "my-route" becomes my-route.kmz
- Characters like slash and backslash stripped or replaced in output filename
- resetSession clears filename input
</acceptance_criteria>
</task>

<task id="T-04-03-02" type="execute">
<title>Copy pass and developer tools toggle</title>
<read_first>
- .planning/phases/04-web-ui-integration/04-UI-SPEC.md (Copywriting Contract)
- .planning/phases/04-web-ui-integration/04-CONTEXT.md (D-COPY-01, D-DBG-01, D-DBG-02)
- PRODUCT.md
</read_first>
<files>index.html</files>
<action>
1. Replace `.lede` with: "Upload an INFOVIAS route PDF, anchor post #1 in GPS, customize how it looks in Google Earth, then download a KMZ file."

2. Ensure `#debugSection` and `#refCompareSection` have `display: none` in HTML default (not only JS).

3. Add page footer `#devToolsToggle` link text **Show developer tools** / **Hide developer tools** toggling display of both sections. Do not call showDebug on parse success.

4. Audit status strings against UI-SPEC table (Parse complete → keep or align with "Parse complete." / success copy).

5. Step 2 heading/copy remains clear; coord hint unchanged unless typo fix only.
</action>
<acceptance_criteria>
- Lede text matches D-COPY-01 sentence (INFOVIAS route PDF…)
- Fresh page load hides debug and ref compare sections
- Footer toggle shows both sections; second click hides
- Successful PDF parse does not auto-show debug or reference sections
</acceptance_criteria>
</task>

<task id="T-04-03-03" type="execute">
<title>setParsingUi includes appearance + filename controls</title>
<read_first>
- index.html (setParsingUi from 04-01)
- 04-02 appearance element ids
</read_first>
<files>index.html</files>
<action>
Extend `setParsingUi(blocked)` to disable/enable:
- All appearance swatches, selects, lineDescription textarea
- kmzFilenameInput (when result section visible logic — disable during parse regardless)
- Ensure Download KMZ disabled during parse (already expected)

Confirm coord form hidden until parse success — appearance inside coord form inherits visibility.
</action>
<acceptance_criteria>
- During active parse, appearance swatches do not respond to clicks
- After parse completes, appearance controls are enabled
- kmzFilenameInput disabled during parse
</acceptance_criteria>
</task>

<task id="T-04-03-04" type="execute">
<title>Create 04-HUMAN-UAT.md checklist</title>
<read_first>
- .planning/ROADMAP.md (Phase 4 success criteria)
- .planning/phases/01-pdf-parser-engine/01-HUMAN-UAT.md (format reference)
- .planning/phases/04-web-ui-integration/04-UI-SPEC.md
</read_first>
<files>.planning/phases/04-web-ui-integration/04-HUMAN-UAT.md</files>
<action>
Create human UAT doc with checkbox table covering:
- UI-01 drag + browse upload, Change file reset
- UI-05 staged progress messages visible
- UI-03 GPS paste + expander second anchor
- UI-04 appearance swatches affect KMZ (Earth visual check)
- UI-02 download one click + filename default/custom
- ROADMAP criteria 6 (clean single page, modern browser)

Fixture: João Born PDF in repo root. Steps: upload → anchor → customize → calculate → download → open in Google Earth.

Mark KMZ-05 cross-check if not already done in Phase 3.
</action>
<acceptance_criteria>
- File exists at .planning/phases/04-web-ui-integration/04-HUMAN-UAT.md
- Contains at least 8 checkbox items mapping UI-01 through UI-05
- References João Born PDF fixture path
</acceptance_criteria>
</task>

</tasks>

<verification>
- Complete 04-HUMAN-UAT.md manually in browser
- `node --test` passes
- No regression: Calculate Route preview still shows 10 posts
</verification>
