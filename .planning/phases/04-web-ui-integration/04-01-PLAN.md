---
phase: 4
plan: "04-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - parser/pdf-parser.js
autonomous: true
requirements:
  - UI-01
  - UI-05
---

<objective>
Deliver the upload vertical slice: drag-and-drop + Browse file picker, staged parse progress in `#status`, UI blocking during parse, full session reset on new PDF, and optional Start over (reload).

After this plan, a user can upload a PDF with clear feedback and cannot interact with Step 2/3 until parse completes or fails.
</objective>

<must_haves>
truths:
  - User can upload via drag-and-drop or Browse with the same parse flow
  - After valid PDF, upload zone shows filename and Change file
  - Selecting a different PDF clears GPS, results, KMZ state, and warnings
  - During parse, coord form, appearance (when added later), and download stay disabled
  - Progress messages are specific (Loading PDF, page N/M, Reading post numbers)
artifacts:
  - index.html upload zone markup and handlers
  - parser/pdf-parser.js onProgress hook
</must_haves>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| UI left disabled after parse error | medium | `finally { setParsingUi(false) }` in handlePdfFile |
| Stale KMZ blob after re-upload | medium | resetSession calls revokeKmzObjectUrl |
| Non-PDF dropped | low | type/extension check before parse; D-UPL-04 error in #status |
</threat_model>

<tasks>

<task id="T-04-01-01" type="execute">
<title>Add parsePdf onProgress hook</title>
<read_first>
- parser/pdf-parser.js (parsePdf, page loop ~317)
- .planning/phases/04-web-ui-integration/04-RESEARCH.md (progress hook section)
</read_first>
<files>parser/pdf-parser.js</files>
<action>
Extend signature to `export async function parsePdf(arrayBuffer, hooks = {})`.

Extract `const onProgress = typeof hooks.onProgress === 'function' ? hooks.onProgress : null`.

Invoke at minimum:
1. After `getDocument` starts / before page loop: `{ stage: 'loading', message: 'Loading PDF…' }`
2. Start of each page iteration: `{ stage: 'pages', pageNum, numPages: pdfDoc.numPages, message: \`Reading page ${pageNum} of ${pdfDoc.numPages}…\` }`
3. Before OCR post-processing (after page loop, where OCR batches run): `{ stage: 'ocr', message: 'Reading post numbers…' }`

Do not change return shape or error branches. Existing `parsePdf(buf)` one-arg calls must work unchanged.
</action>
<acceptance_criteria>
- `parsePdf` accepts optional second argument with `onProgress` callback
- One-arg `parsePdf(buf)` behavior unchanged (no throw)
- Progress callback receives objects with `message` string
- Page progress includes numeric `pageNum` and `numPages` when stage is `pages`
</acceptance_criteria>
</task>

<task id="T-04-01-02" type="execute">
<title>Upload zone HTML/CSS — drop zone, Browse, selected state</title>
<read_first>
- index.html (#uploadSection, :root tokens, btn-secondary)
- .planning/phases/04-web-ui-integration/04-UI-SPEC.md (Upload zone)
- DESIGN.md (Field Notebook tokens)
- .planning/phases/04-web-ui-integration/04-PATTERNS.md
</read_first>
<files>index.html</files>
<action>
Replace `#uploadSection` contents with:
- `#uploadZone` bordered region (per UI-SPEC / PATTERNS)
- Hidden `#pdfInput` (`accept=".pdf"`)
- Idle: label **Upload route PDF**, hint "Drag a file here or choose Browse.", **Browse** button (`btn-secondary`) triggers `pdfInput.click()`
- Selected state `#uploadSelected` (hidden by default): show `#uploadFileName` text + **Change file** button

Add CSS: `#uploadZone`, `.drag-over`, no scale animation; honor `prefers-reduced-motion` (existing rule).

Update lede to D-COPY-01 text in plan 04-03 if not done yet — optional stub acceptable in 04-01.
</action>
<acceptance_criteria>
- `#uploadZone` visible with border and padding using CSS variables
- Browse opens native file picker via hidden input
- Selected state shows filename and Change file; idle drop zone hidden when selected
- No new external CSS frameworks
</acceptance_criteria>
</task>

<task id="T-04-01-03" type="execute">
<title>handlePdfFile, drag-drop, setParsingUi, resetSession</title>
<read_first>
- index.html (pdfInput change handler, clearResults, showStatus)
- .planning/phases/04-web-ui-integration/04-CONTEXT.md (D-UPL-01..04, D-PRG-01..04)
- .planning/phases/04-web-ui-integration/04-RESEARCH.md (session reset checklist)
</read_first>
<files>index.html</files>
<action>
Implement:

1. `let lastPdfFile = null` — set on successful file pick.

2. `function resetSession()` — full reset per RESEARCH: revoke KMZ URL, clear parse/calc state, GPS fields, hide result/coord/summary/warnings, reset upload UI to idle, clear filename input placeholder if present, hide `#debugSection` and `#refCompareSection`, reset file input value.

3. `function setParsingUi(blocked)` — when true: disable `#coordForm` inputs/buttons, `#downloadKmzBtn`, upload Browse/Change file; set `aria-busy="true"` on `#uploadSection` or body; when false reverse.

4. `async function handlePdfFile(file)` — validate PDF (type `application/pdf` or case-insensitive `.pdf` suffix); on invalid `showStatus` error and return; 50MB guard unchanged; call `resetSession()` then show selected filename UI; `setParsingUi(true)`; `parsePdf(buf, { onProgress: ({ message }) => showStatus(message, 'info') })`; existing error/success branches; **remove** auto `showDebug` / `refCompareSection.style.display = 'block'` on success (dev tools plan 04-03); `finally { setParsingUi(false) }`.

5. Wire: `pdfInput` change → `handlePdfFile(files[0])`; dragover/dragleave/drop on `#uploadZone`; Change file → `resetSession()` + show idle zone.

6. Optional footer or upload area link **Start over** → `location.reload()` (D-PRG-04).

Rename or delegate old `clearResults()` into `resetSession()` — no duplicate logic.
</action>
<acceptance_criteria>
- Dropping a valid PDF triggers same flow as Browse
- Invalid non-PDF shows `#status.error` without breaking upload zone
- During parse, Calculate Route and coord inputs are disabled
- Change file calls resetSession and returns to idle upload UI
- Successful parse does not auto-display debug or reference compare sections
- `parsePdf` progress updates `#status` with info class
</acceptance_criteria>
</task>

</tasks>

<verification>
- Manual: drag PDF, see staged messages, Step 2 appears after success
- Manual: pick second PDF — GPS and download state cleared
- `node --test` (parser tests) still pass
</verification>
