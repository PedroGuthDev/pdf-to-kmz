---
phase: 07-solver-prerequisites
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - parser/__tests__/fixtures/valmor-post-positions-truth.json
  - tools/run-valmor-post-position-gate.mjs
autonomous: false
requirements: [SOLVE-05]

must_haves:
  truths:
    - "tools/run-valmor-post-position-gate.mjs exits 0 against hand-verified anchors for all 11 Valmor posts"
    - "The Valmor PDF parses through parsePdf and yields usable per-post x,y (verified before fixture is committed)"
    - "If the Valmor PDF does NOT produce usable pole symbols, the blocker is surfaced to the user, not silently exempted (D-06)"
  artifacts:
    - path: "parser/__tests__/fixtures/valmor-post-positions-truth.json"
      provides: "Valmor hand-known PDF pole anchors, all 11 posts (D-06)"
      contains: '"tolerancePt"'
    - path: "tools/run-valmor-post-position-gate.mjs"
      provides: "Valmor layer-B position gate (clone of LC gate)"
      min_lines: 80
  key_links:
    - from: "tools/run-valmor-post-position-gate.mjs"
      to: "parser/pdf-parser.js parsePdf"
      via: "dynamic import + ArrayBuffer slice parse of the Valmor PDF"
      pattern: "parsePdf"
    - from: "tools/run-valmor-post-position-gate.mjs"
      to: "parser/__tests__/fixtures/valmor-post-positions-truth.json"
      via: "Math.hypot(p.x - t.x, p.y - t.y) > tolPt"
      pattern: "Math.hypot"
---

<objective>
Build the Valmor per-post PDF position fixture + gate (D-06). Valmor was previously
treated as DWG-only; this plan FIRST verifies the Valmor PDF parses with usable pole
symbols, then clones the LC position gate. D-06 forbids treating Valmor as exempt — a
parse failure is a real blocker to escalate, not a reason to skip.

Purpose: SOLVE-05 / ROADMAP SC-1 require a green per-post position gate for Valmor.
Output: valmor-post-positions-truth.json + run-valmor-post-position-gate.mjs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-solver-prerequisites/07-RESEARCH.md
@.planning/phases/07-solver-prerequisites/07-PATTERNS.md

<interfaces>
<!-- PDF parse viability check (PATTERNS Shared Patterns — ArrayBuffer slice) -->
```js
import "fake-indexeddb/auto";
const { parsePdf } = await import("../parser/pdf-parser.js");
const buf = readFileSync(path.join(ROOT, "INFOVIAS_PJC INTERNET_Palhoça_RUA VALMOR FRANCISCO_v1.pdf"));
const parsed = await parsePdf(buf.buffer.slice(buf.byteOffset, buf.byteOffset + buf.byteLength));
// usable iff parsed.posts has entries with finite numeric x,y
```

<!-- Clone source + fixture shape identical to 07-02 (run-lc-post-position-gate.mjs). Swap PDF_PATH/TRUTH_PATH and env to VALMOR_POST_POS_TOL_PT. 11 posts. -->
</interfaces>
</context>

<tasks>

<task type="auto" id="07-03-T1">
  <name>Task 1: Verify Valmor PDF parses with usable per-post x,y</name>
  <read_first>tools/run-lc-post-position-gate.mjs (parse boilerplate); INFOVIAS_PJC INTERNET_Palhoça_RUA VALMOR FRANCISCO_v1.pdf; RESEARCH §8 secondary risk (Valmor PDF parse viability, Q5)</read_first>
  <files>tools/run-valmor-post-position-gate.mjs</files>
  <action>
    Create `tools/run-valmor-post-position-gate.mjs` as a clone of run-lc-post-position-gate.mjs
    (swap PDF_PATH to the Valmor PDF, TRUTH_PATH to valmor-post-positions-truth.json, env var
    `VALMOR_POST_POS_TOL_PT`, keep `import "fake-indexeddb/auto"`). Add the same
    `VALMOR_POST_POS_UPDATE_BASELINE=1`/absent-file capture branch as 07-02 (dump
    { number, pageNum, x, y } for all parsed posts + print anchorX/anchorY to stderr).
    BEFORE writing any fixture, run the capture branch and assert the Valmor PDF parses:
    `parsed.error` is falsy AND at least 11 posts have finite numeric x,y. If parsing fails or
    produces no usable pole symbols, print a clear blocker message ("Valmor PDF did not parse
    usable per-post positions — escalate per D-06; do NOT exempt Valmor") and exit non-zero so
    the checkpoint can surface it to the user.
  </action>
  <verify>
    <automated>VALMOR_POST_POS_UPDATE_BASELINE=1 node tools/run-valmor-post-position-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - run-valmor-post-position-gate.mjs exists, imports fake-indexeddb/auto, and has the capture branch.
    - When the Valmor PDF parses, a candidate valmor-post-positions-truth.json (11 posts, _meta tolerancePt 50) is written and the command exits 0.
    - When the PDF does not yield usable positions, the command exits non-zero with the explicit D-06 blocker message (no fixture written).
  </acceptance_criteria>
  <done>Valmor PDF parse viability is proven (or the blocker is raised); candidate fixture captured if viable.</done>
</task>

<task type="checkpoint:human-verify" id="07-03-T2" gate="blocking">
  <name>Task 2: Human-verify Valmor anchors (or handle parse blocker)</name>
  <action>Present the Valmor anchor dump (or the D-06 parse-blocker message) to the user and pause for verification/decision before locking the fixture (no automation in this step).</action>
  <what-built>tools/run-valmor-post-position-gate.mjs and (if the PDF parsed) a candidate valmor-post-positions-truth.json covering all 11 Valmor posts, captured from the Valmor PDF parse with anchorX/anchorY printed for verification.</what-built>
  <how-to-verify>
    1. If Task 1 raised the D-06 parse blocker, the executor must STOP and report it — decide with the user whether to repair the parse path or accept Valmor as an explicit exception (overrides D-06; user call).
    2. If the PDF parsed: spot-check 3-5 posts (first + last + middle) against the Valmor PDF sheet `INFOVIAS_PJC INTERNET_Palhoça_RUA VALMOR FRANCISCO_v1.pdf` — confirm captured x,y land on the correct pole symbols.
    3. Confirm all 11 posts are present.
    4. Approve to lock, or list posts whose anchors need correction.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the Valmor position fixture, list posts to correct, or confirm how to handle a parse blocker.</resume-signal>
</task>

</tasks>

<verification>
- `node tools/run-valmor-post-position-gate.mjs` exits 0 (no env flag) against the locked 11-post fixture.
- OR the parse blocker is documented and a user decision recorded in the SUMMARY.
</verification>

<success_criteria>
Valmor has a green per-post PDF position gate backed by user-verified hand-known anchors
for all 11 posts (or the parse-viability blocker is surfaced per D-06) — one of the four
SOLVE-05 position gates.
</success_criteria>

<output>
After completion, create `.planning/phases/07-solver-prerequisites/07-03-SUMMARY.md`
</output>
