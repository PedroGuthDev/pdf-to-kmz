---
phase: 07-solver-prerequisites
plan: 06
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - parser/post-positioning.js
  - parser/__tests__/fixtures/luizcarolino-post-positions-truth.json
autonomous: false
requirements: [SOLVE-05]

must_haves:
  truths:
    - "tools/run-lc-post-position-gate.mjs exits 0 — the LC layer-B collapse (posts 9/10/11) is fixed"
    - "The Siriu 1.0-pt position gate stays green at every checkpoint (no regression through shared layer-B code)"
    - "The fix is an additive predicate in assignPolesGloballyByLabels — no Siriu-calibrated constant is edited"
    - "All other gates (JB/Valmor position, junction tests, txt-accuracy, residual, Siriu regression) stay green at every commit (D-11)"
  artifacts:
    - path: "parser/post-positioning.js"
      provides: "additive LC shared-symbol collapse predicate in assignPolesGloballyByLabels (D-09/D-10)"
      contains: "assignPolesGloballyByLabels"
    - path: "parser/__tests__/fixtures/luizcarolino-post-positions-truth.json"
      provides: "LC hand-known anchors extended to D-10 scope"
      contains: '"tolerancePt"'
  key_links:
    - from: "parser/post-positioning.js assignPolesGloballyByLabels"
      to: "tools/run-lc-post-position-gate.mjs"
      via: "corrected post.x/post.y within tolerance of hand anchors"
      pattern: "assignPolesGloballyByLabels"
    - from: "the LC fix"
      to: "tools/run-siriu-post-position-gate.mjs"
      via: "Siriu byte-identical placement (1.0 pt gate stays green)"
      pattern: "run-siriu-post-position-gate"
---

<objective>
The ONLY production-code change in Phase 7: fix the Luiz Carolino layer-B placement so
`tools/run-lc-post-position-gate.mjs` greens (D-09/D-10), via an ADDITIVE predicate in
`assignPolesGloballyByLabels` (parser/post-positioning.js ~line 1554) that recognizes the
LC shared-symbol collapse (posts 9/10/11) WITHOUT editing any Siriu-calibrated constant.
This is the primary Pitfall-2 risk: four prior LC fixes each regressed 12-89 Siriu posts.
The 1.0-pt Siriu position gate (live in test:gate since 07-01) is the red-line instrument.

Purpose: SOLVE-05 requires a green LC per-post position gate before Phase 8.
Output: additive predicate in post-positioning.js + extended LC position truth.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-solver-prerequisites/07-RESEARCH.md
@.planning/phases/07-solver-prerequisites/07-PATTERNS.md
@.planning/STATE.md

<interfaces>
<!-- Fix target: parser/post-positioning.js -> assignPolesGloballyByLabels (~line 1554, layer B). -->
<!-- Pattern: "generic predicate, not literal guard" (260601-k1a). Add a predicate recognizing the
     shared-symbol collapse (multiple LC posts assigned the same/wrong pole symbol) without touching
     any path Siriu exercises. NEVER edit Siriu-calibrated constants in post-positioning.js or
     distance-associator.js. NEVER re-seed Siriu baseline (SIRIU_POST_POS_UPDATE_BASELINE=1) to mask. -->

<!-- All-green checkpoint command (run after EVERY incremental change, D-11): -->
//   node tools/run-siriu-post-position-gate.mjs   (1.0 pt — hard red-line)
//   node tools/run-lc-post-position-gate.mjs      (the target — must go green)
//   node tools/run-joaoborn-post-position-gate.mjs && node tools/run-valmor-post-position-gate.mjs
//   node --test parser/__tests__/branch-traversal*.test.mjs
//   node tools/run-siriu-regression-gate.mjs && node tools/run-residual-gate.mjs
//   node tools/run-*-txt-accuracy-gate.mjs
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" id="07-06-T1" gate="blocking">
  <name>Task 1: Decide LC posts-21-31 scope (P7 fix vs Phase 8 solver)</name>
  <action>Present the scope decision below to the user and pause for their selection before extending the LC truth or writing the predicate (no automation in this step).</action>
  <decision>Is the LC posts-21-31 ~179 m rigid offset in Phase 7 LC-fix scope, or deferred to the Phase 8 solver?</decision>
  <context>
    D-10 says "fix all bad-tier / position-failing posts." But RESEARCH §3/Q2 shows posts 21-31
    are an absolute-position / solver problem (the ~179 m rigid offset), already locked as the
    Phase 5 residual gate MUST-FAIL fixture; the LC position-fixture deliberately excludes 21-31
    (_meta.scope). Posts 9/10/11 are the genuine layer-B collapse this plan fixes. We need the
    user to confirm scope before writing the fixture extension + predicate.

    NOTE: 07-05 already shipped the LC txt-accuracy gate with posts 21-31 EXCLUDED from its
    zero-bad-tier exit rule (provisional default = layerb-only), so the Wave-2 gate already exits
    0. This decision FINALIZES that scope; choosing full-scope requires re-widening both the LC
    position truth (here) AND the LC txt-accuracy gate (07-05) to include 21-31.
  </context>
  <options>
    <option id="layerb-only">
      <name>P7 fixes layer-B collapse only (posts 9/10/11 + any other position-gate failures within 1-20); 21-31 rigid offset stays a Phase 8 solver target (residual gate keeps it must-fail).</name>
      <pros>Matches the proven failure mode; keeps the 21-31 must-fail regression fixture intact; lowest Siriu-regression risk; the LC txt-accuracy gate already exits 0 with 21-31 scoped out (07-05) — no further gate change needed.</pros>
      <cons>LC txt-accuracy gate leaves posts 21-31 bad-tier (reported, not gating) until Phase 8 — the LC txt-accuracy zero-bad-tier rule must be marked a soft fence in the 07-07 audit (D-18).</cons>
    </option>
    <option id="full-scope">
      <name>P7 also attempts to fix posts 21-31 (full D-10 bad-tier scope).</name>
      <pros>Strict literal reading of D-10; LC fully green vs txt in P7.</pros>
      <cons>21-31 is a rigid-offset/solver problem, not layer-B; risks conflicting with the Phase 5 must-fail fixture and is high Siriu-regression risk; requires re-widening the 07-05 LC txt-accuracy gate (remove the 21-31 exclusion) and the LC position truth; likely out of P7's fixture-engineering remit.</cons>
    </option>
  </options>
  <resume-signal>Select: layerb-only (keep the 07-05 LC gate scope as-is) or full-scope (executor re-widens the LC position truth AND the 07-05 LC txt-accuracy gate to include 21-31, then writes the predicate).</resume-signal>
</task>

<task type="auto" id="07-06-T2" tdd="true">
  <name>Task 2: Extend LC position truth + additive layer-B predicate (all-green discipline)</name>
  <read_first>parser/post-positioning.js (assignPolesGloballyByLabels ~line 1554, layer B); parser/__tests__/fixtures/luizcarolino-post-positions-truth.json (_meta.scope, current posts 1-20); tools/run-lc-post-position-gate.mjs (the gate to green); .planning/quick/260603-n4k-debug-lc-post-symbol-assignment-collapse/260603-n4k-MILESTONE-SCOPE.md (four-revert Siriu regression proof + shared-symbol collapse mechanism); .planning/quick/260601-k1a-replace-hardcoded-post-number-guards-gra (generic-predicate pattern)</read_first>
  <behavior>
    - LC position gate: posts in the approved scope (at minimum 9/10/11) land within 50 pt of their hand-known anchors -> run-lc-post-position-gate.mjs exits 0.
    - Siriu position gate: every Siriu post stays within 1.0 pt of its locked snapshot -> run-siriu-post-position-gate.mjs exits 0 (byte-identical placement).
    - No other gate regresses (JB/Valmor position, junction tests, txt-accuracy, Siriu regression, residual).
  </behavior>
  <files>parser/post-positioning.js, parser/__tests__/fixtures/luizcarolino-post-positions-truth.json</files>
  <action>
    Per the T1 decision, extend `luizcarolino-post-positions-truth.json` hand-known anchors to
    cover the approved scope (keep tolerancePt: 50; update _meta.scope). Then add an ADDITIVE
    predicate to `assignPolesGloballyByLabels` in parser/post-positioning.js that detects the LC
    shared-symbol collapse (posts 9/10/11 collapsing onto a wrong/shared pole symbol) and routes
    them to the correct anchor — following the "generic predicate, not literal post-number guard"
    pattern from 260601-k1a. DO NOT edit any Siriu-calibrated constant in post-positioning.js or
    distance-associator.js. After EACH incremental change, run the full all-green checkpoint suite
    (see interfaces); no commit may leave any gate red (D-11). NEVER re-seed the Siriu baseline to
    mask a regression. If the predicate cannot green LC without moving a Siriu post >1 pt, stop and
    report — do not force it.
  </action>
  <verify>
    <automated>node tools/run-siriu-post-position-gate.mjs && node tools/run-lc-post-position-gate.mjs && node tools/run-joaoborn-post-position-gate.mjs && node tools/run-valmor-post-position-gate.mjs && node --test parser/__tests__/branch-traversal.test.mjs parser/__tests__/branch-traversal-lc.test.mjs parser/__tests__/branch-traversal-joaoborn.test.mjs parser/__tests__/branch-traversal-valmor.test.mjs && node tools/run-siriu-regression-gate.mjs && node tools/run-residual-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - run-lc-post-position-gate.mjs exits 0 (LC collapse fixed within scope).
    - run-siriu-post-position-gate.mjs exits 0 (Siriu unchanged, 1.0 pt).
    - JB + Valmor position gates, all four junction tests, Siriu regression and residual gates all exit 0.
    - The diff to post-positioning.js adds a predicate only; no existing Siriu-calibrated constant changed.
  </acceptance_criteria>
  <done>LC position gate green with zero regression across all other gates.</done>
</task>

</tasks>

<verification>
- Full all-green checkpoint suite (Siriu pos + LC pos + JB pos + Valmor pos + 4 junction tests + Siriu regression + residual + 4 txt-accuracy) exits 0.
- LC scope decision recorded in the SUMMARY.
</verification>

<success_criteria>
The Luiz Carolino per-post position gate is GREEN via an additive layer-B predicate, with
the Siriu 1.0-pt position gate and every other gate staying green at every checkpoint
(D-11) — clearing the last red gate for SOLVE-05.
</success_criteria>

<output>
After completion, create `.planning/phases/07-solver-prerequisites/07-06-SUMMARY.md`
</output>
