---
phase: 07-solver-prerequisites
plan: 07
type: execute
wave: 4
depends_on: ["07-06"]
files_modified:
  - .planning/phases/07-solver-prerequisites/07-GATE-AUDIT.md
  - package.json
  - .planning/phases/07-solver-prerequisites/07-BASELINE-CASCADE.md
autonomous: true
requirements: [SOLVE-05]

must_haves:
  truths:
    - "07-GATE-AUDIT.md classifies every active gate as regression fence or accuracy assertion, with fence gates annotated for the Phase 8 mid-flight policy (D-16/D-18)"
    - "npm run test:gate runs ALL Phase 7 gates (4 position gates, 4 txt-accuracy gates, 4 junction tests, JB/Valmor PDF accuracy, plus existing Siriu/residual/DXF gates) as a single green command (D-17)"
    - "A baseline cascade run over all four routes completes with the Phase 5 residual gate active and no solver code present, recorded as evidence (SC-4)"
  artifacts:
    - path: ".planning/phases/07-solver-prerequisites/07-GATE-AUDIT.md"
      provides: "fence-vs-accuracy classification + D-18 mid-flight policy"
      min_lines: 40
    - path: "package.json"
      provides: "test:gate umbrella with all Phase 7 gates"
      contains: "run-lc-post-position-gate.mjs"
    - path: ".planning/phases/07-solver-prerequisites/07-BASELINE-CASCADE.md"
      provides: "recorded 4-route baseline cascade output (SC-4)"
      min_lines: 20
  key_links:
    - from: "package.json scripts.test:gate"
      to: "all Phase 7 gate scripts + junction tests"
      via: "&&-chained single command"
      pattern: "run-lc-post-position-gate"
    - from: "07-GATE-AUDIT.md"
      to: "Phase 8 planner mid-flight policy"
      via: "hard red-line vs soft fence annotations (D-18)"
      pattern: "red-line"
---

<objective>
Phase 7 exit (RESEARCH §6 Wave 3): author `07-GATE-AUDIT.md` classifying every active gate
fence-vs-accuracy with the D-18 Phase 8 mid-flight policy, wire ALL Phase 7 gates into the
single `npm run test:gate` umbrella so it is one green bar (D-17), and record a baseline
cascade run over all four routes with the Phase 5 residual gate active and no solver code
present (SC-4). This is the green gate that authorizes Phase 8 to begin.

Purpose: SOLVE-05 / ROADMAP SC-3 + SC-4 — every gate audited; single green command; stable
measurement baseline before any solver code.
Output: 07-GATE-AUDIT.md, extended test:gate, 07-BASELINE-CASCADE.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-solver-prerequisites/07-RESEARCH.md
@.planning/phases/07-solver-prerequisites/07-PATTERNS.md

<interfaces>
<!-- Gate inventory to classify (RESEARCH §2 table). Fence = encodes current output incl. compensated error;
     Accuracy = independent layer assertion. D-18 hard red-lines: per-post position gates, Siriu regression,
     junction GT. Soft (may go RED mid-flight in P8): cumulative accuracy baselines (JB PDF baseline, Valmor
     accuracy baseline, residual absolute sub-score fence; LC txt-accuracy zero-bad-tier IF 21-31 deferred). -->

<!-- test:gate extension (RESEARCH §7): fold all branch-traversal*.test.mjs into the node --test arg list;
     append each standalone gate as &&-chained step. Full target set:
       node --test graph-walker + distance-associator + coordinate-calculator + branch-traversal(+lc,joaoborn,valmor)
       && run-siriu-post-position-gate && run-lc-post-position-gate && run-joaoborn-post-position-gate && run-valmor-post-position-gate
       && run-siriu-txt-accuracy-gate && run-lc-txt-accuracy-gate && run-joaoborn-txt-accuracy-gate && run-valmor-txt-accuracy-gate
       && run-route-joaoborn-pdf-accuracy-gate && run-valmor-accuracy-gate
       && run-siriu-regression-gate && run-residual-gate && run-dxf-ingest-timing-gate -->
</interfaces>
</context>

<tasks>

<task type="auto" id="07-07-T1">
  <name>Task 1: Author 07-GATE-AUDIT.md (fence vs accuracy + D-18 policy)</name>
  <read_first>RESEARCH §2 (gate inventory table), §8 (Pitfall 7 fence rationale); 07-CONTEXT.md D-16/D-18; package.json (current + post-07-06 test:gate)</read_first>
  <files>.planning/phases/07-solver-prerequisites/07-GATE-AUDIT.md</files>
  <action>
    Write 07-GATE-AUDIT.md. For EVERY active gate (the RESEARCH §2 inventory plus the Phase 7
    additions: 4 position gates, 4 txt-accuracy gates, 4 junction tests, JB PDF accuracy, Valmor
    accuracy, Siriu regression, residual, DXF timing), produce a table row with: gate path, kind
    (regression FENCE vs accuracy ASSERTION), what it measures, and the Phase 8 mid-flight policy.
    Apply D-18: HARD RED-LINES (must stay green in P8) = the four per-post position gates, the
    Siriu regression gate, the junction GT assertions. SOFT (may go RED mid-flight if a correct
    solver fix removes a compensated error) = cumulative accuracy baselines (JB PDF baseline,
    Valmor accuracy baseline, residual absolute sub-score fence) and — IF the 07-06 decision
    deferred LC 21-31 — the LC txt-accuracy zero-bad-tier rule. Include the Pitfall-7 rationale
    (why position gates, not cumulative ceilings, are the mid-flight acceptance criterion).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/07-solver-prerequisites/07-GATE-AUDIT.md','utf8');for(const k of ['run-siriu-post-position-gate','run-lc-post-position-gate','run-residual-gate','red-line','FENCE'])if(!t.includes(k))throw new Error('audit missing '+k);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 07-GATE-AUDIT.md lists every active gate with a FENCE/ASSERTION classification.
    - The four position gates + Siriu regression + junction GT are marked hard red-lines for Phase 8.
    - Cumulative baselines + residual sub-score fence are marked soft mid-flight per D-18, with Pitfall-7 rationale.
  </acceptance_criteria>
  <done>Gate audit doc complete and classifies every active gate.</done>
</task>

<task type="auto" id="07-07-T2">
  <name>Task 2: Wire all Phase 7 gates into test:gate (single green command)</name>
  <read_first>package.json (scripts.test:gate after 07-01/07-06 edits); RESEARCH §7 (extension strategy)</read_first>
  <files>package.json</files>
  <action>
    Extend `scripts.test:gate` to the full target set (see interfaces). Fold the three new
    branch-traversal-{lc,joaoborn,valmor}.test.mjs into the leading `node --test ...` arg list
    (Siriu branch-traversal.test.mjs was added in 07-01). Append, &&-chained, each standalone
    gate not yet present: run-lc-post-position-gate.mjs (now green after 07-06),
    run-joaoborn-post-position-gate.mjs, run-valmor-post-position-gate.mjs, the four
    run-*-txt-accuracy-gate.mjs, run-route-joaoborn-pdf-accuracy-gate.mjs, and
    run-valmor-accuracy-gate.mjs. Keep run-siriu-post-position-gate.mjs (07-01),
    run-siriu-regression-gate.mjs, run-residual-gate.mjs, run-dxf-ingest-timing-gate.mjs. The
    result must be ONE umbrella command that exits 0 (D-17). If the single line is unwieldy, you
    may add a `test:gate:fixtures` sub-script invoked from `test:gate`, but `test:gate` stays the
    single entry point.
  </action>
  <verify>
    <automated>npm run test:gate</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:gate` exits 0.
    - The command (directly or via a sub-script it calls) includes all four position gates, all four txt-accuracy gates, all four junction tests, JB PDF + Valmor accuracy gates, and the existing Siriu/residual/DXF gates.
  </acceptance_criteria>
  <done>Single `npm run test:gate` runs every Phase 7 gate green.</done>
</task>

<task type="auto" id="07-07-T3">
  <name>Task 3: Record baseline cascade over all four routes (SC-4)</name>
  <read_first>tools/route-dwg-accuracy-harness.mjs (cascade entry returning dwgConfidence); tools/run-residual-gate.mjs (Phase 5 gate); ROADMAP SC-4</read_first>
  <files>.planning/phases/07-solver-prerequisites/07-BASELINE-CASCADE.md</files>
  <action>
    Run the full DWG pairing cascade on all four routes (Siriu, LC, JB, Valmor) with the Phase 5
    residual gate active and NO solver code present, capturing per-route: post count, tier
    histogram (perfect/good/acceptable/bad), max error, and the residual gate decision +
    dwgConfidence. Record the captured output verbatim in 07-BASELINE-CASCADE.md as the stable
    measurement baseline that Phase 8 will be compared against. Note explicitly that no
    munkres-js / solver code is present (Phase 8 only). If the LC 21-31 deferral (07-06) leaves
    LC bad-tier posts, document them here as the known pre-solver baseline.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/07-solver-prerequisites/07-BASELINE-CASCADE.md','utf8');for(const r of ['Siriu','Luiz','Jo','Valmor'])if(!t.toLowerCase().includes(r.toLowerCase()))throw new Error('baseline missing route '+r);if(!/no solver/i.test(t))throw new Error('must note no solver code');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 07-BASELINE-CASCADE.md records all four routes' cascade output with tier histograms + residual decision.
    - It explicitly states no solver/munkres code is present (pre-Phase-8 baseline).
  </acceptance_criteria>
  <done>Baseline cascade recorded for all four routes with the Phase 5 gate active.</done>
</task>

</tasks>

<verification>
- `npm run test:gate` exits 0 running every Phase 7 gate.
- 07-GATE-AUDIT.md classifies every gate fence-vs-accuracy with D-18 policy.
- 07-BASELINE-CASCADE.md records the 4-route pre-solver baseline.
</verification>

<success_criteria>
Every active gate is audited and classified, all gates run green under a single
`npm run test:gate`, and a stable 4-route baseline cascade is recorded with the Phase 5
gate active and no solver code — the green gate authorizing Phase 8 (SOLVE-05 / SC-3 + SC-4).
</success_criteria>

<output>
After completion, create `.planning/phases/07-solver-prerequisites/07-07-SUMMARY.md`
</output>
