---
phase: 07-solver-prerequisites
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tools/lib/accuracy-tiers.mjs
  - tools/run-siriu-txt-accuracy-gate.mjs
  - tools/run-lc-txt-accuracy-gate.mjs
  - tools/run-joaoborn-txt-accuracy-gate.mjs
  - tools/run-valmor-txt-accuracy-gate.mjs
autonomous: true
requirements: [SOLVE-05]

must_haves:
  truths:
    - "Each route's txt-accuracy gate runs the full DWG cascade, compares per-post lat/lon to the txt-derived ground truth, and exits non-zero if ANY post is bad-tier (>15 m)"
    - "Each gate prints a per-route tier histogram (perfect <=5 / good <=10 / acceptable <=15 / bad >15)"
    - "All four txt-accuracy gates exit 0 against the current cascade output (zero bad-tier posts within each gate's scope) after JB post-35 was excluded in 07-01; the LC gate scopes OUT posts 21-31 (~179 m rigid offset) from its zero-bad-tier exit rule, mirroring the LC position fixture _meta.scope (final scope confirmed in 07-06-T1)"
  artifacts:
    - path: "tools/lib/accuracy-tiers.mjs"
      provides: "shared tier classifier (D-03 four-tier vocabulary)"
      contains: "perfect"
    - path: "tools/run-joaoborn-txt-accuracy-gate.mjs"
      provides: "JB txt-accuracy gate, zero-bad-tier rule"
      min_lines: 40
  key_links:
    - from: "tools/run-*-txt-accuracy-gate.mjs"
      to: "tools/route-dwg-accuracy-harness.mjs runRouteDwgAccuracyHarness"
      via: "import + call with {pdfPath, dwgRegionPath, groundTruthPath, regionId}"
      pattern: "runRouteDwgAccuracyHarness"
    - from: "tools/run-*-txt-accuracy-gate.mjs"
      to: "tools/lib/accuracy-tiers.mjs"
      via: "import tierOf + classify errorsByPost (metres)"
      pattern: "accuracy-tiers"
---

<objective>
Build per-route txt GPS accuracy gates (D-01/D-03) for all four named routes. Each gate
runs the full DWG cascade via the existing accuracy harness, classifies every post's
haversine error vs the txt-derived ground truth into the four tiers (perfect <=5 / good
<=10 / acceptable <=15 / bad >15 m), and FAILS LOUD (exit 1) on any bad-tier post — the
D-03 zero-bad-tier floor (Claude's discretion: floor = zero bad-tier + report histogram,
RESEARCH §8). These gates measure final accuracy against the real surveyed coordinates.

Purpose: SOLVE-05 / ROADMAP SC-1 + SC-4 require zero bad-tier posts across the four routes.
Output: shared tier helper + four txt-accuracy gate scripts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-solver-prerequisites/07-RESEARCH.md
@.planning/phases/07-solver-prerequisites/07-PATTERNS.md

<interfaces>
<!-- Clone scaffolding from tools/run-valmor-accuracy-gate.mjs; harness from route-dwg-accuracy-harness.mjs -->
```js
import { runRouteDwgAccuracyHarness } from "./route-dwg-accuracy-harness.mjs";
const { errorsByPost, posts } = await runRouteDwgAccuracyHarness({
  pdfPath, dwgRegionPath, groundTruthPath, regionId,
}); // errorsByPost: Map<number, metres>
```

<!-- Shared tier classifier (D-03) -->
```js
// tools/lib/accuracy-tiers.mjs
export const tierOf = (m) => (m <= 5 ? "perfect" : m <= 10 ? "good" : m <= 15 ? "acceptable" : "bad");
export function histogram(errorsByPost) { /* count per tier */ }
export function badPosts(errorsByPost) { return [...errorsByPost].filter(([, m]) => m > 15); }
```

<!-- Gate failure tail (mirror run-valmor-accuracy-gate.mjs lines 136-148): print histogram, list bad posts, process.exit(1) -->
```js
const bad = badPosts(errorsByPost);
if (bad.length) { for (const [n, m] of bad) console.error(`  x post ${n}: ${m.toFixed(1)} m (BAD >15 m)`); process.exit(1); }
```
<!-- regionId / dwgRegionPath per route: reuse the *-dwg-region.json fixtures already present (see route-dwg-accuracy-harness.mjs usage). groundTruthPath = parser/__tests__/fixtures/<route>-ground-truth.json (regenerated in 07-01). -->
</interfaces>
</context>

<tasks>

<task type="auto" id="07-05-T1">
  <name>Task 1: Shared tier classifier + Siriu & LC txt-accuracy gates</name>
  <read_first>tools/run-valmor-accuracy-gate.mjs (haversine + failure/exit structure, DWG-region nearest-INSERT mapping lines 46-67); tools/route-dwg-accuracy-harness.mjs (runRouteDwgAccuracyHarness signature + return shape); parser/geo/utm-calibrator.js (haversineMeters)</read_first>
  <files>tools/lib/accuracy-tiers.mjs, tools/run-siriu-txt-accuracy-gate.mjs, tools/run-lc-txt-accuracy-gate.mjs</files>
  <action>
    Create `tools/lib/accuracy-tiers.mjs` exporting `tierOf(m)`, `histogram(errorsByPost)`,
    and `badPosts(errorsByPost)` per the interfaces (D-03 vocabulary). Then create
    run-siriu-txt-accuracy-gate.mjs and run-lc-txt-accuracy-gate.mjs by cloning the
    scaffolding of run-valmor-accuracy-gate.mjs: import runRouteDwgAccuracyHarness, pass the
    route's pdfPath + the existing *-dwg-region.json + groundTruthPath
    (parser/__tests__/fixtures/<route>-ground-truth.json) + regionId, then classify
    errorsByPost with the shared helper. Print the per-route tier histogram to stdout, list any
    bad-tier posts to stderr, and `process.exit(1)` if any post is bad-tier (zero-bad-tier
    floor). Do NOT add an env re-baseline branch — this gate fails hard rather than re-snapshot
    a bad tier (PATTERNS Shared Patterns note). Import `fake-indexeddb/auto`.

    LC GATE SCOPE (resolves the Wave-2 contradiction): the LC posts 21-31 ~179 m rigid offset is
    an absolute-position/solver problem deferred to Phase 8 (it is the Phase 5 residual MUST-FAIL
    fixture), so a strict zero-bad-tier rule over ALL LC posts cannot exit 0 in Wave 2. The LC
    gate MUST therefore exclude posts 21-31 from its zero-bad-tier EXIT rule, mirroring the LC
    position fixture's `_meta.scope`. Define an explicit `EXCLUDED_POSTS = [21..31]` (with a
    comment that it mirrors luizcarolino-post-positions-truth.json `_meta.scope`): still measure
    and PRINT 21-31 in the histogram (clearly labeled "deferred to Phase 8 — soft fence"), but
    only `process.exit(1)` for bad-tier posts OUTSIDE the excluded set. This scope is provisional
    pending the 07-06-T1 decision: if the user chooses `full-scope` there, the executor widens the
    LC gate to include 21-31; if `layerb-only`, the exclusion stays and 07-07 marks the LC
    txt-accuracy zero-bad-tier as a soft fence (D-18).
  </action>
  <verify>
    <automated>node tools/run-siriu-txt-accuracy-gate.mjs && node tools/run-lc-txt-accuracy-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tools/lib/accuracy-tiers.mjs exports tierOf/histogram/badPosts.
    - Siriu and LC txt-accuracy gates run the cascade, print a tier histogram, and exit 0.
    - The LC gate measures and prints posts 21-31 but excludes them from the zero-bad-tier exit rule (EXCLUDED_POSTS mirrors the LC position fixture _meta.scope), so the gate exits 0 in Wave 2.
  </acceptance_criteria>
  <done>Tier helper + Siriu/LC txt-accuracy gates exist and exit 0; LC 21-31 reported but scoped out of the exit rule.</done>
</task>

<task type="auto" id="07-05-T2">
  <name>Task 2: João Born & Valmor txt-accuracy gates</name>
  <read_first>tools/run-siriu-txt-accuracy-gate.mjs (just authored — clone target); tools/run-valmor-accuracy-gate.mjs (DWG-region nearest-INSERT mapping for routes without a PDF distance walk); parser/__tests__/fixtures/joaoborn-ground-truth.json (34 posts after 07-01)</read_first>
  <files>tools/run-joaoborn-txt-accuracy-gate.mjs, tools/run-valmor-txt-accuracy-gate.mjs</files>
  <action>
    Clone the Siriu txt-accuracy gate authored in Task 1 for João Born and Valmor (swap
    pdfPath, dwgRegionPath, groundTruthPath, regionId). For JB the ground truth is the 34-post
    fixture (post 35 excluded in 07-01) so the cascade-vs-truth comparison only covers real
    posts. For Valmor, reuse the DWG-region nearest-INSERT mapping already in
    run-valmor-accuracy-gate.mjs if Valmor has no PDF distance walk. Both gates use the shared
    accuracy-tiers helper and the zero-bad-tier exit rule.
  </action>
  <verify>
    <automated>node tools/run-joaoborn-txt-accuracy-gate.mjs && node tools/run-valmor-txt-accuracy-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - JB and Valmor txt-accuracy gates exist, print tier histograms, and exit 0 (zero bad-tier posts).
    - JB gate compares against 34 posts only (no post-35 km-scale outlier).
  </acceptance_criteria>
  <done>JB + Valmor txt-accuracy gates exist and are green.</done>
</task>

</tasks>

<verification>
- All four `node tools/run-*-txt-accuracy-gate.mjs` exit 0 (zero bad-tier posts within each gate's scope; the LC gate excludes posts 21-31 from the exit rule but still prints them, deferred to Phase 8 per 07-06-T1).
- Each prints a per-route tier histogram.
</verification>

<success_criteria>
Every named route has a txt GPS accuracy gate measuring the full cascade against the real
surveyed coordinates with the four-tier classification and a zero-bad-tier exit rule
(SOLVE-05 / SC-1).
</success_criteria>

<output>
After completion, create `.planning/phases/07-solver-prerequisites/07-05-SUMMARY.md`
</output>
