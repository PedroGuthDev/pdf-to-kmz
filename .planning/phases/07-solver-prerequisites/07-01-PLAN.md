---
phase: 07-solver-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/import-ground-truth-txt.mjs
  - parser/__tests__/fixtures/siriu-ground-truth.json
  - parser/__tests__/fixtures/luizcarolino-ground-truth.json
  - parser/__tests__/fixtures/joaoborn-ground-truth.json
  - parser/__tests__/fixtures/valmor-ground-truth.json
  - package.json
autonomous: true
requirements: [SOLVE-05]

must_haves:
  truths:
    - "Running `node tools/import-ground-truth-txt.mjs` regenerates all four *-ground-truth.json from the repo-root .txt files without treating blank lines as posts"
    - "joaoborn-ground-truth.json contains 34 posts (post 35 Siriu-coordinate outlier excluded and logged), not 35"
    - "siriu-ground-truth.json contains 85 posts (8 blank lines skipped)"
    - "`npm run test:gate` runs the Siriu post-position gate (1.0 pt) and the Siriu junction DFS oracle as a live regression net"
  artifacts:
    - path: "tools/import-ground-truth-txt.mjs"
      provides: "txt -> ground-truth JSON importer with outlier exclusion (D-02)"
      min_lines: 60
    - path: "parser/__tests__/fixtures/joaoborn-ground-truth.json"
      provides: "JB GPS truth, 34 posts, post-35 outlier removed"
      contains: '"number": 34'
    - path: "package.json"
      provides: "test:gate extended with Siriu position gate + Siriu junction test"
      contains: "run-siriu-post-position-gate.mjs"
  key_links:
    - from: "tools/import-ground-truth-txt.mjs"
      to: "parser/__tests__/fixtures/*-ground-truth.json"
      via: "writeFileSync JSON.stringify(out, null, 2) + newline"
      pattern: "writeFileSync"
    - from: "package.json scripts.test:gate"
      to: "tools/run-siriu-post-position-gate.mjs"
      via: "&&-chained node invocation"
      pattern: "run-siriu-post-position-gate"
---

<objective>
Establish the Phase 7 truth foundation: a txt -> JSON ground-truth importer (D-02),
resolve the João Born post-35 data anomaly so the zero-bad-tier rule is satisfiable
(RESEARCH §8), and wire the existing Siriu position gate (1.0 pt) + Siriu junction
DFS oracle into `npm run test:gate` NOW so every later wave inherits the
Pitfall-2 regression net (RESEARCH §6 Wave 0).

Purpose: All downstream txt-accuracy gates read these regenerated JSON fixtures, and
the LC layer-B fix (07-06) needs the Siriu position gate live before it begins.
Output: import script, four regenerated *-ground-truth.json files, extended test:gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-solver-prerequisites/07-CONTEXT.md
@.planning/phases/07-solver-prerequisites/07-RESEARCH.md
@.planning/phases/07-solver-prerequisites/07-PATTERNS.md

<interfaces>
<!-- Ground-truth JSON output shape (verified against valmor-ground-truth.json): a FLAT array. -->
```js
// parser/__tests__/fixtures/<route>-ground-truth.json
[ { "number": 1, "lat": -27.64xxxx, "lon": -48.66xxxx }, ... ]
```

<!-- Repo-root txt format (D-01). Valmor uses lowercase `poste`, zero-padded numbers, NO trailing `;`. -->
// "Poste 12; -27.643210, -48.661234;"   (Siriu/LC/JB)
// "poste 01; -27.6, -48.6"               (Valmor)

<!-- Parse regex (RESEARCH §5 / PATTERNS) -->
```js
const m = line.match(/poste\s+(\d+)\s*;\s*(-?\d+\.\d+)\s*,\s*(-?\d+\.\d+)/i);
if (!m) continue; // skips blank/garbage lines
out.push({ number: +m[1], lat: +m[2], lon: +m[3] });
```

<!-- Existing test:gate line (package.json:13) -->
node --test parser/__tests__/graph-walker.test.mjs parser/__tests__/distance-associator.test.mjs parser/__tests__/coordinate-calculator.test.mjs && node tools/run-siriu-regression-gate.mjs && node tools/run-residual-gate.mjs && node tools/run-dxf-ingest-timing-gate.mjs
</interfaces>
</context>

<tasks>

<task type="auto" id="07-01-T1">
  <name>Task 1: Build txt -> ground-truth JSON importer with outlier exclusion</name>
  <read_first>tools/run-siriu-post-position-gate.mjs (seedTruth write pattern, path bootstrap lines 20-32, 96); parser/__tests__/fixtures/valmor-ground-truth.json (flat-array output shape); coordenadas postes rua joao born.txt (post-35 anomaly); coordenadas postes rua valmor.txt (lowercase poste, no trailing semicolon)</read_first>
  <files>tools/import-ground-truth-txt.mjs</files>
  <action>
    Create `tools/import-ground-truth-txt.mjs` (D-02). Map the four repo-root txt files to
    their fixture outputs:
      - "coordenadas postes siriu.txt" -> parser/__tests__/fixtures/siriu-ground-truth.json
      - "coordenadas postes rua luiz carolino pereira..txt" -> luizcarolino-ground-truth.json
      - "coordenadas postes rua joao born.txt" -> joaoborn-ground-truth.json
      - "coordenadas postes rua valmor.txt" -> valmor-ground-truth.json
    Use the path bootstrap from run-lc-post-position-gate.mjs (fileURLToPath, ROOT, FIXTURES).
    Parse each line with the case-insensitive regex above; `continue` on non-match (skips the
    8 Siriu blank lines and Valmor's no-trailing-`;` is still matched by the regex which does
    not require a trailing semicolon). Output a FLAT array of { number, lat, lon } via
    `writeFileSync(OUT, JSON.stringify(out, null, 2) + "\n", "utf8")`.
    OUTLIER EXCLUSION (RESEARCH §8 — JB post-35 carries Siriu coords ~37 km off): after parsing
    each route, compute the median lat and median lon; drop any post whose haversine distance
    from (medianLat, medianLon) exceeds a configurable `--outlier-km` (default 2.0 km), printing
    `EXCLUDED post N (outlier: Z.Z km from route cluster)` to stderr. This removes JB post 35
    (Siriu coordinates) while leaving street-scale routes intact. Print a per-route summary line
    `<route>: wrote N posts (excluded M)`. Exit 0 on success.
    Reuse `haversineMeters` from parser/geo/utm-calibrator.js for the cluster distance (no new dep).
    After writing the script, RUN it once to (re)generate all four fixtures.
  </action>
  <verify>
    <automated>node tools/import-ground-truth-txt.mjs && node -e "const s=require('./parser/__tests__/fixtures/siriu-ground-truth.json');const jb=require('./parser/__tests__/fixtures/joaoborn-ground-truth.json');if(s.length!==85)throw new Error('siriu='+s.length);if(jb.some(p=>p.number===35))throw new Error('JB post35 not excluded');if(jb.length!==34)throw new Error('jb='+jb.length);console.log('OK',s.length,jb.length)"</automated>
  </verify>
  <acceptance_criteria>
    - siriu-ground-truth.json has exactly 85 entries (blank lines skipped).
    - joaoborn-ground-truth.json has 34 entries; no entry with number===35; the post-35 exclusion was logged to stderr.
    - All four fixtures are flat arrays of { number, lat, lon }, pretty-printed with trailing newline.
    - Re-running the script is idempotent (byte-identical output on a clean tree).
  </acceptance_criteria>
  <done>Importer exists; four ground-truth JSONs regenerated; JB is 34 posts.</done>
</task>

<task type="auto" id="07-01-T2">
  <name>Task 2: Wire Siriu position gate + Siriu junction oracle into test:gate (regression net)</name>
  <read_first>package.json (scripts.test:gate, line 13); tools/run-siriu-post-position-gate.mjs (must already exit 0 at baseline, tol 1.0 pt); parser/__tests__/branch-traversal.test.mjs (Siriu junction DFS oracle)</read_first>
  <files>package.json</files>
  <action>
    Extend the existing `scripts.test:gate` line in package.json (RESEARCH §6/§7). Fold
    `parser/__tests__/branch-traversal.test.mjs` into the leading `node --test ...` argument
    list, and append `&& node tools/run-siriu-post-position-gate.mjs` as a chained step BEFORE
    `run-residual-gate.mjs`. Do NOT add the LC position gate or any not-yet-green gate here
    (those are wired in 07-07 only after they green — D-17). Both additions are green at baseline,
    so test:gate must stay green after this edit. Keep the single-line umbrella convention.
    Rationale (Pitfall 2): the 1.0-pt Siriu position gate is the only instrument that catches a
    shared layer-B regression; it must be live before the LC fix (07-06) starts.
  </action>
  <verify>
    <automated>npm run test:gate</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:gate` exits 0.
    - The test:gate command string contains `branch-traversal.test.mjs` and `run-siriu-post-position-gate.mjs`.
    - The command string does NOT yet contain `run-lc-post-position-gate.mjs` (deferred to 07-07).
  </acceptance_criteria>
  <done>test:gate runs the Siriu position + junction regression net and is green.</done>
</task>

</tasks>

<verification>
- `node tools/import-ground-truth-txt.mjs` regenerates 4 fixtures; Siriu=85, JB=34 (post 35 excluded + logged).
- `npm run test:gate` green with the new Siriu position + junction steps chained in.
</verification>

<success_criteria>
The canonical GPS truth fixtures are regenerated from the txt files with the JB post-35
anomaly resolved, and the Siriu regression net (position gate + junction oracle) is live in
`npm run test:gate` before any other wave runs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-solver-prerequisites/07-01-SUMMARY.md`
</output>
