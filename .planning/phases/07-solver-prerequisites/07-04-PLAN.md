---
phase: 07-solver-prerequisites
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - parser/__tests__/fixtures/luizcarolino-junction-ground-truth.json
  - parser/__tests__/fixtures/joaoborn-junction-ground-truth.json
  - parser/__tests__/fixtures/valmor-junction-ground-truth.json
  - parser/__tests__/branch-traversal-lc.test.mjs
  - parser/__tests__/branch-traversal-joaoborn.test.mjs
  - parser/__tests__/branch-traversal-valmor.test.mjs
autonomous: false
requirements: [SOLVE-05]

must_haves:
  truths:
    - "Junction DFS-oracle tests pass for Luiz Carolino, João Born, and Valmor (Siriu already passes)"
    - "João Born junction fixture is linear with junctions: {} (D-14 locked: no bifurcations)"
    - "Every route's forbiddenArms phantom edges are asserted absent, and no degree>=3 junction arises from inferred-label edges alone (D-15)"
  artifacts:
    - path: "parser/__tests__/fixtures/joaoborn-junction-ground-truth.json"
      provides: "JB linear topology, empty junctions (D-14)"
      contains: '"junctions": {}'
    - path: "parser/__tests__/fixtures/luizcarolino-junction-ground-truth.json"
      provides: "LC junctions + forbiddenArms from 260602-decouple (D-13/D-15)"
      contains: '"forbiddenArms"'
    - path: "parser/__tests__/branch-traversal-lc.test.mjs"
      provides: "LC DFS oracle (clone of branch-traversal.test.mjs)"
      min_lines: 60
  key_links:
    - from: "parser/__tests__/branch-traversal-lc.test.mjs"
      to: "parser/__tests__/fixtures/luizcarolino-junction-ground-truth.json"
      via: "readFileSync fixture path swap"
      pattern: "luizcarolino-junction-ground-truth"
    - from: "parser/__tests__/branch-traversal-*.test.mjs"
      to: "parser/branch-traversal.mjs walkBranchGraph"
      via: "DFS oracle forbidden-arm assertions"
      pattern: "forbiddenArms"
---

<objective>
Clone the Siriu junction ground-truth pattern to the three remaining routes (D-12):
Luiz Carolino, João Born (LOCKED linear, D-14), and Valmor. Each route gets a
`{route}-junction-ground-truth.json` fixture + a `branch-traversal-{route}.test.mjs`
DFS oracle that asserts every post is visited once, declared forbiddenArms phantoms stay
absent, and no degree>=3 junction arises from inferred-label edges alone (D-15). This is
the Pitfall-10 phantom-edge defense for the Phase 8 solver input graph.

Purpose: SOLVE-05 / ROADMAP SC-2 require clean junction ground-truth for all named routes.
Output: 3 junction fixtures + 3 junction test files.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-solver-prerequisites/07-RESEARCH.md
@.planning/phases/07-solver-prerequisites/07-PATTERNS.md

<interfaces>
<!-- Fixture schema: clone parser/__tests__/fixtures/siriu-junction-ground-truth.json -->
```json
{
  "junctions": {
    "9": { "post": 9, "degree": 3, "slots": 1, "forbiddenArms": [11],
      "armMetersChecks": {},
      "arms": [ { "to": 8, "meters": 0.0, "label": "from 8", "inbound": true },
                { "to": 10, "meters": 0.0, "label": "->10" },
                { "to": 21, "meters": 0.0, "label": "->21" } ] }
  },
  "edges": [ { "from": 8, "to": 9, "meters": 0.0, "arm": true, "junction": 9, "inbound": true } ]
}
```
<!-- JB (D-14): "junctions": {} and edges = consecutive chain 1->2->...->34 only. -->
<!-- LC forbiddenArms draft (260602-decouple phantom pairs): 3->1, 11->8, 9->11. Real branch around 9-11 / 21-31 spur. USER must approve. -->
<!-- Valmor: 11-post near-linear DWG route -> likely "junctions": {} (user confirms). -->

<!-- Test oracle: clone parser/__tests__/branch-traversal.test.mjs, change FIXTURE path only (line ~12). Keep buildGraph(fixture) + the five test(...) blocks verbatim (DFS oracle lines 130-191 = forbidden-arm assertion). -->
```js
const FIXTURE = JSON.parse(readFileSync(
  path.join(__dirname, "fixtures", "luizcarolino-junction-ground-truth.json"), "utf8"));
```
</interfaces>
</context>

<tasks>

<task type="auto" id="07-04-T1">
  <name>Task 1: Draft LC + Valmor junction fixtures and lock JB linear fixture</name>
  <read_first>parser/__tests__/fixtures/siriu-junction-ground-truth.json (exact schema); parser/__tests__/branch-traversal.test.mjs (assertions, so the fixtures satisfy them); .planning/quick/260602-decouple-graph-walker-phantom-edges/260602-decouple-SUMMARY.md (LC phantom pairs 3->1, 11->8, 9->11); RESEARCH §4 (per-route topology table)</read_first>
  <files>parser/__tests__/fixtures/joaoborn-junction-ground-truth.json, parser/__tests__/fixtures/luizcarolino-junction-ground-truth.json, parser/__tests__/fixtures/valmor-junction-ground-truth.json</files>
  <action>
    Create three junction fixtures mirroring the Siriu schema:
    - joaoborn-junction-ground-truth.json (D-14 LOCKED): `"junctions": {}` and `edges` = the
      consecutive chain 1->2->...->34 only. Zero junctions, zero forbiddenArms. This is fixed
      by user declaration — no user approval needed.
    - luizcarolino-junction-ground-truth.json (D-13 DRAFT): populate `forbiddenArms` from the
      260602-decouple phantom pairs (3->1, 11->8, 9->11) and encode the real LC branch topology
      around posts 9-11 / the 21-31 spur. Mark every junction with a `forbiddenArms` array (the
      oracle requires it). This is a DRAFT pending user approval (Task 2).
    - valmor-junction-ground-truth.json (D-13 DRAFT): start from `"junctions": {}` (11-post
      near-linear DWG route) with the consecutive `edges` chain; flag as needing user confirm.
    Each fixture must satisfy the branch-traversal oracle's structural expectations (junctions
    expose degree-1 arms, edges cover every post once).
  </action>
  <verify>
    <automated>node -e "const lc=require('./parser/__tests__/fixtures/luizcarolino-junction-ground-truth.json');const jb=require('./parser/__tests__/fixtures/joaoborn-junction-ground-truth.json');const vm=require('./parser/__tests__/fixtures/valmor-junction-ground-truth.json');if(Object.keys(jb.junctions).length!==0)throw new Error('JB must be linear');if(!Array.isArray(lc.edges)||!Array.isArray(jb.edges)||!Array.isArray(vm.edges))throw new Error('edges missing');for(const j of Object.values(lc.junctions)){if(!Array.isArray(j.forbiddenArms))throw new Error('LC junction missing forbiddenArms')}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - joaoborn-junction-ground-truth.json has `junctions: {}` and a 34-post consecutive edge chain (D-14).
    - luizcarolino fixture has every junction declaring a forbiddenArms array including the decouple phantom pairs.
    - valmor fixture exists with edges; junction list drafted per the near-linear assumption.
  </acceptance_criteria>
  <done>Three junction fixtures exist; JB locked linear; LC/Valmor drafted for approval.</done>
</task>

<task type="auto" id="07-04-T2-tests">
  <name>Task 2: Clone DFS-oracle tests per route</name>
  <read_first>parser/__tests__/branch-traversal.test.mjs (clone target — change FIXTURE path only); parser/branch-traversal.mjs (walkBranchGraph consumer)</read_first>
  <files>parser/__tests__/branch-traversal-lc.test.mjs, parser/__tests__/branch-traversal-joaoborn.test.mjs, parser/__tests__/branch-traversal-valmor.test.mjs</files>
  <action>
    Clone `parser/__tests__/branch-traversal.test.mjs` once per route into
    branch-traversal-{lc,joaoborn,valmor}.test.mjs, changing ONLY the FIXTURE readFileSync path
    (to the matching *-junction-ground-truth.json). Keep buildGraph(fixture) and all five
    test(...) blocks verbatim. For JB (empty junctions) the per-junction oracle loops become
    no-ops while the "visits every post once" and "no inferred degree>=3 junction" (D-15 global
    rule) assertions still run against the edges-only chain. Run all three test files.
  </action>
  <verify>
    <automated>node --test parser/__tests__/branch-traversal-lc.test.mjs parser/__tests__/branch-traversal-joaoborn.test.mjs parser/__tests__/branch-traversal-valmor.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All three new branch-traversal-*.test.mjs files exist and import the matching fixture.
    - `node --test` over the three files exits 0 (every post visited once, forbiddenArms absent, no inferred degree>=3 junction).
  </acceptance_criteria>
  <done>Three junction DFS-oracle test files pass.</done>
</task>

<task type="checkpoint:human-verify" id="07-04-T3" gate="blocking">
  <name>Task 3: Human-approve LC + Valmor junction lists</name>
  <action>Present the drafted LC and Valmor junction fixtures to the user and pause for approval/correction before locking (no automation in this step).</action>
  <what-built>Junction ground-truth fixtures for LC, JB (locked linear), Valmor + three passing DFS-oracle tests. LC junctions/forbiddenArms were drafted from the 260602-decouple phantom pairs; Valmor was drafted as near-linear.</what-built>
  <how-to-verify>
    1. Review the LC junction list and forbiddenArms in luizcarolino-junction-ground-truth.json — confirm the authoritative LC junction post numbers + arm topology (D-13 requires user-curated lists). The draft used phantom pairs 3->1, 11->8, 9->11 and the 9-11 / 21-31 branch.
    2. Confirm Valmor's topology: is it truly linear (junctions: {}), or are there real junctions? (D-13).
    3. JB is fixed (no bifurcations, D-14) — no action needed unless you disagree.
    4. Approve to lock, or provide corrected junction posts/arms for LC and/or Valmor (executor re-runs the tests after edits).
  </how-to-verify>
  <resume-signal>Type "approved" to lock LC + Valmor junction fixtures, or provide the authoritative junction post lists/arms to correct.</resume-signal>
</task>

</tasks>

<verification>
- `node --test parser/__tests__/branch-traversal-lc.test.mjs parser/__tests__/branch-traversal-joaoborn.test.mjs parser/__tests__/branch-traversal-valmor.test.mjs` exits 0.
- User has approved the LC and Valmor junction lists; JB confirmed linear.
</verification>

<success_criteria>
All four named routes have junction ground-truth with passing DFS-oracle tests; phantom
edges are asserted absent and no inferred degree>=3 junction can poison the Phase 8 solver
input graph (SOLVE-05 / SC-2).
</success_criteria>

<output>
After completion, create `.planning/phases/07-solver-prerequisites/07-04-SUMMARY.md`
</output>
