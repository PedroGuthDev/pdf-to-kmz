---
phase: 09-diagnostic-failure-confidence-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parser/dwg/residual-gate.js
  - parser/__tests__/residual-gate.test.mjs
  - parser/dwg/coordinate-calculator-dwg.js
  - parser/__tests__/coordinate-calculator-dwg-conf.test.mjs
autonomous: true
requirements: [CONF-01, CONF-04]
must_haves:
  truths:
    - "applyResidualGate exposes per-post shape_residual and anchor_gap meters alongside each tier"
    - "applyResidualGate returns an overall route tier that is high ONLY when gateDecision trusts AND no post is below HIGH"
    - "successResult.dwgConfidence carries overall; result carries an explicit hardBlock boolean"
    - "a no-region or zone-mismatch failure sets hardBlock true; a matched-region degradation sets hardBlock false"
    - "formatDwgWarning renders a Portuguese diverged-at-post reason with at_post + residual_m"
  artifacts:
    - path: "parser/dwg/residual-gate.js"
      provides: "per-post sub-scores + overall tier (additive return fields)"
      contains: "overall"
    - path: "parser/dwg/coordinate-calculator-dwg.js"
      provides: "overall attach, hardBlock flag, diverged-at-post warning kind"
      contains: "hardBlock"
    - path: "parser/__tests__/coordinate-calculator-dwg-conf.test.mjs"
      provides: "hardBlock + overall + diverged-at-post coverage"
  key_links:
    - from: "parser/dwg/coordinate-calculator-dwg.js successResult.dwgConfidence"
      to: "parser/dwg/residual-gate.js applyResidualGate"
      via: "applyResidualGate(shape, anchor) return object"
      pattern: "applyResidualGate"
---

<objective>
Expose the confidence signals Phase 9 needs to surface — all of them already computed
upstream — by making three ADDITIVE changes to the gate and the cascade assembly point:
(1) per-post shape/anchor meters on each `postTiers[]` entry (D-06), (2) an `overall`
route tier on the gate return (D-08), and (3) at the calculator: attach `overall`, set an
explicit `hardBlock` boolean (D-12/D-13), and add a `diverged-at-post` Portuguese warning
kind (D-09). This is the data-source wave; Plans 02 (KMZ) and 03 (UI) consume these fields.

Purpose: every downstream sink (KMZ colors, balloon meters, status banner, block-vs-flag
decision) reads from these fields. Without an explicit `hardBlock`, the UI would have to
string-sniff `dwgStatus` — which is ambiguous (RESEARCH Pitfall 3 / Q2).
Output: enriched `applyResidualGate` return shape + enriched `successResult`/miss results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-diagnostic-failure-confidence-surfacing/09-CONTEXT.md
@.planning/phases/09-diagnostic-failure-confidence-surfacing/09-RESEARCH.md
@.planning/phases/09-diagnostic-failure-confidence-surfacing/09-PATTERNS.md

<interfaces>
<!-- Current applyResidualGate return (parser/dwg/residual-gate.js, line 222). ADD fields only. -->
Returns: { gateDecision: "trust"|"fallback"|"fail", shapeFidelity, anchorGap,
           postTiers: Array<{ postNumber, tier }> }
Internals already computed (lines 186-216):
  incidentRel: Map<postNumber, maxRelError>   (the shape sub-score per post)
  anchorByPost: Map<postNumber, gapM>          (the anchor sub-score per post)
Locked threshold constants (lines 10-15, DO NOT TOUCH):
  SHAPE_TRUST=0.05  SHAPE_FALLBACK=0.15  ANCHOR_TRUST_M=10  ANCHOR_FALLBACK_M=15  ANCHOR_FAIL_M=20

<!-- coordinate-calculator-dwg.js result exits (verified line numbers): -->
no-region miss  (lines 307-330): carries dwgNoRegion + dwgStatus "pdf-fallback"   → HARD BLOCK
zone/error miss (lines 282-305): carries dwgStatus "pdf-fallback", NO dwgNoRegion → HARD BLOCK (unit/envelope)
cascade-fail-after-match (lines 440-449): dwgStatus "pdf-fallback" + dwgRegionId set, NO dwgNoRegion → FLAG
success (lines 478-512): dwgConfidence + solverPath/Demoted/demotionReason/solverScore → FLAG by tier
noRegionError() (lines 19-36): returns { code:"NO_REGION", nearest:{ name, distanceKm } }

<!-- residual-gate.test.mjs line 180 asserts EXACT keys on postTiers entries: -->
assert.deepEqual(Object.keys(t).sort(), ["postNumber", "tier"]);  // THIS WILL BREAK with D-06 — must update
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose per-post sub-scores + overall tier in applyResidualGate (D-06/D-08)</name>
  <files>parser/dwg/residual-gate.js, parser/__tests__/residual-gate.test.mjs</files>
  <read_first>
    - parser/dwg/residual-gate.js (full — especially lines 170-223: the applyResidualGate body, the incidentRel/anchorByPost maps at 186-195, the per-post tier loop 203-219, and the locked threshold constants 10-15)
    - parser/__tests__/residual-gate.test.mjs (lines 160-211 — the exact-keys assertion at line 180 and the UNRESOLVABLE/HIGH-LOW cases that must stay green)
  </read_first>
  <behavior>
    - Each postTiers entry gains numeric fields: shapeResidualM (the post's worst incident residualM in meters, or null) and anchorGapM (the post's anchor gap in meters, or null). Source the meters from shapeFidelity.perEdge[].residualM (worst incident edge) and anchorGap.perPost[].gapM — NOT recomputed.
    - The tier VALUE for every post is byte-identical to today (no threshold or branch change).
    - New top-level field `overall` ∈ {"high","med","low","unresolvable"}: "high" ONLY when gateDecision === "trust" AND no post tier is below HIGH (no MED/LOW/UNRESOLVABLE); otherwise the worst material per-post tier mapped to lowercase (UNRESOLVABLE→"unresolvable", LOW→"low", MED→"med"). A route that trusts but has all-MED posts must NOT be "high".
    - Existing fields gateDecision / shapeFidelity / anchorGap / postTiers[].tier are unchanged and unreordered.
  </behavior>
  <action>
    In applyResidualGate, before building each postTiers entry, capture the per-post meters: add a second incident map (postNumber → worst incident residualM) built from `shape.perEdge` the same way `incidentRel` is built from relError, and reuse `anchorByPost` (already gapM). Change `postTiers.push({ postNumber, tier })` to `postTiers.push({ postNumber, tier, shapeResidualM: <worst incident residualM or null>, anchorGapM: <anchorByPost.get(postNumber) ?? null> })`. Do NOT change `incidentRel` (it stays the relError source for tier decisions) — add a parallel residual-meters map so tier logic is untouched.

    After the postTiers loop, compute `overall` via a pure read over `gateDecision` + `postTiers[].tier`: find the worst tier among {UNRESOLVABLE, LOW, MED, HIGH}; if `gateDecision === "trust"` AND that worst is HIGH (or postTiers empty) return "high"; else map worst → lowercase ("unresolvable"/"low"/"med"; HIGH-but-not-trust → "med"). Add `overall` to the returned object alongside the existing keys. Keep threshold constants at lines 10-15 untouched.

    In residual-gate.test.mjs, update the line-180 exact-keys assertion: change the expected key set to include the new fields, e.g. assert the entry has at minimum postNumber + tier and that shapeResidualM/anchorGapM keys are present (numbers or null). Add a new test asserting overall === "high" when gateDecision trusts and all posts HIGH, and overall is NOT "high" (degrades to worst material tier) when one post is MED/LOW while gateDecision still trusts.
  </action>
  <verify>
    <automated>node --test parser/__tests__/residual-gate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test parser/__tests__/residual-gate.test.mjs` exits 0
    - applyResidualGate return object has key `overall` and each postTiers entry has keys shapeResidualM and anchorGapM (number or null)
    - A gate input with gateDecision "trust" and all-HIGH posts yields overall === "high"; the same gateDecision "trust" with one MED post yields overall === "med" (NOT "high")
    - Threshold constants SHAPE_TRUST/SHAPE_FALLBACK/ANCHOR_TRUST_M/ANCHOR_FALLBACK_M/ANCHOR_FAIL_M are unchanged (grep -v '^//' parser/dwg/residual-gate.js shows the same numeric values)
    - `node tools/run-residual-gate.mjs` exits 0 (CI baseline snapshots gateDecision only — unchanged)
  </acceptance_criteria>
  <done>Per-post meters + overall tier are exposed additively; gate baseline and unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach overall + hardBlock flag + diverged-at-post warning at the calculator (D-08/D-09/D-12/D-13)</name>
  <files>parser/dwg/coordinate-calculator-dwg.js, parser/__tests__/coordinate-calculator-dwg-conf.test.mjs</files>
  <read_first>
    - parser/dwg/coordinate-calculator-dwg.js (lines 19-36 noRegionError; 80-127 formatDwgWarning switch; 282-330 the two miss exits; 440-449 cascade-fail-after-match; 478-513 successResult assembly where dwgConfidence attaches at line 497)
    - parser/dwg/residual-gate.js (the overall field added in Task 1 — successResult.dwgConfidence.overall now exists)
  </read_first>
  <behavior>
    - Every result object (success, cascade-fail-after-match, no-region miss, zone/error miss) carries a boolean `hardBlock`.
    - hardBlock === true on: the no-region miss (dwgNoRegion present) AND the zone/error miss (dwgStatus "pdf-fallback" with NO dwgNoRegion at the early catch/zone exit). hardBlock === false on: cascade-fail-after-match (region matched, dwgRegionId set) AND success.
    - formatDwgWarning renders a new kind "diverged-at-post" → Portuguese string of the form `DXF: rota divergiu no poste <at_post> (resíduo <residual_m, 1 decimal> m).` with no percentage sign.
    - On success, when the worst anchor gap post crosses ANCHOR_FALLBACK_M, a structured warning { kind:"diverged-at-post", at_post, residual_m } is pushed (read-only from the already-computed anchor.perPost — no new math).
  </behavior>
  <action>
    Add `hardBlock: true` to the two BLOCK exits: the no-region miss result (the object at lines 322-330 carrying dwgNoRegion) and the catch/zone miss result (lines 297-304, the pdf-fallback object with no dwgNoRegion). Add `hardBlock: false` to the cascade-fail-after-match result (lines 441-449) and to successResult (lines 478-512). Do not branch on `dwgStatus` string to decide the flag — set it literally at each exit per the block-vs-flag table in the interfaces block.

    In formatDwgWarning's switch (after an existing meter-formatting arm such as `dwg-pair-fail`), add `case "diverged-at-post":` returning the Portuguese string named in <behavior>, using `Number(o.residual_m).toFixed(1)` for the meter value and `o.at_post` for the post — mirror the existing `DWG:`/`DXF:` prefix idiom. Never emit a `%`.

    In the success branch, after `successResult.dwgConfidence = applyResidualGate(shape, anchor)` (line 497), read the worst-gap post from `anchor.perPost` (the entry with max gapM); if that gapM >= ANCHOR_FALLBACK_M (import or re-declare the constant value 15 locally as a named const — do NOT mutate residual-gate's constant), push `{ kind: "diverged-at-post", at_post: <that postNumber>, residual_m: <that gapM> }` into successResult.warnings. successResult.dwgConfidence already carries overall from Task 1 — no extra attach needed.

    Create parser/__tests__/coordinate-calculator-dwg-conf.test.mjs (node:test + node:assert/strict) covering: (a) formatDwgWarning({kind:"diverged-at-post", at_post:7, residual_m:179.04}) contains "poste 7", "179.0", and no "%"; (b) a unit assertion that the block-vs-flag policy is literal — construct minimal stand-ins is not feasible for the full async path, so instead assert formatDwgWarning output strings and (if practical) import and assert hardBlock literal on the no-region path via a stubbed regionLibrary returning no region.
  </action>
  <verify>
    <automated>node --test parser/__tests__/coordinate-calculator-dwg-conf.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test parser/__tests__/coordinate-calculator-dwg-conf.test.mjs` exits 0
    - formatDwgWarning({ kind:"diverged-at-post", at_post:7, residual_m:179.04 }) returns a string containing "poste 7" and "179.0" and NOT containing "%"
    - grep -n "hardBlock" parser/dwg/coordinate-calculator-dwg.js shows hardBlock set at all four result exits (two true, two false)
    - The no-region miss result object includes hardBlock:true alongside its existing dwgNoRegion field; the cascade-fail-after-match result includes hardBlock:false alongside dwgRegionId
    - `npm run test:gate` exits 0 (zero accuracy regression on Siriu/LC/JB/Valmor; run-residual-gate green)
  </acceptance_criteria>
  <done>overall is on dwgConfidence; hardBlock is explicit at every exit; diverged-at-post reason renders in Portuguese; all gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user PDF/DXF data → gate/calculator output fields | post numbers, region names, gap meters flow into structured fields later serialized to KML/DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | diverged-at-post warning string | mitigate | values are numeric (at_post, residual_m via Number().toFixed) — no raw user text interpolated; downstream KML/DOM escaping owned by Plans 02/03 |
| T-09-02 | Information disclosure | overall/hardBlock fields | accept | client-side only; no PII; fields describe the user's own drawing |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | none — this plan installs no packages (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `node --test parser/__tests__/residual-gate.test.mjs` exits 0
- `node --test parser/__tests__/coordinate-calculator-dwg-conf.test.mjs` exits 0
- `npm run test:gate` exits 0 (all four route gates + residual gate baseline green — additive-only proof)
- No `%` character appears in any new warning string (grep the new case arm)
</verification>

<success_criteria>
- applyResidualGate exposes per-post shapeResidualM/anchorGapM and a top-level `overall` tier (D-06/D-08), additively, with zero tier-value or threshold change
- successResult.dwgConfidence carries `overall`; every result exit carries an explicit boolean `hardBlock` matching the D-12/D-13 block-vs-flag table
- A `diverged-at-post` Portuguese reason is renderable via formatDwgWarning (D-09) with meters, no percentage
- `npm run test:gate` stays green (additive discipline; Pitfalls 1, 2, 4)
</success_criteria>

<output>
Create `.planning/phases/09-diagnostic-failure-confidence-surfacing/09-01-SUMMARY.md` when done
</output>
