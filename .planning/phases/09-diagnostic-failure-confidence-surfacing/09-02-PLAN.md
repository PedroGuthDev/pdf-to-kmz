---
phase: 09-diagnostic-failure-confidence-surfacing
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - parser/dwg/tier-styles.js
  - parser/kmz-defaults.js
  - parser/kml-builder.js
  - parser/__tests__/kml-builder.test.mjs
autonomous: true
requirements: [CONF-02, CONF-03, CONF-04]
must_haves:
  truths:
    - "buildKml emits four tier-keyed Style blocks (tierHigh/tierMed/tierLow/tierUnresolvable) with traffic-light colors"
    - "each post placemark references the styleUrl for its own tier, not the shared #postPoint"
    - "each post placemark carries ExtendedData with tier + shape_residual_m + anchor_gap_m + source"
    - "each post balloon description begins with a Portuguese tier line (Confiança: ALTA/MÉDIA/BAIXA/NÃO RESOLVIDO)"
    - "an UNRESOLVABLE post WITH a coordinate renders a red marker; a post with NO coordinate is reported in stats, never silently dropped"
    - "route line styling stays a single uniform routeLine (no per-segment recolor)"
    - "no percent sign appears anywhere in the emitted KML"
  artifacts:
    - path: "parser/dwg/tier-styles.js"
      provides: "tier→hex map, tierStyleId(), tierStyleBlock(), Portuguese tier label map"
      exports: ["TIER_HEX", "tierStyleId", "tierStyleBlock", "TIER_LABEL_PT"]
    - path: "parser/kml-builder.js"
      provides: "tier styles, per-post tier styleUrl, ExtendedData, balloon tier line, unresolvable flag stat"
      contains: "ExtendedData"
  key_links:
    - from: "parser/kml-builder.js buildKml"
      to: "options.postTiers"
      via: "tier lookup by post number"
      pattern: "postTiers"
    - from: "parser/kml-builder.js"
      to: "parser/kml-color.js hexToKmlColor"
      via: "tier hex → aabbggrr"
      pattern: "hexToKmlColor"
---

<objective>
Encode per-post confidence in the KMZ (CONF-02): replace the single shared `#postPoint`
style with four traffic-light tier styles (D-01), reference each post's own tier styleUrl,
add `<ExtendedData>` diagnostics (D-04, meters allowed), prepend a Portuguese tier line to
the balloon `<description>` (D-05), and replace the silent `omittedNoGps` drop with explicit
UNRESOLVABLE flagging (D-11/CONF-03). Route lines stay one uniform color (D-02). No
percentage anywhere (CONF-04).

Purpose: a user opening the KMZ in Google Earth sees risk by marker color and can inspect
WHY a post is MED/LOW in ExtendedData — without any numeric quality seal.
Output: tier-aware `buildKml` consuming the `postTiers` produced by Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-diagnostic-failure-confidence-surfacing/09-CONTEXT.md
@.planning/phases/09-diagnostic-failure-confidence-surfacing/09-RESEARCH.md
@.planning/phases/09-diagnostic-failure-confidence-surfacing/09-PATTERNS.md
@DESIGN.md

<interfaces>
<!-- postTiers entry shape produced by Plan 01 (residual-gate.js). buildKml receives these via options.postTiers. -->
postTiers: Array<{ postNumber: number, tier: "HIGH"|"MED"|"LOW"|"UNRESOLVABLE",
                   shapeResidualM: number|null, anchorGapM: number|null }>
<!-- post entries may also carry source ("dwg" | undefined) and demotionReason via the calculator. -->

<!-- kml-color.js (DO reuse — never hand-build aabbggrr): -->
hexToKmlColor(hex: "#RRGGBB", alpha=255) -> "aabbggrr"

<!-- kml-builder.js current emit sites: -->
escapeXml(str)            // lines 7-14 — reuse for EVERY new <Data>/<description> value
single #postPoint Style   // line 317 — REPLACE with 4 tier styles; #postPoint stays as fallback
placemark loop            // lines 321-343 — desc at 330, styleUrl at 335, ExtendedData goes before </Placemark>
omittedNoGps silent drop  // lines 322-328 — repurpose per D-11
stats object              // lines 251-257 — add an unresolved-list / no-coord counter
routeLine + polyline loop // lines 318, 352-388 — DO NOT TOUCH (D-02 uniform line)
normalizeConnections / buildRoutePolylines / preferMainRouteEdge — BYTE-STABLE (Pitfall 4)

<!-- kmz-defaults.js PRESET_COLORS already has green/yellow/amber/red (lines 15-25). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure tier-styles helper module (D-01/D-03/D-05)</name>
  <files>parser/dwg/tier-styles.js, parser/kmz-defaults.js</files>
  <read_first>
    - parser/kml-color.js (full — the pure-module shape to mirror: JSDoc header, named exports, throw on invalid input)
    - parser/kmz-defaults.js (lines 15-25 PRESET_COLORS — reuse green/yellow/amber/red rather than inventing hexes)
    - parser/dwg/residual-gate.js (the tier vocabulary HIGH/MED/LOW/UNRESOLVABLE and the overall lowercase vocabulary, for label parity)
  </read_first>
  <behavior>
    - TIER_HEX maps HIGH→green, MED→yellow, LOW→orange/amber, UNRESOLVABLE→red as #RRGGBB strings (traffic-light, D-01). UNRESOLVABLE is red, not grey.
    - tierStyleId("HIGH") === "tierHigh", "MED"→"tierMed", "LOW"→"tierLow", "UNRESOLVABLE"→"tierUnresolvable".
    - tierStyleBlock(tier, iconHref) returns a single-line `<Style id="tier…">` string whose IconStyle <color> is hexToKmlColor(TIER_HEX[tier]) and whose <Icon><href> is the passed iconHref (escaped by the caller or here consistently); includes a LabelStyle matching the existing #postPoint label scale convention.
    - TIER_LABEL_PT maps HIGH→"ALTA", MED→"MÉDIA", LOW→"BAIXA", UNRESOLVABLE→"NÃO RESOLVIDO".
    - Throws on an unknown tier (mirror hexToKmlColor's throw discipline).
  </behavior>
  <action>
    Create parser/dwg/tier-styles.js as a pure no-I/O module importing hexToKmlColor from ../kml-color.js. Export const TIER_HEX (the four #RRGGBB values; reuse the PRESET_COLORS green/yellow/amber/red hex literals so the palette stays consistent), const TIER_LABEL_PT (the four Portuguese labels above), function tierStyleId(tier) returning the camelCase id, and function tierStyleBlock(tier, iconHref) returning the `<Style id="${tierStyleId(tier)}">…</Style>` string built exactly like the existing #postPoint template in kml-builder.js (IconStyle color+scale+Icon href, LabelStyle color+scale) but with the tier color. Throw an Error on an unknown tier key.

    In kmz-defaults.js, optionally export a TIER_COLORS convenience referencing the same preset hexes for discoverability (D-03 coexistence note); do not change mergeOptions or resolveStyleColors signatures or the user icon/line/label options. Tier color winning over user icon color is the DEFAULT and is enforced in Task 2 by emitting tier styleUrls — no toggle (toggle deferred per CONTEXT).
  </action>
  <verify>
    <automated>node -e "import('./parser/dwg/tier-styles.js').then(m=>{const s=m.tierStyleBlock('UNRESOLVABLE','x');if(!/tierUnresolvable/.test(s)||/%/.test(s))process.exit(1);if(m.TIER_LABEL_PT.HIGH!=='ALTA')process.exit(1);console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - tier-styles.js exports TIER_HEX, TIER_LABEL_PT, tierStyleId, tierStyleBlock
    - tierStyleId("UNRESOLVABLE") === "tierUnresolvable" and tierStyleId("HIGH") === "tierHigh"
    - tierStyleBlock("HIGH","href") contains `<Style id="tierHigh">` and a `<color>` value equal to hexToKmlColor(TIER_HEX.HIGH)
    - TIER_LABEL_PT.UNRESOLVABLE === "NÃO RESOLVIDO"; no key value contains "%"
    - tierStyleBlock("BOGUS","x") throws
  </acceptance_criteria>
  <done>Pure tier-styles helper exists with traffic-light colors, camelCase style ids, and Portuguese labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tier styles, per-post styleUrl, ExtendedData, balloon line, unresolvable flag in buildKml (D-01/02/04/05/11)</name>
  <files>parser/kml-builder.js, parser/__tests__/kml-builder.test.mjs</files>
  <read_first>
    - parser/kml-builder.js (full — the parts array header 312-319, the placemark loop 321-343 including the omittedNoGps drop, the stats object 251-257, and the polyline loop 352-388 which must stay untouched)
    - parser/dwg/tier-styles.js (the helpers created in Task 1)
    - parser/__tests__/kml-builder.test.mjs (lines 200-229 — the omittedNoGps test that must be updated for D-11 semantics, and the escaping/empty-doc tests that must stay green)
  </read_first>
  <behavior>
    - When options.postTiers is provided, buildKml emits the four tier Style blocks (via tierStyleBlock) into the document header in addition to (or replacing) #postPoint, keeps #postPoint as the fallback style when a post has no tier entry.
    - Each post placemark with a tier references `<styleUrl>#tier{Cap}</styleUrl>` (tierStyleId) instead of #postPoint; posts with no tier entry keep #postPoint.
    - Each post placemark gets an `<ExtendedData>` block (sibling of <Point>, before </Placemark>) with <Data name="tier">, <Data name="shape_residual_m"> (omit or empty when null), <Data name="anchor_gap_m"> (omit or empty when null), <Data name="source"> (post.source ?? "pdf"); every value passes through escapeXml.
    - The balloon <description> is prefixed with `Confiança: {TIER_LABEL_PT} — ` before the existing `Lat: …, Lon: …`; when no tier, the description is unchanged.
    - D-11: a post with NO coordinate is no longer silently dropped — buildKml records it (stats.unresolvedNoCoord array of post numbers) AND, when an UNRESOLVABLE-tier post HAS a coordinate, it renders normally with the red tierUnresolvable style. Keep a back-compat numeric stats.omittedNoGps equal to the count of no-coordinate posts so existing callers do not crash.
    - Route lines unchanged: single routeLine style, polyline loop byte-identical. No "%" anywhere in output.
  </behavior>
  <action>
    Import { tierStyleId, tierStyleBlock, TIER_LABEL_PT } from "./dwg/tier-styles.js". Build a `tierByPost` Map from options.postTiers (postNumber → entry). In the parts header, after #postPoint and routeLine (or replacing #postPoint), push the four tierStyleBlock(tier, merged.iconHref) strings when postTiers is present.

    In the post loop: compute `const tp = tierByPost.get(post.number)`. For the styleUrl, use `#${tierStyleId(tp.tier)}` when tp exists, else `#postPoint`. Prepend the balloon line: when tp exists, `desc = "Confiança: " + TIER_LABEL_PT[tp.tier] + " — " + existingLatLon`. Before `</Placemark>`, when tp exists, push an `<ExtendedData>` block built from escapeXml-wrapped values: tier=tp.tier, shape_residual_m = tp.shapeResidualM (toFixed(1) when a number, omit the <Data> when null), anchor_gap_m = tp.anchorGapM (same rule), source = escapeXml(String(post.source ?? "pdf")). When tp.demotionReason is present on the entry, add a <Data name="demotionReason"> too. NEVER emit a `%`.

    For D-11: extend the stats object with `unresolvedNoCoord: []`. In the no-GPS branch (currently lines 322-328), push post.number into stats.unresolvedNoCoord and keep stats.omittedNoGps as a count (omittedNoGps = unresolvedNoCoord.length) for back-compat; the warning text may stay but reword to "sem coordenada (não resolvido)". Do NOT touch normalizeConnections / buildRoutePolylines / preferMainRouteEdge / the routeLine style / the polyline loop.

    In kml-builder.test.mjs: update the "omits posts without GPS and counts them" test (lines 201-210) to assert the new D-11 semantics — stats.omittedNoGps still counts no-coord posts AND stats.unresolvedNoCoord contains that post's number. Add new tests: (a) given postTiers with a HIGH and a MED post, the KML contains `<Style id="tierHigh">` and the HIGH post's placemark references `#tierHigh` and contains `Confiança: ALTA`; (b) an ExtendedData block contains `<Data name="tier">` and `shape_residual_m`; (c) `assert.doesNotMatch(kml, /%/)`; (d) an UNRESOLVABLE post WITH lat/lon references `#tierUnresolvable` and is NOT dropped.
  </action>
  <verify>
    <automated>node --test parser/__tests__/kml-builder.test.mjs parser/__tests__/kml-builder-siriu-dwg.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test parser/__tests__/kml-builder.test.mjs parser/__tests__/kml-builder-siriu-dwg.test.mjs` exits 0
    - With postTiers supplied, the emitted KML contains all four of `<Style id="tierHigh">`, `tierMed`, `tierLow`, `tierUnresolvable`
    - A HIGH post's placemark contains `<styleUrl>#tierHigh</styleUrl>` and `Confiança: ALTA` and an `<ExtendedData>` with `<Data name="tier">`
    - `assert.doesNotMatch(kml, /%/)` passes for a tiered build
    - An UNRESOLVABLE post with lat/lon present is emitted as a placemark referencing `#tierUnresolvable` (not dropped); a post with null lat/lon appears in stats.unresolvedNoCoord
    - The siriu-dwg golden line test stays green (route line styling untouched, D-02)
    - Calling buildKml WITHOUT postTiers produces the same #postPoint output as before (back-compat: no tier styles, no ExtendedData)
  </acceptance_criteria>
  <done>KMZ encodes per-post tier color + ExtendedData + Portuguese balloon line; unresolvable posts flagged not dropped; lines uniform; no percentage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| post/region/tier data → KML XML | post.source, region names, meters serialized into <Data>/<description> |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | new ExtendedData/balloon values | mitigate | every new <Data>/<description> value passes through existing escapeXml() (V5 output-encoding) |
| T-09-04 | (anti-feature) | tiered KML output | mitigate | assert.doesNotMatch(kml, /%/) test gate enforces CONF-04 no-numeric-% |
| T-09-SC | Tampering | npm installs | mitigate | none — zero new dependencies (RESEARCH) |
</threat_model>

<verification>
- `node --test parser/__tests__/kml-builder.test.mjs parser/__tests__/kml-builder-siriu-dwg.test.mjs` exits 0
- Tier helper smoke test (Task 1 verify) exits 0
- `npm run test:gate` exits 0 (route accuracy unaffected — surfacing is read-only over coords/connections)
- No `%` in emitted KML for a tiered build
</verification>

<success_criteria>
- Four traffic-light tier Style blocks emitted; each post references its own tier styleUrl (D-01)
- Per-post ExtendedData carries tier + meters + source (D-04); balloon gains a Portuguese tier line (D-05)
- UNRESOLVABLE-with-coord renders red; no-coord post is recorded, never silently dropped (D-11/CONF-03)
- Route lines stay uniform (D-02); zero `%` anywhere (CONF-04); accuracy gates green (Pitfall 4)
</success_criteria>

<output>
Create `.planning/phases/09-diagnostic-failure-confidence-surfacing/09-02-SUMMARY.md` when done
</output>
