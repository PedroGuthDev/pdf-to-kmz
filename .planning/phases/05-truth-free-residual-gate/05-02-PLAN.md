---
phase: 05-truth-free-residual-gate
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - parser/dwg/coordinate-calculator-dwg.js
  - tools/residual-gate-harness.mjs
  - tools/run-residual-gate.mjs
  - parser/__tests__/fixtures/luizcarolino-residual-mustfail.json
  - package.json
autonomous: false
requirements: [ACC-02, ACC-03, ACC-04]
must_haves:
  truths:
    - "result.dwgConfidence is attached on the cascade success path with NO change to posts/connections/dwgStatus bytes (D-01)"
    - "The CI gate runs over validated routes via `npm run test:gate` and asserts Siriu → trust"
    - "The LC posts-21-31 PDF-path fixture (~6.1 m shape / ~178.7 m anchor) produces gateDecision 'fail' on the ANCHOR sub-score"
    - "ANCHOR_* thresholds are calibrated from real Siriu/Valmor/João Born anchor gaps then locked as named constants"
    - "Siriu/Valmor/João Born do not false-fail the gate (sanity-check during calibration, D-05)"
  artifacts:
    - path: "parser/dwg/coordinate-calculator-dwg.js"
      provides: "additive residual-gate invocation; attaches successResult.dwgConfidence"
      contains: "successResult.dwgConfidence"
    - path: "tools/run-residual-gate.mjs"
      provides: "CI gate: Siriu trust + LC must-fail assertions, baseline/RESIDUAL_UPDATE_BASELINE pattern"
    - path: "tools/residual-gate-harness.mjs"
      provides: "per-route runner exposing dwgConfidence (mirrors route-dwg-accuracy-harness.mjs)"
    - path: "parser/__tests__/fixtures/luizcarolino-residual-mustfail.json"
      provides: "real captured LC PDF-path 21-31 coords (D-06), expectedDecision fail / anchor"
  key_links:
    - from: "parser/dwg/coordinate-calculator-dwg.js"
      to: "parser/dwg/residual-gate.js"
      via: "import computeResiduals, computeAnchorGap, applyResidualGate"
      pattern: "import.*residual-gate"
    - from: "package.json"
      to: "tools/run-residual-gate.mjs"
      via: "test:gate script chain"
      pattern: "run-residual-gate"
---

<objective>
Wire the Plan-01 residual gate into the live pipeline (additive, pure-judge per D-01), build the LC
must-fail fixture (D-06), calibrate + lock the ANCHOR_* thresholds against real route output (D-05),
and add the CI gate to `npm run test:gate` (ACC-04). After this plan: Siriu output is rated "trust",
the LC 21-31 rigid-offset is a locked "fail" on the anchor sub-score, and every route receives per-post
tiers without crashing — all with zero change to emitted coordinate bytes.

Purpose: This makes the gate real and regression-locked. Closes ACC-02 (live anchor gap), ACC-03
(LC must-fail), ACC-04 (CI gate). The threshold calibration is the single most consequential step —
it must use measured known-good anchor gaps, not guesses.
Output: modified coordinate-calculator-dwg.js, residual-gate-harness.mjs, run-residual-gate.mjs,
luizcarolino-residual-mustfail.json, updated package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-truth-free-residual-gate/05-CONTEXT.md
@.planning/phases/05-truth-free-residual-gate/05-RESEARCH.md
@.planning/phases/05-truth-free-residual-gate/05-PATTERNS.md
@.planning/phases/05-truth-free-residual-gate/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01 contract (already built): parser/dwg/residual-gate.js -->
  export function computeResiduals(coords, distances) -> { medianRelError, p95RelError, edgeCount, perEdge }
  export function computeAnchorGap(coords, gpsByPostNumber) -> { meanGapM, p95GapM, perPost }
  export function applyResidualGate(shape, anchor, thresholds?) -> { gateDecision, shapeFidelity, anchorGap, postTiers }

<!-- Live integration site — coordinate-calculator-dwg.js (VERIFIED line numbers) -->
  - line 314-319: `gpsByPostNumber` Map<number,{lat,lon}> built from pdfResult.posts (user-anchored PDF coords) — the D-03 source, present BEFORE the dwg merge.
  - line 352-365: `const cascade = runDwgPairingCascade({...})`; cascade shape = { ok, coords, dwgPath }; cascade.coords entries = { postNumber, lat, lon, dwg_block }.
  - line 367-376: cascade-fail branch returns early (pdf-fallback). Gate sits on SUCCESS path only.
  - line 405-412: `successResult` assembled. line 419: userWarnings set. line 420: return.
  - Insertion: after line 418 (successResult fully assembled), before `successResult.userWarnings = ...`.
  - `distances` in scope is the post-augmentation snapshot consistent with cascade.coords (Pitfall 4).

<!-- CI harness contract — route-dwg-accuracy-harness.mjs (VERIFIED) -->
  export async function runRouteDwgAccuracyHarness({ pdfPath, dwgRegionPath, groundTruthPath, regionId })
    -> { dwgStatus, posts, errorsByPost, idxByPost, walkOk, walkCoords, gpsFirstDivergentPost }
  NOTE: it does NOT currently return dwgConfidence. Because Plan 02 attaches dwgConfidence to the
  pipeline result, expose it: in route-dwg-accuracy-harness.mjs add `dwgConfidence: result.dwgConfidence ?? null`
  to the returned object (additive, 1 line), and reuse that field in the new harness/gate.

<!-- Gate skeleton analog — tools/run-route-dwg-accuracy-gate.mjs (VERIFIED, full read) -->
  slackM(observed) = Math.ceil((observed + 0.5) * 10) / 10
  baseline pattern: const updateBaseline = process.env.<NAME>_UPDATE_BASELINE === "1" || !existsSync(BASELINE_PATH)
  fail pattern: failures[] -> console.error("  x "+f) per item -> process.exit(1); success -> console.log("PASS — ...")
  tail: main().catch(e => { console.error(e); process.exit(1); })
  route fixture paths (VERIFIED present): siriu-*, valmor-*, joaoborn-*, luizcarolino-* dwg-region.json + ground-truth.json

<!-- package.json scripts.test:gate (VERIFIED current value) -->
  "test:gate": "node --test parser/__tests__/graph-walker.test.mjs parser/__tests__/distance-associator.test.mjs parser/__tests__/coordinate-calculator.test.mjs && node tools/run-siriu-regression-gate.mjs"
  Append: ` && node tools/run-residual-gate.mjs`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additively wire residual gate into the cascade success path (D-01, pure judge)</name>
  <read_first>
    - parser/dwg/coordinate-calculator-dwg.js (lines 1-13 imports, 295-421 success/fail paths) — exact insertion site, in-scope variables (gpsByPostNumber, cascade.coords, distances, successResult)
    - parser/dwg/residual-gate.js — the Plan-01 module being imported (export names)
    - .planning/phases/05-truth-free-residual-gate/05-CONTEXT.md (D-01) — pure-judge: no coord/byte change
    - .planning/phases/05-truth-free-residual-gate/05-PATTERNS.md (§coordinate-calculator-dwg.js) — additive insertion pattern
  </read_first>
  <action>
    Add `import { computeResiduals, computeAnchorGap, applyResidualGate } from "./residual-gate.js";` alongside the other
    `./` dwg imports near lines 6-13. On the cascade SUCCESS path only (after `successResult` is fully assembled at
    line ~418, before `successResult.userWarnings = buildCalcUserWarnings(successResult)` at line 419), compute:
    `const shape = computeResiduals(cascade.coords, distances);`, `const anchor = computeAnchorGap(cascade.coords, gpsByPostNumber);`,
    and `successResult.dwgConfidence = applyResidualGate(shape, anchor);`. Attach ONLY — do not touch successResult.posts,
    .connections, .dwgStatus, .dwgRegionId, or any coordinate. The cascade-fail branch (lines 367-376) returns earlier and
    must remain untouched (no gate call there — pdf-fallback path is out of scope this phase). Field name `dwgConfidence`
    is the chosen sibling to the existing `dwgStatus`/`dwgRegionId` fields (Claude's-Discretion D-01).
  </action>
  <verify>
    <automated>node --test parser/__tests__/coordinate-calculator.test.mjs && node tools/run-siriu-regression-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - coordinate-calculator-dwg.js contains `import { computeResiduals, computeAnchorGap, applyResidualGate } from "./residual-gate.js"`
    - coordinate-calculator-dwg.js contains `successResult.dwgConfidence = applyResidualGate(`
    - The assignment appears AFTER successResult is assembled and the cascade-fail early-return branch contains NO dwgConfidence reference (grep: dwgConfidence appears only on the success path)
    - `node tools/run-siriu-regression-gate.mjs` still passes (proves no coordinate/byte change — D-01)
    - `node --test parser/__tests__/coordinate-calculator.test.mjs` exits 0
  </acceptance_criteria>
  <done>dwgConfidence attached on success path; Siriu regression gate still green (output bytes unchanged).</done>
</task>

<task type="auto">
  <name>Task 2: Build LC must-fail fixture + per-route harness + CI gate; calibrate and LOCK thresholds</name>
  <read_first>
    - tools/run-route-dwg-accuracy-gate.mjs (full) — gate skeleton: slackM, existence checks, baseline/UPDATE_BASELINE, failure list, exit 1, catch tail
    - tools/route-dwg-accuracy-harness.mjs (lines 82-198) — runRouteDwgAccuracyHarness return shape; add dwgConfidence passthrough
    - tools/run-siriu-regression-gate.mjs — the gate already wired into test:gate (pattern to mirror, baseline conventions)
    - parser/__tests__/fixtures/luizcarolino-ground-truth.json — fixture array shape `[{ number, lat, lon }]`
    - parser/dwg/residual-gate.js — applyResidualGate, and the ANCHOR_* named constants to recalibrate and lock
    - .planning/phases/05-truth-free-residual-gate/05-RESEARCH.md (§Pitfall 2, §Open Questions 1-2, §State of the Art) — LC fixture source = PDF-path 21-31; ANCHOR thresholds must be measured on known-good routes before locking
  </read_first>
  <action>
    (a) FIXTURE (D-06): Create `parser/__tests__/fixtures/luizcarolino-residual-mustfail.json` containing the REAL captured
    LC PDF-path coords for posts 21-31 (NOT synthetic). Run the existing pipeline / harness to capture the actual PDF-path
    coords for LC posts 21-31, then store as `{ "lc-21-31-pdfpath": [ { "postNumber", "lat", "lon" }, ... ], "distances": [ ... labelled edges among 21-31 ... ], "gpsByPostNumber": { ... user-anchored PDF positions ... }, "expectedDecision": "fail", "expectedFailSubScore": "anchor" }`.
    This is the textbook shape-pass (~6.1 m abs / 18.4% rel) / anchor-fail (~178.7 m) case (Pitfall 2). Capture the
    DWG-cascade scrambled output only as a secondary "fails for any reason" note if convenient — the PRIMARY assertion is
    the anchor-fail on PDF-path 21-31.
    (b) HARNESS: Add `dwgConfidence: result.dwgConfidence ?? null` to the object returned by `runRouteDwgAccuracyHarness`
    in route-dwg-accuracy-harness.mjs (additive). Create `tools/residual-gate-harness.mjs` ONLY as a thin wrapper if a
    different shape is needed; otherwise import runRouteDwgAccuracyHarness directly from the gate (prefer reuse).
    (c) CALIBRATE (D-05): Run computeAnchorGap-derived gateDecision over Siriu, Valmor, João Born using their existing
    dwg-region + ground-truth fixtures. Measure each route's anchor p95GapM AND median shape relError. Set
    `ANCHOR_TRUST_M` = (max known-good route p95 gap) + slackM, `ANCHOR_FAIL_M` between that ceiling and LC's 202 m, and
    confirm Siriu/Valmor/João Born all yield gateDecision "trust" (median shape < 0.05). Replace the Plan-01 placeholder
    ANCHOR_* constants in residual-gate.js with these measured, locked values and update the header comment from
    "placeholder/calibrated in Plan 02" to "LOCKED <date>, calibrated against Siriu/Valmor/João Born".
    (d) GATE: Create `tools/run-residual-gate.mjs` mirroring run-route-dwg-accuracy-gate.mjs: copy slackM verbatim, the
    `#!/usr/bin/env node` + header doc with `RESIDUAL_UPDATE_BASELINE=1` refresh hint, existence-check guards, baseline
    write/compare with `RESIDUAL_UPDATE_BASELINE`, failure list → `console.error("  x ...")` → `process.exit(1)`, and the
    `main().catch(...)` tail. Assertions: for Siriu → `dwgConfidence.gateDecision === "trust"` (and median shape <
    SHAPE_TRUST); load the LC must-fail fixture, run applyResidualGate(computeResiduals(...), computeAnchorGap(...)) over
    its 21-31 coords/distances/gpsByPostNumber, and assert `gateDecision === "fail"` with the anchor sub-score (not shape)
    being the cause; sanity-assert Valmor/João Born do not return "fail".
  </action>
  <verify>
    <automated>node tools/run-residual-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - parser/__tests__/fixtures/luizcarolino-residual-mustfail.json exists, contains `"expectedDecision": "fail"` and `"expectedFailSubScore": "anchor"`, and its coords are real captured LC PDF-path 21-31 values (postNumber range 21-31)
    - route-dwg-accuracy-harness.mjs returned object includes `dwgConfidence`
    - residual-gate.js ANCHOR_* constants are concrete numbers with a header comment stating "LOCKED" + the calibrating routes (not "placeholder")
    - `node tools/run-residual-gate.mjs` runs the Siriu route and prints a PASS line asserting Siriu gateDecision === "trust"
    - `node tools/run-residual-gate.mjs` asserts the LC must-fail fixture yields gateDecision === "fail" caused by the anchor sub-score, and exits 0 (all assertions hold)
    - `node tools/run-residual-gate.mjs` confirms Valmor and João Born do NOT return gateDecision "fail" (no false-fail)
  </acceptance_criteria>
  <done>LC must-fail fixture captured, ANCHOR thresholds measured and locked, CI gate asserts Siriu trust + LC anchor-fail + no known-good false-fail.</done>
</task>

<task type="auto">
  <name>Task 3: Hook residual gate into npm run test:gate</name>
  <read_first>
    - package.json — current scripts.test:gate value (the chain to extend)
    - tools/run-residual-gate.mjs — the script being appended (must already pass standalone)
  </read_first>
  <action>
    In package.json, append ` && node tools/run-residual-gate.mjs` to the END of the existing `scripts.test:gate` string
    (after `node tools/run-siriu-regression-gate.mjs`). Change NOTHING else in package.json — no dependency additions
    (the gate uses only in-house modules + existing dev deps). Do not alter `scripts.test` or `scripts.test:unit`.
  </action>
  <verify>
    <automated>npm run test:gate</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts.test:gate ends with `&& node tools/run-residual-gate.mjs`
    - package.json dependencies and devDependencies are byte-identical to before this plan (no new packages)
    - `npm run test:gate` runs the full chain including the residual gate and exits 0
  </acceptance_criteria>
  <done>Residual gate runs as part of `npm run test:gate` and the full chain is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The residual gate is wired live (pure-judge, dwgConfidence attached), the LC 21-31 PDF-path rigid-offset is a locked
    must-fail fixture, ANCHOR_* thresholds are calibrated against real Siriu/Valmor/João Born output and locked, and
    `npm run test:gate` runs the new gate. No emitted coordinate bytes changed (Siriu regression gate still green).
  </what-built>
  <how-to-verify>
    1. Run `npm run test:gate` — confirm it exits 0 and the output includes a PASS line from the residual gate.
    2. Run `node tools/run-residual-gate.mjs` and read the printed sub-scores — confirm Siriu shows gateDecision "trust"
       with median shape < 5%, and the LC must-fail fixture shows gateDecision "fail" caused by the ANCHOR sub-score
       (shape ~6 m would alone pass; anchor ~179 m fails it). This is the ACC-03 success criterion.
    3. Confirm Valmor and João Born are reported as not-failing (no false-fail of known-good routes).
    4. Confirm the locked ANCHOR_* constant values in parser/dwg/residual-gate.js look sane (above known-good route gaps,
       below LC's ~202 m) and the header comment says LOCKED.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what the gate decided incorrectly (which route / which sub-score).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline coords → gate | Already-parsed floating-point coords/distances from the trusted pipeline; no external/untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure (false confidence) | applyResidualGate per-post tiering | mitigate | Fail-loud: any post without paired coord/incident edge → UNRESOLVABLE, never dropped (project lock); enforced by Plan-01 Task 2 + unit test |
| T-05-02 | Tampering (NaN/divide-by-zero corrupting the score) | computeResiduals / computeAnchorGap | mitigate | Guard `meters > 0` and `lat != null`; return null aggregates on empty sets (ASVS V5; Plan-01 Task 1) |
| T-05-03 | Tampering (silent coordinate drift via the gate) | coordinate-calculator-dwg.js integration | mitigate | Pure-judge D-01: attach dwgConfidence only; Siriu regression gate (run-siriu-regression-gate.mjs) re-asserts byte-identical output in Task 1 verify |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 installs ZERO packages (RESEARCH Package Audit: N/A); no install task exists, so no legitimacy checkpoint required |
</threat_model>

<verification>
- `npm run test:gate` exits 0 (full chain incl. residual gate).
- `node tools/run-siriu-regression-gate.mjs` still green (D-01 byte-identical proof).
- LC must-fail fixture yields gateDecision "fail" on the anchor sub-score; Siriu yields "trust".
- package.json has no new dependencies.
</verification>

<success_criteria>
- The gate is live as a pure judge (dwgConfidence attached, no coordinate change).
- The LC 21-31 rigid-offset is a locked regression fixture that fails the gate on the anchor sub-score (ACC-03).
- ANCHOR_* thresholds are calibrated against real route output and locked; known-good routes do not false-fail (D-05).
- The residual gate runs in CI via `npm run test:gate` (ACC-04).
</success_criteria>

<output>
Create `.planning/phases/05-truth-free-residual-gate/05-02-SUMMARY.md` when done.
</output>
