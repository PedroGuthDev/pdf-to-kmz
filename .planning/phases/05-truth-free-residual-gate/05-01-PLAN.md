---
phase: 05-truth-free-residual-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parser/dwg/residual-gate.js
  - parser/__tests__/residual-gate.test.mjs
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03, ACC-05]
must_haves:
  truths:
    - "computeResiduals returns a MEDIAN relative-error route score, not a mean (mean is 60.5% on Siriu, median 0.3%)"
    - "computeAnchorGap measures DWG-paired coords vs user-anchored PDF coords per post (non-zero downstream, ~0 at post 1)"
    - "applyResidualGate rates HIGH only when BOTH shape and anchor sub-scores pass"
    - "A post with no paired coord or no labelled incident edge is tagged UNRESOLVABLE, never dropped"
    - "Thresholds live as named constants in residual-gate.js (D-05), output is TIER labels only (D-07)"
  artifacts:
    - path: "parser/dwg/residual-gate.js"
      provides: "computeResiduals, computeAnchorGap, applyResidualGate pure functions"
      exports: ["computeResiduals", "computeAnchorGap", "applyResidualGate"]
      contains: "haversineMeters"
    - path: "parser/__tests__/residual-gate.test.mjs"
      provides: "node:test unit tests proving median aggregation, anchor gap, two-gate logic, UNRESOLVABLE tiering"
  key_links:
    - from: "parser/dwg/residual-gate.js"
      to: "parser/geo/utm-calibrator.js"
      via: "import haversineMeters"
      pattern: "import.*haversineMeters.*utm-calibrator"
---

<objective>
Build `parser/dwg/residual-gate.js` — the truth-free, pure-math quality judge. It exposes three
named ESM functions: `computeResiduals` (shape-fidelity sub-score, MEDIAN-aggregated), `computeAnchorGap`
(absolute-anchor sub-score: DWG-paired vs user-anchored PDF coords per post), and `applyResidualGate`
(two-gate decision + per-post HIGH/MED/LOW/UNRESOLVABLE tiers). Closes ACC-01, ACC-02, ACC-03 (logic),
ACC-05. No integration this plan — pure module + its unit tests only.

Purpose: This is the contract every downstream piece (Plan 02 integration, the CI gate, P7/P8) builds on.
Getting the aggregator (median) and the anchor formulation (D-03) right here is load-bearing.
Output: `parser/dwg/residual-gate.js` + `parser/__tests__/residual-gate.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-truth-free-residual-gate/05-CONTEXT.md
@.planning/phases/05-truth-free-residual-gate/05-RESEARCH.md
@.planning/phases/05-truth-free-residual-gate/05-PATTERNS.md

<interfaces>
<!-- Reuse the in-house haversine. Do NOT add turf.js or any geo/stats lib (STACK.md "Do NOT Add"). -->

From parser/geo/utm-calibrator.js:770 (VERIFIED at source):
  export function haversineMeters(lat1, lon1, lat2, lon2) -> number  // metres, spherical R=6371000

Import from a parser/dwg/* module:
  import { haversineMeters } from "../geo/utm-calibrator.js";

Module-convention analog — parser/dwg/cable-topology.js lines 1-29:
  - leading geo import on line 1
  - file-level + per-function JSDoc block ("why" + algorithm), typed @param/@returns
  - named `export function`, plain-object returns, ESM only ("type": "module"; no CommonJS, no default export)

Input shapes (VERIFIED from coordinate-calculator-dwg.js):
  cascade.coords: Array<{ postNumber: number, lat: number|null, lon: number|null, dwg_block?: any }>
  distances:      Array<{ from: number, to: number, meters: number|null, source: string }>
  gpsByPostNumber: Map<number, { lat: number, lon: number }>  // user-anchored PDF positions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeResiduals (shape) + computeAnchorGap (anchor) pure functions</name>
  <read_first>
    - parser/dwg/cable-topology.js (lines 1-40) — module/JSDoc/named-export convention to replicate
    - parser/geo/utm-calibrator.js (lines 760-780) — haversineMeters signature to import
    - .planning/phases/05-truth-free-residual-gate/05-PATTERNS.md (§residual-gate.js) — exact function bodies for computeResiduals + computeAnchorGap
    - .planning/phases/05-truth-free-residual-gate/05-RESEARCH.md (§Architecture Patterns, §State of the Art) — why MEDIAN not mean; live numbers
  </read_first>
  <behavior>
    - computeResiduals: over distances with `meters > 0` whose BOTH endpoints have non-null paired coords, computes relError = |haversine(A,B) − meters| / meters; returns { medianRelError, p95RelError, edgeCount, perEdge } where medianRelError is the MEDIAN of sorted relErrors (NOT mean). perEdge entries: { from, to, printed, hav, relError, residualM, source }.
    - computeResiduals on empty/no-valid-edge input returns medianRelError: null, p95RelError: null, edgeCount: 0, perEdge: [] (no divide-by-zero, no NaN).
    - computeAnchorGap: for each coord with non-null lat that has a matching gpsByPostNumber entry, gapM = haversine(dwgCoord, pdfCoord); returns { meanGapM, p95GapM, perPost: [{ postNumber, gapM }] }. Post 1 (shared anchor) gap ≈ 0; downstream posts diverge.
    - Guards: skip meters <= 0, skip null/NaN lat/lon, return null aggregates on empty sets.
  </behavior>
  <action>
    Create `parser/dwg/residual-gate.js`. Add `import { haversineMeters } from "../geo/utm-calibrator.js";` on line 1.
    Implement and named-export `computeResiduals(coords, distances)` and `computeAnchorGap(coords, gpsByPostNumber)`
    exactly per the bodies in 05-PATTERNS.md §residual-gate.js (median via `rels[Math.floor(rels.length / 2)]` after
    ascending sort; p95 via `Math.floor(rels.length * 0.95)`). Build the post lookup as
    `new Map(coords.filter(c => c.lat != null).map(c => [c.postNumber, c]))`. In computeResiduals skip any distance where
    `!(d.meters > 0)` (handles null/zero/NaN) and any edge whose `from`/`to` is not in the map. In computeAnchorGap skip
    `c.lat == null` and any post missing from gpsByPostNumber. Write a file-level JSDoc block plus per-function typed
    `@param`/`@returns` JSDoc following the cable-topology.js convention. CRITICAL: route-level shape aggregate is the
    MEDIAN — a mean here scores Siriu at 60.5% and breaks ACC-04. Do NOT add any npm dependency.
  </action>
  <verify>
    <automated>node -e "import('./parser/dwg/residual-gate.js').then(m=>{const e=['computeResiduals','computeAnchorGap'];for(const k of e)if(typeof m[k]!=='function')throw new Error('missing '+k);console.log('exports ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - parser/dwg/residual-gate.js contains `import { haversineMeters } from "../geo/utm-calibrator.js"`
    - parser/dwg/residual-gate.js contains `export function computeResiduals(` and `export function computeAnchorGap(`
    - computeResiduals route aggregate uses median indexing `Math.floor(rels.length / 2)` (grep: `Math.floor(rels.length / 2)`) and the file contains NO arithmetic-mean route score for shape (no `reduce` producing the returned medianRelError)
    - Node import probe above prints `exports ok` and exits 0
    - computeResiduals([], []) returns an object with medianRelError === null and edgeCount === 0 (no throw)
  </acceptance_criteria>
  <done>Both pure functions exported, median-aggregated, guard against empty/null input, no new dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applyResidualGate two-gate decision + per-post tiers + named threshold constants</name>
  <read_first>
    - parser/dwg/residual-gate.js — the file created in Task 1 (current state; add to it)
    - .planning/phases/05-truth-free-residual-gate/05-RESEARCH.md (§Pattern 3, §Pitfall 1, §Anti-Patterns) — two-gate rule, per-post tiering, "HIGH only when BOTH pass"
    - .planning/phases/05-truth-free-residual-gate/05-CONTEXT.md (D-04, D-05, D-07) — UNRESOLVABLE rule, named-constant thresholds, tier-labels-only
  </read_first>
  <behavior>
    - applyResidualGate(shape, anchor, thresholds?) returns { gateDecision: "trust"|"fallback"|"fail", shapeFidelity, anchorGap, postTiers } where postTiers is Array<{ postNumber, tier }> with tier in HIGH/MED/LOW/UNRESOLVABLE.
    - Route decision is "trust" ONLY when shape passes (median < SHAPE_TRUST) AND anchor passes (p95GapM < ANCHOR_TRUST_M). If either fails its hard threshold → "fail". Middle band → "fallback". (D-01: label only this phase, no behavior change downstream.)
    - Single-score proof: a result whose shape median passes but anchor p95 exceeds ANCHOR_FAIL_M yields gateDecision "fail" (the LC rigid-offset mechanism — ACC-03).
    - Per-post: a post with no paired coord OR no labelled incident edge → tier "UNRESOLVABLE" (flagged, never omitted). Otherwise shapeScore = MAX relError over the post's incident perEdge entries (worst edge, fail-loud); anchorScore = that post's gapM; tier from the HIGH/MED/LOW bands.
    - Thresholds are named module constants with research seeds (shape 5% trust / 15% fallback; ANCHOR_* placeholders); output exposes tier labels only, never a numeric percentage field.
  </behavior>
  <action>
    Extend `parser/dwg/residual-gate.js` with named constants near the top: `SHAPE_TRUST = 0.05`, `SHAPE_FALLBACK = 0.15`,
    `ANCHOR_TRUST_M`, `ANCHOR_FALLBACK_M`, `ANCHOR_FAIL_M` — seed the ANCHOR_* values from research (LC mean gap 202 m must
    fail; known-good routes must pass) as documented placeholders, with a header comment noting they are calibrated and
    LOCKED in Plan 02 against real Siriu/Valmor/João Born output (D-05). Implement and named-export
    `applyResidualGate(shape, anchor, thresholds = {})` returning `{ gateDecision, shapeFidelity, anchorGap, postTiers }`.
    Decision: `gateDecision = (shape.medianRelError != null && shape.medianRelError < SHAPE_TRUST && anchor.p95GapM != null && anchor.p95GapM < ANCHOR_TRUST_M) ? "trust" : (shape.medianRelError >= SHAPE_FALLBACK || anchor.p95GapM >= ANCHOR_FAIL_M) ? "fail" : "fallback"`.
    Build a per-post incident-edge index from `shape.perEdge` (group by `from` and `to`) and an anchor lookup from
    `anchor.perPost`. For each post present in either index: if it has no incident edge AND no anchor entry → UNRESOLVABLE;
    else shapeScore = max incident relError (or null if no edge), anchorScore = gapM (or null); derive tier via HIGH/MED/LOW
    bands (HIGH = shapeScore < SHAPE_TRUST AND anchorScore < ANCHOR_TRUST_M). Use the MED/LOW boundary as a single
    intermediate threshold (Claude's-Discretion D-05; document the choice in JSDoc). Tier values are string labels only —
    do NOT emit any numeric % field (D-07). Keep `shapeFidelity`/`anchorGap` as the raw sub-score objects for diagnostics.
  </action>
  <verify>
    <automated>node --test parser/__tests__/residual-gate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - parser/dwg/residual-gate.js contains `export function applyResidualGate(`
    - residual-gate.js declares named constants `SHAPE_TRUST`, `ANCHOR_TRUST_M`, `ANCHOR_FAIL_M` (grep matches each token)
    - A unit case where shape.medianRelError = 0.03 and anchor.p95GapM = 200 yields gateDecision === "fail" (shape passes, anchor fails — the ACC-03 mechanism)
    - A unit case where shape.medianRelError = 0.003 and anchor.p95GapM is below ANCHOR_TRUST_M yields gateDecision === "trust"
    - A post supplied with no incident edge and no anchor entry yields tier === "UNRESOLVABLE"
    - No code path in residual-gate.js returns a numeric percentage field in postTiers (tier values are only HIGH/MED/LOW/UNRESOLVABLE strings)
    - `node --test parser/__tests__/residual-gate.test.mjs` exits 0
  </acceptance_criteria>
  <done>applyResidualGate exports a two-gate decision + per-post tiers; thresholds are named constants; tier labels only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests proving median aggregation, anchor gap, two-gate logic, UNRESOLVABLE tiering</name>
  <read_first>
    - parser/dwg/residual-gate.js — the module under test (final state after Tasks 1-2)
    - parser/__tests__/coordinate-calculator.test.mjs — existing node:test style/assert conventions in this repo
    - .planning/phases/05-truth-free-residual-gate/05-RESEARCH.md (§State of the Art table) — the live numbers to encode as expectations
  </read_first>
  <behavior>
    - Test: computeResiduals returns MEDIAN — feed edges where one outlier edge has ~1000% relError and the rest ~0; assert medianRelError stays small (< 0.05) and that the equivalent MEAN would exceed 0.5 (prove median ≠ mean).
    - Test: computeResiduals with `meters: null` / `meters: 0` edges skips them (edgeCount excludes them); empty input → medianRelError null, no throw.
    - Test: computeAnchorGap — post 1 with identical dwg/pdf coords yields gapM ≈ 0; a downstream post offset ~180 m yields gapM in the 150-220 m range (haversine sanity).
    - Test: applyResidualGate — shape-pass/anchor-fail → "fail"; both-pass → "trust"; post with no edge/anchor → "UNRESOLVABLE".
  </behavior>
  <action>
    Create `parser/__tests__/residual-gate.test.mjs` using `node:test` (`test`, `assert` from `node:assert/strict`),
    mirroring the import/assert style of `coordinate-calculator.test.mjs`. Import the three functions from
    `../dwg/residual-gate.js`. Encode the median-vs-mean proof using a synthetic perEdge set (one 10x-error outlier plus
    several near-zero edges) and assert `medianRelError < 0.05` while computing the mean inline and asserting it `> 0.5`.
    Use real lat/lon pairs roughly 180 m apart (e.g. two points differing ~0.0016° in latitude near Santa Catarina,
    ~ -27.6, -48.6) for the anchor-gap range assertion. Cover the four applyResidualGate cases (trust, fail-on-anchor,
    fallback band, UNRESOLVABLE post). Do not import any route fixture file here — this plan's tests are pure-unit;
    route-level CI assertions live in Plan 02.
  </action>
  <verify>
    <automated>node --test parser/__tests__/residual-gate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - parser/__tests__/residual-gate.test.mjs imports computeResiduals, computeAnchorGap, applyResidualGate from `../dwg/residual-gate.js`
    - Test file contains an assertion that medianRelError < 0.05 alongside an inline mean computed > 0.5 on the same edge set
    - Test file contains a "fail" assertion for a shape-pass/anchor-fail case and an "UNRESOLVABLE" assertion for an edge-less anchor-less post
    - `node --test parser/__tests__/residual-gate.test.mjs` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Unit suite passes and locks the median aggregator, anchor-gap math, two-gate decision, and UNRESOLVABLE rule.</done>
</task>

</tasks>

<verification>
- `node --test parser/__tests__/residual-gate.test.mjs` exits 0.
- residual-gate.js exports exactly computeResiduals, computeAnchorGap, applyResidualGate (named, ESM).
- No new entries added to package.json dependencies (grep package.json — unchanged this plan).
- Route shape aggregate is median (grep `Math.floor(rels.length / 2)`), never a mean.
</verification>

<success_criteria>
- The pure residual-gate module exists with the three contract functions and named threshold constants.
- Median aggregation, the D-03 anchor-gap formulation, the two-gate "HIGH only when both pass" rule, and the
  UNRESOLVABLE-never-drop rule are all proven by passing unit tests.
- Zero new npm dependencies; tier labels only (no numeric %).
</success_criteria>

<output>
Create `.planning/phases/05-truth-free-residual-gate/05-01-SUMMARY.md` when done.
</output>
