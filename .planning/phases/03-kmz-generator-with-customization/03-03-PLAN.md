---
phase: 3
plan: "03-03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - parser/kmz-packager.js
  - parser/pdf-parser.js
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - KMZ-04
---

<objective>
Package KML strings into downloadable KMZ blobs via JSZip 3.10.1, with dual-environment loading (browser CDN ESM + Node npm). Re-export `buildKml` and `packageKmz` from `parser/pdf-parser.js` for a single import surface.

After this plan, Node can produce a Blob/Buffer KMZ; browser import path is ready for index.html.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| ZIP slip / wrong entry name | low | Only write `doc.kml` at ZIP root per Google KMZ spec |
| Supply-chain (JSZip CDN) | low | Pin 3.10.1; match package.json version |
</threat_model>

<tasks>

<task id="T-03-03-01" type="execute">
<title>Add jszip@3.10.1 dependency</title>
<read_first>
- package.json
- .planning/phases/03-kmz-generator-with-customization/03-RESEARCH.md (Standard Stack)
</read_first>
<files>package.json, package-lock.json</files>
<action>
Run `npm install jszip@3.10.1 --save` (or add to dependencies manually and install).

Ensure `package.json` `dependencies` includes `"jszip": "3.10.1"` (exact or ^3.10.1 per project convention).
</action>
<acceptance_criteria>
- `package.json` lists `jszip` at 3.10.x
- `npm ls jszip` resolves without error
</acceptance_criteria>
</task>

<task id="T-03-03-02" type="execute">
<title>Create parser/kmz-packager.js — packageKmz with getJSZip</title>
<read_first>
- parser/pdf-parser.js (`getPdfjsLib` lazy import pattern, lines ~1–40)
- .planning/phases/03-kmz-generator-with-customization/03-RESEARCH.md (Pattern 2, browser CDN snippet)
- .planning/phases/03-kmz-generator-with-customization/03-PATTERNS.md (kmz-packager section)
</read_first>
<files>parser/kmz-packager.js</files>
<action>
Create `parser/kmz-packager.js`.

Implement `async function getJSZip()`:
- If `typeof process !== 'undefined' && process.versions?.node`: `return (await import('jszip')).default`
- Else: `return (await import('https://cdn.jsdelivr.net/npm/jszip@3.10.1/+esm')).default`

Export `async function packageKmz(kmlString)`:
- Validate `kmlString` is non-empty string; throw if empty.
- `const zip = new JSZip(); zip.file('doc.kml', kmlString);`
- `return zip.generateAsync({ type: 'blob', compression: 'DEFLATE' });`

No other files in the ZIP root.
</action>
<acceptance_criteria>
- `parser/kmz-packager.js` exports `packageKmz`
- Node script can `import { packageKmz } from './parser/kmz-packager.js'` and await a Blob-like result
- Generated zip contains entry named exactly `doc.kml`
- `getJSZip` is not required export (may stay private)
</acceptance_criteria>
</task>

<task id="T-03-03-03" type="execute">
<title>Re-export buildKml and packageKmz from pdf-parser.js</title>
<read_first>
- parser/pdf-parser.js (existing coordinate-calculator re-export block ~65–73)
- parser/kml-builder.js
- parser/kmz-packager.js
</read_first>
<files>parser/pdf-parser.js</files>
<action>
Add imports:
`import { buildKml } from './kml-builder.js';`
`import { packageKmz } from './kmz-packager.js';`

Add to export block (after coordinate exports):
`export { buildKml, packageKmz };`

Also export `mergeOptions` from `./kmz-defaults.js` for index.html convenience (optional but recommended).
</action>
<acceptance_criteria>
- `parser/pdf-parser.js` contains `export { buildKml, packageKmz`
- `import { buildKml, packageKmz } from './parser/pdf-parser.js'` works in Node type-check/import test
</acceptance_criteria>
</task>

<task id="T-03-03-04" type="execute">
<title>Smoke script — package sample KML to KMZ on disk (Node)</title>
<read_first>
- parser/kml-builder.js
- parser/kmz-packager.js
</read_first>
<files>debug-package-kmz.mjs</files>
<action>
Add root `debug-package-kmz.mjs` (or extend existing debug script): build minimal 2-post KML via `buildKml`, call `packageKmz`, write `route-smoke.kmz` with `fs.writeFileSync` from Blob buffer in Node.

Document one-line run in plan summary only; file is dev utility.
</action>
<acceptance_criteria>
- `node debug-package-kmz.mjs` exits 0 and creates `route-smoke.kmz`
- File is non-zero bytes
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
node debug-package-kmz.mjs
unzip -l route-smoke.kmz 2>/dev/null || node -e "const fs=require('fs'); console.log('kmz size', fs.statSync('route-smoke.kmz').size)"
```
</verification>

<must_haves>
<truths>
- KMZ archive contains single root file `doc.kml`
- Browser and Node can import the packager via the same module API
</truths>
<artifacts>
- parser/kmz-packager.js
- package.json (jszip dependency)
</artifacts>
</must_haves>
