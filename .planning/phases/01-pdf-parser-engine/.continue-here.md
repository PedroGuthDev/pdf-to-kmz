---
context: phase
phase: 01-pdf-parser-engine
task: 2
total_tasks: 2
status: in_progress
last_updated: "2026-05-14T01:30:00Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: WRONG-OPS-CONSTANTS — pdf.js 5.x OPS values differ from plan docs — FIXED in commit 4f072de; verify text-extractor.js uses 44/45
- [ ] CONSTRAINT: DRAWOPS-FORMAT — constructPath args format is [renderOp, interleavedData, bbox], NOT [opsArray, coordsArray] — FIXED in commit 4f072de; verify construct-path-parser.js reads args[1]

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| WRONG-OPS-CONSTANTS | Plan docs said showText=49, showSpacedText=50. Actual pdf.js 5.x values are showText=44, showSpacedText=45. The text extractor recorded zero CTM positions, so CTM correlation matched nothing and post count was 0. Fixed in 4f072de. | blocking | Always verify OPS constants against the installed pdfjs-dist source (node_modules/pdfjs-dist/build/pdf.mjs line ~245) before implementing any operator-list walker. |
| DRAWOPS-FORMAT | Plan docs said constructPath args = [opsArray, coordsArray] with op codes 13-19. Actual pdf.js 5.x format is [renderOp_int, interleavedData_Float32Array, bbox_Float32Array]. The interleavedData uses DrawOPS codes 0=moveTo, 1=lineTo, 2=curveTo, 3=quadCurveTo, 4=closePath, interleaved with their coordinate values. The error was "ops is not iterable" because args[0]=20 (OPS.stroke integer) was passed to `for...of`. Fixed in 4f072de. | blocking | Read the actual makePathFromDrawOPS function (line ~1825 in pdf.mjs) to confirm the interleaved format before writing any constructPath decoder. |

<current_state>
Phase 1, Plan 01-03 (Browser UI), Task 2: checkpoint:human-verify.

Task 1 (build index.html) is complete and committed (5b2827d).

Three fixes have been applied:
1. commit 4f072de: OPS constants (49→44, 50→45) and constructPath DrawOPS format
2. commit ec5771f: all-page text fallback in pdf-parser.js when CTM correlation yields 0 items

Root cause of 0 posts/distances: CTM correlation in text-extractor.js produces positions that
don't match getTextContent() item positions (likely because pdf.js applies the page's initial
transform to its internal CTM, while our walker starts from identity). The fallback bypasses
the CTM correlation and uses all getTextContent items filtered by pattern (/^\d{1,3}$/ for
posts, numeric for distances), relying on proximity matching to associate items correctly.

The user needs to re-test the browser with the sample PDF after these fixes.
</current_state>

<completed_work>

Completed This Session:
- Plan 01-02, Task 1: ocg-map.js and construct-path-parser.js (commit cfa5900)
- Plan 01-02, Task 2: text-extractor.js and graphics-extractor.js (commit a5a2fad)
- Plan 01-02, Task 3: post-assembler.js, distance-associator.js, cable-builder.js, pdf-parser.js (commit e552413)
- Plan 01-02 SUMMARY.md created (commit 9e40f23)
- Plan 01-03, Task 1: index.html built at project root (commit 5b2827d)
- Plan 01-03 SUMMARY.md created (commit 7844050) — with checkpoint status
- Fix: OPS constants (49→44, 50→45) and constructPath DrawOPS format (commit 4f072de)
</completed_work>

<remaining_work>

- Plan 01-03, Task 2: User must test index.html in browser with real PDF → type "approved"
- After approval: continuation agent finishes plan 01-03 (creates/updates SUMMARY.md)
- Phase 1 close-out: update ROADMAP.md [x] for 01-02 and 01-03, update STATE.md
</remaining_work>

<decisions_made>

- pdf.js 5.7.284 constructPath args format is [renderOp, interleavedDrawOPS, bbox] — NOT [opsArray, coordsArray] as plan docs stated
- DrawOPS codes: 0=moveTo, 1=lineTo, 2=curveTo(cubic), 3=quadraticCurveTo, 4=closePath
- pdf.js 5.x OPS.showText=44, OPS.showSpacedText=45 (corrected from wrong plan values 49/50)
- Circle positions still use CTM (e,f) from graphics-extractor — this approach is correct
</decisions_made>

<blockers>

- HUMAN REQUIRED: User must run `npx serve .` and test index.html in browser with sample PDF.
  Looking for: Posts found > 0, Distances found > 0, no parse_failed error.
  After confirming, type "approved" to proceed.
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-pdf-parser-engine/SKELETON.md` — layer map, CTM approach, output contract
2. `.planning/phases/01-pdf-parser-engine/01-03-PLAN.md` — plan 01-03 task 2 acceptance criteria
3. `parser/construct-path-parser.js` — verify DrawOPS format is correct (args[1] interleaved)
4. `parser/text-extractor.js` — verify OPS_SHOW_TEXT=44, OPS_SHOW_SPACED_TEXT=45

## Infrastructure State
- Local HTTP server: NOT running — user must start with `npx serve .` at project root
- No running background processes

<context>
Session was executing Phase 1 of the PDF-to-KMZ converter. All 8 parser modules were built in
plan 01-02. Plan 01-03 built index.html successfully. During browser testing, the user reported
"Parse failed: ops is not iterable" which led to discovering two critical bugs in the plan's
documented OPS constants vs actual pdf.js 5.7.284 values.

The bugs were found by reading the installed pdfjs-dist source in node_modules and are fixed.
The constructPath format in pdf.js 5.x uses a completely different argument structure than the
plan assumed — the ops and coordinates are interleaved in a single Float32Array using DrawOPS
codes (0-4), not the separate [opsArray, coordsArray] format documented in RESEARCH.md.

After the user approves the browser test, the continuation agent (a98384a62302ca1c9) should
be messaged to complete plan 01-03 Task 2, then the orchestrator needs to update ROADMAP.md
and STATE.md to close Phase 1.
</context>

<next_action>
Start with: Run `npx serve .` at project root, open browser at localhost, load sample PDF,
verify Posts found > 0, then type "approved" to trigger the continuation agent for plan 01-03.
After approval: run /gsd-execute-phase 01 to pick up from the checkpoint.
</next_action>
