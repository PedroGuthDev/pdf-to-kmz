---
phase: 01-pdf-parser-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/skeleton-test.html
autonomous: false
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04
user_setup: []

must_haves:
  truths:
    - pdf.js 5.7.284 loads from jsDelivr CDN without errors in the browser
    - All OCG layer names in the real sample PDF are logged to console
    - At least one beginMarkedContentProps sentinel is found in page 2 text content
    - TEXTO layer yields text items containing two-digit post number strings
    - Numero_Poste layer yields at least one constructPath fn=91 operation
    - Raw constructPath args from Numero_Poste are logged to resolve assumption A1 circle shape
    - Distancia_Poste layer name variant accent vs no accent is resolved from allNames log
  artifacts:
    - path: test/skeleton-test.html
      provides: Walking Skeleton validation page
      contains: walkingSkeleton
  key_links:
    - from: test/skeleton-test.html
      to: cdn.jsdelivr.net/npm/pdfjs-dist@5.7.284/build/pdf.mjs
      via: ESM import
      pattern: pdfjs-dist@5.7.284
---
<tasks>
<task type="execute">
  <name>Task 1: Build Walking Skeleton</name>
  <description>Create a minimal HTML page that loads pdf.js from CDN and logs OCG layer names and raw path data to the console.</description>
  <files_modified>
    - test/skeleton-test.html
  </files_modified>
  <acceptance_criteria>
    - test/skeleton-test.html exists
    - Uses pdfjsLib from cdn.jsdelivr.net/npm/pdfjs-dist@5.7.284/build/pdf.mjs
    - Contains: walkingSkeleton
    - Contains: id="fileInput"
    - No console errors when opened with no PDF loaded
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Walking Skeleton on real PDF and record A1/A2 resolutions</name>
  <what-built>
    test/skeleton-test.html logs 5 acceptance criteria plus A1 and A2 assumption resolution data when the real INFOVIAS sample PDF is loaded.
  </what-built>
  <how-to-verify>
    1. Open test/skeleton-test.html in Chrome or Firefox
    2. Click file input, select real sample PDF
    3. Open DevTools (F12) -> Console
    4. Confirm ALL of the following:
       a. "pdf.js version:" shows 5.x.y
       b. "All OCG layer names:" array has at least 4 entries
       c. "AC3 hasBeginMarked: true"
       d. "AC4 TEXTO items:" has two-digit number strings
       e. "AC5 Numero_Poste constructPath count:" greater than 0
       f. "A1 first Numero_Poste constructPath ops:" array of integers
       g. "A2 distance layer name:" non-empty string
    5. Record for SKELETON.md: A1 op codes and A2 exact layer name.
  </how-to-verify>
</task>
</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | skeleton-test.html file input | mitigate | Wrap walkingSkeleton in try/catch; log error to console |
| T-01-02 | Denial of Service | Large PDF | accept | Dev-only tool; size guard goes in index.html Plan 02 |
| T-01-03 | Tampering | PDF embedded JavaScript | accept | pdf.js disables PDF JS execution by default |
</threat_model>

<verification>
All 5 RESEARCH.md Walking Skeleton acceptance criteria pass on the real sample PDF.
A1 circle shape op codes and A2 exact distance layer name are resolved.
</verification>

<success_criteria>
- test/skeleton-test.html opens in browser without console errors
- Loading real sample PDF logs all 5 AC values plus A1/A2 resolution data
- Checkpoint approved and assumption resolutions recorded before Plan 02 begins
</success_criteria>