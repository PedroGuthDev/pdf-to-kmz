---
phase: 08-global-pdf-dxf-solver
plan: 03
type: execute
wave: 4
depends_on: [08-02]
files_modified:
  - parser/dwg/coordinate-calculator-dwg.js
  - parser/__tests__/global-solver-cascade.test.mjs
  - tools/run-solver-timing-gate.mjs
  - package.json
autonomous: true
requirements: [SOLVE-02, SOLVE-04]
must_haves:
  truths:
    - "solveGlobalGraphAlignment runs as cascade level-0, BEFORE pairPostsByGraphWalk, inside runDwgPairingCascade (SOLVE-02)"
    - "On any solver accept-bar failure the cascade logs 'solver demoted; using graph-walker' and runs the walker UNCHANGED with pristine inputs (D-04)"
    - "The walker output on Siriu is byte-identical to its pre-P8 behavior when the solver does not produce the emitted coords (strangler-fig)"
    - "The success result carries solverPath, solverDemoted, demotionReason, solverScore + a human-readable warnings[] entry (D-13)"
    - "All four route gates (Siriu regression + per-post position, LC, JB, Valmor) re-clear green to phase exit (D-06, SOLVE-04)"
  artifacts:
    - path: "parser/dwg/coordinate-calculator-dwg.js"
      provides: "level-0 solver call in runDwgPairingCascade + D-13 structured fields on success result"
      contains: "solveGlobalGraphAlignment"
    - path: "parser/__tests__/global-solver-cascade.test.mjs"
      provides: "level-0-then-demote, pristine-walker-input, Siriu byte-identical-when-uninvoked, D-13 fields"
    - path: "tools/run-solver-timing-gate.mjs"
      provides: "2s budget assertion per named route (Pitfall 8)"
  key_links:
    - from: "parser/dwg/coordinate-calculator-dwg.js"
      to: "parser/dwg/global-solver.js"
      via: "runDwgPairingCascade calls solveGlobalGraphAlignment first (level-0)"
      pattern: "solveGlobalGraphAlignment"
    - from: "parser/dwg/coordinate-calculator-dwg.js"
      to: "parser/dwg/graph-walker.js"
      via: "pairPostsByGraphWalk runs UNCHANGED on demotion with pristine inputs"
      pattern: "pairPostsByGraphWalk"
---

<objective>
Wire the solver in as cascade level-0 and prove zero regression across all four routes. Insert
`solveGlobalGraphAlignment` at the TOP of `runDwgPairingCascade` (before the existing
`pairPostsByGraphWalk` level-1 call). On accept (D-05 bar green) the solver's coords are used and the
walker is NOT run (short-circuit, D-04). On any failure, log `"solver demoted; using graph-walker"`
and call `pairPostsByGraphWalk` UNCHANGED with the SAME pristine inputs (the solver must not have
mutated posts/distances/regionPosts — established in Wave 1). Attach the D-13 structured fields
(`solverPath`, `solverDemoted`, `demotionReason`, `solverScore`) to the success result and push a
human-readable string to the existing `warnings`/`userWarnings` channel the diagnostic panel reads.
Add a 2s-budget timing gate. Then re-clear ALL FOUR route gates (Siriu regression + per-post position,
LC, JB, Valmor) green (D-06, SOLVE-04), asserting the walker stays byte-identical on Siriu when it is
the emitter (strangler-fig, Pitfall 2).

Purpose: This is the phase-exit wave — it makes the solver live as level-0 (SOLVE-02) while proving
the hard red-line gates (per-post position, Siriu regression, junction oracles) stay green at every
checkpoint and the walker remains a pristine byte-identical fallback (SOLVE-04). KMZ/UI tier surfacing
is explicitly Phase 9 — this wave only emits the structured data channel.
Output: level-0 wiring + D-13 fields in coordinate-calculator-dwg.js; cascade integration tests;
solver timing gate wired into npm run test:gate; four-route green bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-global-pdf-dxf-solver/08-CONTEXT.md
@.planning/phases/08-global-pdf-dxf-solver/08-RESEARCH.md
@.planning/phases/07-solver-prerequisites/07-GATE-AUDIT.md

<interfaces>
<!-- The insertion point (parser/dwg/coordinate-calculator-dwg.js) -->
function runDwgPairingCascade({ posts, distances, connections, startLat, startLon, regionData,
  regionPosts, regionEdges, postIndex, adjacencyGraph, warnings, gpsByPostNumber }) { ... }  // line 134
// Level 1 today: pairPostsByGraphWalk(...) → { ok:true, coords, dwgPath:"dwg-graph-walk" }  (line 149/166)
// Level 2 today: pairPostsAgainstRegion(...) → dwgPath:"dwg-pdf-walk"  (line 170/187)
// Caller fallback: { ok:false } → "pdf-fallback"
// INSERT level-0 before the Level-1 pairPostsByGraphWalk call; on solver demote, fall through to it UNCHANGED.

<!-- Success-result build where D-13 fields attach (coordinate-calculator-dwg.js lines 433-455) -->
// successResult = { ...pdfResult, posts: dwgPosts, ..., dwgStatus: cascade.dwgPath, dwgRegionId };
// successResult.dwgConfidence = applyResidualGate(shape, anchor);   // line 452 — D-13 fields go alongside
// Solver success: cascade.dwgPath = "global-solve"

<!-- Solver entry (parser/dwg/global-solver.js — built Waves 1-2) -->
export function solveGlobalGraphAlignment({ posts, distances, connections, startLat, startLon,
  regionData, regionPosts, regionEdges, postIndex, adjacencyGraph, gpsByPostNumber }):
  { ok:true, coords, elapsedMs, solverScore, warnings } | { ok:false, reason, partialCoords };

<!-- Walker (parser/dwg/graph-walker.js) — MUST stay byte-identical; NEVER edit this file -->
export function pairPostsByGraphWalk({ posts, distances, connections, startLat, startLon, region,
  postIndex, adjacencyGraph, warnings, gpsByPostNumber }): { ok, coords };   // line 1047
</interfaces>

<warning>
PRISTINE-INPUT RULE (Pitfall 2): the walker on demotion must receive the EXACT same inputs it
receives today — never a state mutated by the solver attempt. The solver is already non-mutating
(Wave 1 immutability test), but the cascade wiring must pass the walker the original objects, not
solver-derived ones. NEVER edit graph-walker.js. The Siriu per-post position gate (tol 1.0 pt) and
the Siriu regression gate are HARD red-lines (07-GATE-AUDIT §3) — a RED there is a real regression and
BLOCKS the commit; never re-seed a hard red-line baseline to mask a move. The residual-gate
LC-must-fail and the LC/JB txt-accuracy zero-bad-tier rules are SOFT fences — if a correct solver fix
flips them, re-baseline deliberately per 07-GATE-AUDIT §5 (confirm all hard red-lines green first).
</warning>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert level-0 solver into runDwgPairingCascade with demotion + D-13 fields</name>
  <files>parser/dwg/coordinate-calculator-dwg.js, parser/__tests__/global-solver-cascade.test.mjs</files>
  <read_first>
    - parser/dwg/coordinate-calculator-dwg.js lines 128-192 (runDwgPairingCascade — exact insertion point + return shape) and lines 380-456 (call site inputs + success-result build where dwgConfidence/D-13 attach)
    - parser/dwg/global-solver.js (solveGlobalGraphAlignment signature + return shape from Waves 1-2)
    - parser/dwg/graph-walker.js lines 1047-1075 (pairPostsByGraphWalk param shape — confirm inputs passed unchanged)
    - .planning/phases/08-global-pdf-dxf-solver/08-RESEARCH.md §"Cascade Integration"
  </read_first>
  <behavior>
    - Level-0 first: when the solver accepts, runDwgPairingCascade returns { ok:true, coords, dwgPath:"global-solve" } and pairPostsByGraphWalk is NOT called (assert via a spy/counter that the walker ran 0 times).
    - Demotion: when the solver returns { ok:false, reason }, console.log emits "solver demoted; using graph-walker", and pairPostsByGraphWalk runs and produces the emitted coords (walker call count = 1).
    - Pristine input: the object passed to pairPostsByGraphWalk on demotion is deep-equal to the object the cascade received (no solver mutation leaked in).
    - Siriu byte-identical: on the Siriu route, the emitted coords equal the pre-P8 walker output exactly — whether the cascade chose global-solve OR demoted (if global-solve is chosen for Siriu, its coords still re-clear the regression + per-post position gates byte-for-byte; if demoted, the walker output is unchanged).
    - D-13 fields: the success result carries solverPath (the chosen dwgPath), solverDemoted (boolean), demotionReason (string|null), solverScore (residual sub-scores|null), and a human-readable string pushed to warnings/userWarnings.
  </behavior>
  <action>
    In `parser/dwg/coordinate-calculator-dwg.js`, import `solveGlobalGraphAlignment` from
    `./global-solver.js`. At the TOP of `runDwgPairingCascade` (before the Level-1
    `pairPostsByGraphWalk` block, ~line 148) add a level-0 call passing the same destructured params.
    On `level0.ok` return `{ ok:true, coords: level0.coords, dwgPath:"global-solve", solverScore:
    level0.solverScore, solverDemoted:false }`. On failure, `console.log("solver demoted; using
    graph-walker")`, push a structured warning `{ kind:"dwg-solver-demoted", reason: level0.reason }`
    to `warnings`, and fall through to the EXISTING Level-1 `pairPostsByGraphWalk` block UNCHANGED —
    pass it the original params, never solver-derived state. Carry `solverDemoted:true` +
    `demotionReason: level0.reason` on the returned cascade object. Then at the success-result build
    (~lines 433-455, where `successResult.dwgConfidence` is set) attach D-13 fields:
    `successResult.solverPath = cascade.dwgPath`, `successResult.solverDemoted = cascade.solverDemoted
    ?? false`, `successResult.demotionReason = cascade.demotionReason ?? null`,
    `successResult.solverScore = cascade.solverScore ?? null`, and push a human-readable Portuguese-or-
    neutral string to `successResult.warnings` (the diagnostic-panel channel) — DO NOT render UI here
    (Phase 9). Do NOT edit graph-walker.js, residual-gate.js, or pairPostsAgainstRegion. Write
    `parser/__tests__/global-solver-cascade.test.mjs` covering the six behaviors with a walker-call
    spy and a Siriu-region fixture. Tests RED first, then GREEN.
  </action>
  <verify>
    <automated>node --test parser/__tests__/global-solver-cascade.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - coordinate-calculator-dwg.js contains `solveGlobalGraphAlignment` called inside runDwgPairingCascade BEFORE the pairPostsByGraphWalk call (verify by line order)
    - graph-walker.js is unmodified (git diff for parser/dwg/graph-walker.js is empty)
    - The demotion path logs exactly `solver demoted; using graph-walker` (asserted in test)
    - The walker-call-spy test asserts walker ran 0 times on accept and 1 time on demote
    - The pristine-input test deep-equals the walker's received params to the cascade's received params
    - The success result exposes solverPath, solverDemoted, demotionReason, solverScore (all four asserted present)
    - `node --test parser/__tests__/global-solver-cascade.test.mjs` exits 0
  </acceptance_criteria>
  <done>Solver is cascade level-0; demotion is observable and logged; walker runs pristine and unedited on demote; D-13 fields populate the success result.</done>
</task>

<task type="auto">
  <name>Task 2: Solver 2s timing gate + four-route green-bar exit (zero regression)</name>
  <files>tools/run-solver-timing-gate.mjs, package.json</files>
  <read_first>
    - package.json lines 13-14 (test:gate / test:gate:fixtures chains — where to add the timing gate)
    - tools/run-siriu-regression-gate.mjs and tools/run-siriu-post-position-gate.mjs (existing hard red-line gate style to mirror for the new timing gate)
    - .planning/phases/07-solver-prerequisites/07-GATE-AUDIT.md §3 (hard red-lines) and §5 (re-baseline protocol for soft fences)
    - .planning/phases/08-global-pdf-dxf-solver/08-RESEARCH.md §"Validation Architecture"
  </read_first>
  <action>
    Create `tools/run-solver-timing-gate.mjs` mirroring the existing `tools/run-*-gate.mjs` exit-code
    style: for each named route (Siriu/LC/JB/Valmor) run the cascade and assert the solver's reported
    `elapsedMs < 2000` (Pitfall 8); exit non-zero with a clear message on breach. Wire it into the
    `test:gate:fixtures` chain in package.json. Then run `npm run test:gate` and confirm a single
    green bar across all four routes. The HARD red-lines (`run-{siriu,lc,joaoborn,valmor}-post-
    position-gate.mjs`, `run-siriu-regression-gate.mjs`, `branch-traversal*.test.mjs`) MUST be green —
    a RED there is a real regression: fix the solver, NEVER re-seed the baseline. If a SOFT fence
    (`run-residual-gate.mjs` LC-must-fail, `run-lc-txt-accuracy-gate.mjs`/`run-joaoborn-txt-accuracy-
    gate.mjs` zero-bad-tier, JB/Valmor cumulative ceilings) flips RED because the solver legitimately
    fixed a compensated error (e.g. LC 21-31 now within tier), follow the §5 re-baseline protocol:
    confirm ALL hard red-lines green, confirm the RED is an intended improvement, re-seed that fence's
    documented baseline deliberately, record it in the SUMMARY, and re-run to a single green bar. For
    each named route, record in the SUMMARY whether the cascade chose `global-solve` or demoted to the
    walker, and the post-position-gate max error.
  </action>
  <verify>
    <automated>npm run test:gate</automated>
  </verify>
  <acceptance_criteria>
    - `tools/run-solver-timing-gate.mjs` exists and is referenced in package.json `test:gate:fixtures`
    - `npm run test:gate` exits 0 (single green bar across all four routes)
    - The four per-post position gates and the Siriu regression gate are GREEN (hard red-lines, not re-seeded to mask a move) — confirmed by their individual gate output in the run
    - The four junction oracles (branch-traversal*.test.mjs) are GREEN
    - The solver timing gate reports elapsedMs < 2000 for every named route
    - SUMMARY records, per route, the chosen path (global-solve vs demote) and the post-position max error; any soft-fence re-baseline is documented with the §5 protocol steps
  </acceptance_criteria>
  <done>Solver runs within the 2s budget on all named routes; npm run test:gate is a single green bar; all four route gates re-clear with zero regression; walker byte-identical on Siriu when it emits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver level-0 → walker fallback | A solver attempt must never leak mutated state into the walker's pristine fallback run |
| solver coords → emitted KMZ | Accepted solver coords become the trusted output; a wrong accept emits silently-wrong GPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering (state leak) | runDwgPairingCascade demotion | mitigate | Walker receives original pristine params; solver is non-mutating (Wave-1 immutability test); graph-walker.js unedited (byte-identical) |
| T-08-09 | Information disclosure (silent Siriu regression) | shared cascade code | mitigate | Per-post position gate (1.0pt) + Siriu regression gate as hard red-lines at this checkpoint; never re-seed to mask a move (Pitfall 2 / 07-GATE-AUDIT §3) |
| T-08-10 | Denial of service | level-0 solve on large DXF | mitigate | run-solver-timing-gate.mjs asserts <2000ms per route; breach demotes to walker (D-05 budget bar) |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO new package (munkres already gated+installed in 08-01); only adds a tools/ gate script |
</threat_model>

<verification>
- `node --test parser/__tests__/global-solver-cascade.test.mjs` exits 0.
- `npm run test:gate` exits 0 — single green bar across all four routes.
- The four per-post position gates + Siriu regression gate + four junction oracles are GREEN (hard red-lines).
- graph-walker.js git diff is empty (byte-identical).
- Solver elapsedMs < 2000 for every named route.
</verification>

<success_criteria>
- Solver runs as cascade level-0 with observable, logged demotion (SOLVE-02).
- Walker is a pristine, byte-identical level-1 fallback (strangler-fig).
- D-13 structured fields + warnings channel populate the success result.
- All four route gates re-clear green with zero regression (SOLVE-04, D-06).
- Solver completes within the 2s budget on all named routes.
</success_criteria>

<output>
Create `.planning/phases/08-global-pdf-dxf-solver/08-03-SUMMARY.md` when done (record per-route chosen path + any soft-fence re-baseline).
</output>
