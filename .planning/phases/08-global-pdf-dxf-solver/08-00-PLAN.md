---
phase: 08-global-pdf-dxf-solver
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - parser/dwg/median-crossval.js
  - parser/__tests__/median-crossval.test.mjs
autonomous: true
requirements: [DXF-01, DXF-02, DXF-03, DXF-04, DXF-05, DXF-06, DXF-07]
must_haves:
  truths:
    - "npm run test:gate exits 0 at the Phase-8 start commit (all Phase-6 ingestion gates green)"
    - "A new median cross-validation guard rejects a PDF/DXF scale mismatch with reason 'scale-mismatch'"
    - "The guard returns scale-derived absolute tolerances (span tol, candidate window) from the agreed medians"
  artifacts:
    - path: "parser/dwg/median-crossval.js"
      provides: "medianCrossValidate() pre-solve scale/unit guard (D-08)"
      exports: ["medianCrossValidate"]
      min_lines: 30
    - path: "parser/__tests__/median-crossval.test.mjs"
      provides: "Unit tests for agreement-pass, scale-mismatch-reject, tolerance derivation"
  key_links:
    - from: "parser/dwg/median-crossval.js"
      to: "parser/geo/utm-calibrator.js"
      via: "planar span math reuses in-house geometry (no new geo dep)"
      pattern: "median"
---

<objective>
Wave 0 (D-09 prerequisite). Phase 6 DXF ingestion is ALREADY fully shipped and green
(06-01/02/03 SUMMARYs confirm `addRegion` envelope/unit validation, `noRegionError`,
the Web Worker parse, and the Palhoça ≤5000ms ingest-timing gate wired into
`npm run test:gate`). This plan therefore does NOT re-build Phase 6 — it (a) ATTESTS
the ingestion gate suite is green at the Phase-8 start commit so the DXF-01..07
requirement IDs re-clear under the Phase-8 namespace, and (b) builds the one genuinely
new pre-solve guard: the D-08 PDF-median vs DXF-median cross-validation that doubles as
a unit/zone-mismatch sanity check before any solver runs.

Purpose: Lock the LOCKED D-09 sequencing contract — the solver never executes against
un-normalized or scale-mismatched DXF inputs. The median guard is the Wave-0→Wave-1 seam
and the source of all scale-derived tolerances the solver consumes downstream.
Output: `parser/dwg/median-crossval.js` (pure function + unit tests); a recorded green
`npm run test:gate` attestation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-global-pdf-dxf-solver/08-CONTEXT.md
@.planning/phases/08-global-pdf-dxf-solver/08-RESEARCH.md
@.planning/phases/06-dxf-ingestion-region-lookup/06-03-SUMMARY.md

<interfaces>
<!-- Geometry helpers the median guard reuses (no new deps). From parser/geo/utm-calibrator.js -->
export function haversineMeters(lat1, lon1, lat2, lon2): number;   // line 770
<!-- DXF cable edges are UTM-planar; spans use plain Math.hypot on (x,y), NOT haversine
     (RESEARCH "Integration gotcha: planar vs haversine"). -->

<!-- Call-site shapes already assembled at coordinate-calculator-dwg.js (do NOT change these here) -->
distances: Array<{ from:number, to:number, meters:number|null, source?:string }>   // printed inter-post labels
regionEdges (croppedRegion.cableEdges): Array<{ a:{x,y}, b:{x,y}, ... }>            // DXF cable polyline edges, UTM
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest Phase-6 ingestion gate suite is green at Phase-8 start</name>
  <files>package.json (verify only — not modified)</files>
  <read_first>
    - package.json (the `test:gate` and `test:gate:fixtures` script bodies — confirm `run-dxf-ingest-timing-gate.mjs` is present in the chain)
    - .planning/phases/06-dxf-ingestion-region-lookup/06-03-SUMMARY.md (Palhoça ≤5000ms gate, ~4588ms baseline)
  </read_first>
  <action>
    Run `npm run test:gate` and confirm exit code 0. This re-attests DXF-01..07 (Phase-6
    shipped code) under the Phase-8 namespace per D-09 — it is a VERIFY step, NOT a build
    step. Do NOT modify any Phase-6 file (region-library.js, dxf-parse.worker.js,
    coordinate-calculator-dwg.js ingestion path, addRegion). If the suite is RED, STOP and
    report which gate failed — a RED here means the Phase-8 baseline is not stable and no
    solver work may begin (D-09 LOCKED). Record the green result + the Palhoça ingest
    timing (printed by `run-dxf-ingest-timing-gate.mjs`) in this plan's SUMMARY.
  </action>
  <verify>
    <automated>npm run test:gate</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:gate` exits 0 with no source file modified by this task (git diff is empty for parser/ and tools/ after the run)
    - The run output includes the `run-dxf-ingest-timing-gate.mjs` line and reports Palhoça ingest under 5000 ms
    - SUMMARY records the green attestation and the measured Palhoça ingest time
  </acceptance_criteria>
  <done>npm run test:gate is green at the Phase-8 start commit; DXF-01..07 re-attested; Palhoça timing recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the D-08 median cross-validation guard (PDF vs DXF scale/unit check)</name>
  <files>parser/dwg/median-crossval.js, parser/__tests__/median-crossval.test.mjs</files>
  <read_first>
    - parser/geo/utm-calibrator.js (haversineMeters at line 770 — reuse; confirm planar hypot for spans)
    - parser/dwg/residual-gate.js (style reference: pure module, null-guards skip meters<=0/NaN, median = sorted[floor(n/2)])
    - .planning/phases/08-global-pdf-dxf-solver/08-RESEARCH.md §"Median Cross-Validation Detail (D-08)"
  </read_first>
  <behavior>
    - medianPDF = median of `distances[].meters` where meters > 0 (skip null/NaN/<=0, matching residual-gate guards)
    - medianDXF = median of DXF cable-span lengths = `Math.hypot(edge.b.x-edge.a.x, edge.b.y-edge.a.y)` over regionEdges (planar UTM, NOT haversine)
    - Agreement: pass when `1/F <= medianPDF/medianDXF <= F`, F = 2 (Claude discretion per A3; export F as a named const for later calibration)
    - Disagreement (e.g. mm-scale DXF inflating medianDXF ~1000x, or wrong zone) → returns `{ ok:false, reason:"scale-mismatch", medianPDF, medianDXF, ratio }`
    - Pass → returns `{ ok:true, medianPDF, medianDXF, ratio, tolerances:{ spanTolM, candidateWindowM } }` where spanTolM ≈ SPAN_TOL_FRAC(0.15)·medianPDF and candidateWindowM ≈ a multiple of medianPDF (document the multiple as a named const)
    - Empty/insufficient input (no valid PDF distance OR no DXF edge) → `{ ok:false, reason:"insufficient-data" }` (fail loud, never NaN)
  </behavior>
  <action>
    Create `parser/dwg/median-crossval.js` exporting `medianCrossValidate({ distances, regionEdges })`.
    Pure function, no I/O, no new dependency — mirror residual-gate.js module style. Compute both
    medians with sorted-array median (`sorted[Math.floor(n/2)]`), apply the F=2 agreement band, and
    on pass derive absolute tolerances FROM the medians (Pitfall-9: only fractions/factors are
    constants — `SPAN_TOL_FRAC = 0.15`, never an absolute meter literal). Use `Math.hypot` for DXF
    spans (planar UTM) and the meters>0 guard for PDF distances. Write
    `parser/__tests__/median-crossval.test.mjs` covering: (a) agreeing medians → ok:true with
    derived tolerances, (b) DXF spans ×1000 (mm-scale) → reason:"scale-mismatch", (c) empty DXF
    edges → reason:"insufficient-data". Write the tests FIRST (RED), then the implementation (GREEN).
  </action>
  <verify>
    <automated>node --test parser/__tests__/median-crossval.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `parser/dwg/median-crossval.js` contains `export function medianCrossValidate(`
    - The module imports nothing from npm beyond in-tree files (grep -v '^//' the file → no new package import; only relative imports allowed)
    - `node --test parser/__tests__/median-crossval.test.mjs` exits 0 with at least 3 test cases (agreement-pass, scale-mismatch, insufficient-data)
    - A mm-scale fixture (DXF spans ~1000× PDF) produces `reason: "scale-mismatch"` (asserted in the test)
    - On the pass case the returned `tolerances.spanTolM` equals `0.15 * medianPDF` (asserted, no absolute-meter literal)
  </acceptance_criteria>
  <done>medianCrossValidate exists, is pure, rejects scale/unit mismatch loudly, derives tolerances from medians, and its unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested DXF → solver inputs | A mm-scale or wrong-zone DXF that slipped past Phase-6 ingestion could feed silently-wrong spans into the solver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information disclosure (misleading GPS) | median-crossval.js | mitigate | F=2 agreement band rejects PDF/DXF median mismatch with reason "scale-mismatch" before any solve (D-08 doubles as unit/zone guard) |
| T-08-02 | Denial of service | median-crossval.js | accept | Pure O(n log n) sort over already-cropped edges; no unbounded loop. Low risk, no external input. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NOTHING; the munkres install + its blocking human-verify checkpoint live in plan 08-01. |
</threat_model>

<verification>
- `npm run test:gate` exits 0 (Phase-6 ingestion re-attested; DXF-01..07 green).
- `node --test parser/__tests__/median-crossval.test.mjs` exits 0.
- No Phase-6 source file modified.
</verification>

<success_criteria>
- DXF-01..07 re-attested green under the Phase-8 namespace (no Phase-6 rebuild).
- The D-08 median guard exists, is pure, and loudly rejects scale/unit mismatch.
- All solver-consumable tolerances are scale-derived (no absolute-meter constants).
</success_criteria>

<output>
Create `.planning/phases/08-global-pdf-dxf-solver/08-00-SUMMARY.md` when done.
</output>
