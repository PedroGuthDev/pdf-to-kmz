---
phase: 08-global-pdf-dxf-solver
plan: 01
type: execute
wave: 2
depends_on: [08-00]
files_modified:
  - package.json
  - package-lock.json
  - parser/dwg/global-solver.js
  - parser/__tests__/global-solver.test.mjs
autonomous: false
requirements: [SOLVE-01]
user_setup:
  - service: munkres (npm)
    why: "Hungarian / Munkres optimal bipartite assignment over rectangular cost matrices — the one pre-authorized new dependency (SOLVE-01)"
    env_vars: []
    dashboard_config:
      - task: "Confirm package name 'munkres' (havelessbemore), NOT 'munkres-js' (addaleax), and version 2.0.3 vs maintained 2.1.1"
        location: "npmjs.com/package/munkres"
must_haves:
  truths:
    - "The munkres package install is gated behind a blocking human-verify checkpoint (name discrepancy: CONTEXT said munkres-js, real package is munkres)"
    - "solveGlobalGraphAlignment() pins post 1 to the nearest DXF INSERT, prunes each post to k<=30 candidates via rbush, builds the D-02 cost matrix, runs munkres, and maps assignments to coords"
    - "The solver returns the walker's exact coords shape { postNumber, lat, lon, source:'dwg', dwg_block } and never mutates its inputs"
    - "A run exceeding 2000ms is reported (timing captured for the D-05 budget bar)"
  artifacts:
    - path: "parser/dwg/global-solver.js"
      provides: "solveGlobalGraphAlignment(): anchor, prune, cost, munkres, coords mapping, timer"
      exports: ["solveGlobalGraphAlignment"]
      min_lines: 80
    - path: "parser/__tests__/global-solver.test.mjs"
      provides: "Unit tests on synthetic small graph: known-assignment recovery, rectangular handling, no-anchor demote, input-immutability, timing field"
    - path: "package.json"
      provides: "munkres dependency pinned"
      contains: "munkres"
  key_links:
    - from: "parser/dwg/global-solver.js"
      to: "munkres"
      via: "import { munkres } from 'munkres'"
      pattern: "munkres"
    - from: "parser/dwg/global-solver.js"
      to: "parser/dwg/median-crossval.js"
      via: "scale-derived tolerances feed the candidate window + cost"
      pattern: "medianCrossValidate"
    - from: "parser/dwg/global-solver.js"
      to: "parser/geo/utm-calibrator.js"
      via: "latLonToUtm anchor + utmToLatLon coords output"
      pattern: "utmToLatLon"
---

<objective>
Install the `munkres` Hungarian package (behind a BLOCKING human-verify checkpoint due to the
package-name discrepancy) and build the core of `parser/dwg/global-solver.js`: anchor post 1 to
the nearest DXF INSERT (D-07), prune each post's candidates to k≤30 nearest DXF nodes via rbush
(D-03), build the D-02 combined cost matrix (geometric position residual + edge-span fit), run
`munkres()` for the global assignment (SOLVE-01), map assignments to the walker's coords shape, and
capture wall-clock for the 2s budget. This wave does NOT yet add the topology gate or the cascade
wiring — it returns `{ ok, coords[] }` (or a demotion `{ ok:false, reason }`) and is validated on a
synthetic small graph.

Purpose: Stand up the Hungarian engine as a pure additive module so later waves can bolt the
topology gate (Wave 2) and cascade demotion (Wave 3) on top. Pristine-input discipline (never
mutate posts/distances/regionPosts) is established here so the walker gets a clean fallback run.
Output: `munkres` in package.json; `parser/dwg/global-solver.js` core; synthetic unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-global-pdf-dxf-solver/08-CONTEXT.md
@.planning/phases/08-global-pdf-dxf-solver/08-RESEARCH.md

<interfaces>
<!-- munkres API (VERIFIED: github.com/havelessbemore/munkres). costMatrix[y][x] = cost(row y → col x). -->
import { munkres } from "munkres";
// munkres(number[][]): Array<[y, x]>  — MINIMIZES by default; rectangular native; result length = min(rows,cols)
// Infinity = forbid (last-resort only); -Infinity = force. Prefer LARGE-FINITE sentinel for non-candidates.

<!-- Geometry (parser/geo/utm-calibrator.js) -->
export function latLonToUtm(lat_deg, lon_deg): { easting, northing, zone };   // line 25
export function utmToLatLon(easting, northing, zone): { lat, lon };           // line 62
export function haversineMeters(lat1, lon1, lat2, lon2): number;              // line 770 (final output / anchor sub-score only)

<!-- rbush (already in deps) + region-pairing helpers (parser/dwg/region-pairing.js) -->
export const DEFAULT_TOLERANCE_M = 15;                       // line 4 (anchor search tol seed)
export function buildPostIndex(posts): RBushTree;            // line 20 — rbush over DXF nodes {x,y,block}
// tree.search({minX,minY,maxX,maxY}) → candidate DXF nodes for the k<=30 prune

<!-- Scale guard (built in 08-00) -->
import { medianCrossValidate } from "./median-crossval.js";  // tolerances.spanTolM, tolerances.candidateWindowM

<!-- Solver inputs (already assembled at coordinate-calculator-dwg.js lines 313-393) -->
posts:     Array<{ number, x, y, lat?, lon?, page }>     // PDF route posts, sorted by number
distances: Array<{ from, to, meters, source }>           // printed inter-post labels
regionPosts (croppedRegion.posts): Array<{ x, y, block }>  // DXF nodes, UTM
regionEdges (croppedRegion.cableEdges): cable polyline edges, UTM
startLat/startLon: anchor (lat1/lon1)
zoneExpected: regionData.crs.zone (default 22)

<!-- Output coords entry MUST match the walker (graph-walker.js lines 1165-1171): -->
{ postNumber, lat, lon, source: "dwg", dwg_block }   // lat/lon via utmToLatLon(node.x, node.y, zoneExpected)
</interfaces>

<warning>
Printed distances are UTM-planar. Use PLANAR Euclidean (Math.hypot on x,y) for span/residual terms
inside the cost matrix; use haversine ONLY for the final lat/lon and the residual-gate anchor
sub-score (RESEARCH Integration gotcha — mixing introduces ~cos(lat) bias). Apply ±5% label-rounding
tolerance in the distance-match cost term (labels are 0.1m precision).
</warning>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Confirm munkres package name + version before install</name>
  <files>package.json (install commits here only AFTER approval, in Task 2)</files>
  <action>BLOCKING human-verify gate before any npm install. Present the package-name discrepancy (CONTEXT D-01 says "munkres-js@2.0.3"; the real package is "munkres" by havelessbemore — v2.0.3 exists there, NOT on munkres-js which tops out at 1.2.2). Do NOT run npm install until the developer replies with an explicit approved package+version string. Record the approval in the SUMMARY. The actual install happens in Task 2 using the approved string.</action>
  <what-built>
    Nothing yet — this is a pre-install gate. CONTEXT D-01 locked the dependency as
    "munkres-js@2.0.3", but RESEARCH verified that `munkres-js` (addaleax) tops out at v1.2.2 (2017,
    no rectangular/sentinel API) and that version 2.0.3 exists ONLY on the differently-named
    `munkres` package (havelessbemore) — ESM, TypeScript types, rectangular matrices, Infinity
    sentinels, MIT, no postinstall. This is the [ASSUMED]-tagged supply-chain item (slopcheck was
    unavailable this environment), so install is human-gated per the package legitimacy protocol.
  </what-built>
  <how-to-verify>
    1. Open https://npmjs.com/package/munkres — confirm it is `munkres` (havelessbemore),
       repo github.com/havelessbemore/munkres, MIT license, version 2.0.3 present in the version list.
    2. Confirm this is the package you intend (Hungarian assignment, rectangular cost matrices),
       NOT the stale `munkres-js` (addaleax) at https://npmjs.com/package/munkres-js.
    3. Decide the version: CONTEXT-pinned `2.0.3` (reproducibility) or maintained `2.1.1` (same API).
    Reply with the exact `npm install` argument to run (e.g. "approved: munkres@2.0.3").
  </how-to-verify>
  <acceptance_criteria>
    - Developer explicitly approves the package name `munkres` (not `munkres-js`) and a version
    - The approved package+version string is recorded in this plan's SUMMARY
  </acceptance_criteria>
  <verify><human-check>Developer approved the exact package name + version; recorded in SUMMARY before install.</human-check></verify>
  <done>Developer has explicitly approved package name "munkres" and a version; no install has run yet.</done>
  <resume-signal>Reply "approved: munkres@&lt;version&gt;" or describe a different choice. Do NOT run npm install before this approval.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install munkres + build the global-solver core (anchor, prune, cost, assign)</name>
  <files>package.json, package-lock.json, parser/dwg/global-solver.js, parser/__tests__/global-solver.test.mjs</files>
  <read_first>
    - parser/dwg/graph-walker.js lines 1047-1174 (anchor step + buildPartialCoords coords push shape — replicate EXACTLY)
    - parser/dwg/region-pairing.js lines 1-60 (DEFAULT_TOLERANCE_M, buildPostIndex, DXF node {x,y,block} shape, span math)
    - parser/geo/utm-calibrator.js lines 25-110 and 770 (latLonToUtm / utmToLatLon / haversineMeters signatures)
    - parser/dwg/median-crossval.js (built in 08-00 — tolerances.spanTolM / candidateWindowM)
    - .planning/phases/08-global-pdf-dxf-solver/08-RESEARCH.md §"munkres API" and §"Architecture Patterns" Pattern 1
  </read_first>
  <behavior>
    - Known-assignment recovery: a synthetic graph where post i sits exactly on DXF node i (identity) → munkres returns the identity assignment → coords map post i to node i's lat/lon.
    - Rectangular handling: |posts| < |candidate nodes| → result length = min(rows,cols); every post gets exactly one assignment; no crash.
    - No-anchor: post 1 has no DXF INSERT within anchor tolerance → returns { ok:false, reason:"no-anchor" } (fail loud, do NOT relax — RESEARCH Open Question 2 recommendation).
    - Scale-mismatch passthrough: medianCrossValidate returns ok:false → solver returns { ok:false, reason:"scale-mismatch" } without running munkres.
    - Input immutability: after the call, the input `posts`, `distances`, and `regionPosts` arrays/objects are deep-equal to before (no mutation — Pitfall-2 pristine-fallback guarantee).
    - Timing: the returned object carries `elapsedMs` (wall-clock of the solve) so Wave 3's 2s budget bar can read it.
    - Coords shape: each coord is exactly { postNumber, lat, lon, source:"dwg", dwg_block } with lat/lon from utmToLatLon(node.x, node.y, zoneExpected).
  </behavior>
  <action>
    First run the developer-approved install (e.g. `npm install munkres@2.0.3`) so it commits to
    package.json + package-lock.json; confirm `node_modules/munkres` exists and has no postinstall.
    Then create `parser/dwg/global-solver.js` exporting
    `solveGlobalGraphAlignment({ posts, distances, connections, startLat, startLon, regionData, regionPosts, regionEdges, postIndex, adjacencyGraph, gpsByPostNumber })`
    — the SAME destructured params runDwgPairingCascade already passes (Wave 3 will wire it).
    Steps, in order:
      1. Call `medianCrossValidate({ distances, regionEdges })`; on ok:false return `{ ok:false, reason }`.
      2. Anchor (D-07): `latLonToUtm(startLat, startLon)`, rbush-search regionPosts within DEFAULT_TOLERANCE_M; pick nearest; none → `{ ok:false, reason:"no-anchor" }`. Force the anchor row's chosen cell with `-Infinity` so munkres pins post 1 to its INSERT.
      3. Per-post candidate prune (D-03): propagate an anchor-relative predicted UTM position per post (use cumulative printed distances along `connections` from the anchor), rbush-query k≤30 nearest DXF nodes within `tolerances.candidateWindowM`; warn (push to a local warnings array) when an unpruned set exceeds 30.
      4. Cost matrix (D-02): rows = posts (number order), cols = the union of pruned candidate nodes. cost = `w_pos * posResidual + w_span * spanFit` where posResidual = planar hypot(predictedUTM, nodeUTM) and spanFit = sum over the post's incident printed distances of |printedMeters − planarSpanToAssignedNeighborCandidate| with ±5% rounding tolerance. Start `w_pos = w_span = 1` (A4 discretion; tune later so Siriu stays "trust"). Non-candidate cells get a LARGE-FINITE sentinel (`10 * maxRealCost`), never Infinity (RESEARCH anti-pattern).
      5. `munkres(costMatrix)` → assignments `[[y,x],...]`. Detect any unassigned post (result length < posts.length, or an assigned cell at sentinel cost) → `{ ok:false, reason:"coverage" }` (D-12 all-or-nothing).
      6. Map assignments → coords via `utmToLatLon(node.x, node.y, zoneExpected)` into the EXACT walker shape.
      7. Wrap the solve in `performance.now()` (or Date.now) start/end; attach `elapsedMs`.
    Return `{ ok:true, coords, elapsedMs, partialCoords, warnings }`. Do NOT mutate any input.
    Do NOT add the topology gate or residual accept bar yet (Wave 2). Do NOT touch
    coordinate-calculator-dwg.js or graph-walker.js (Wave 3 + never, respectively).
    Write `parser/__tests__/global-solver.test.mjs` covering the seven behaviors above on small
    synthetic graphs (no real route fixtures needed yet). Tests RED first, then GREEN.
  </action>
  <verify>
    <automated>node --test parser/__tests__/global-solver.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"munkres"` in dependencies; `node_modules/munkres` exists; the installed package has no postinstall script
    - `parser/dwg/global-solver.js` contains `export function solveGlobalGraphAlignment(` and `import { munkres } from "munkres"`
    - global-solver.js contains NO absolute-meter literal as a tolerance (all tolerances come from medianCrossValidate / DEFAULT_TOLERANCE_M / fractions) — verify by grep showing tolerances reference `tolerances.` or `DEFAULT_TOLERANCE_M`, not bare meter numbers
    - `node --test parser/__tests__/global-solver.test.mjs` exits 0 with the identity-recovery, rectangular, no-anchor, scale-mismatch, immutability, and elapsedMs cases all asserted
    - The immutability test deep-clones inputs before the call and asserts deep-equality after (no mutation)
    - `npm run test:gate` still exits 0 (no hard red-line touched — solver is not yet wired into the cascade)
  </acceptance_criteria>
  <done>munkres installed (approved name/version), global-solver core assigns posts→DXF nodes, returns the walker coords shape, never mutates inputs, carries elapsedMs, and synthetic unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | A new external dependency crosses into the build; name discrepancy raises slopsquat risk |
| cost matrix → munkres | Untrusted-scale geometry feeds the assignment; a poisoned matrix yields confident-but-wrong coords |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | npm install munkres | mitigate | BLOCKING human-verify checkpoint (Task 1) confirms `munkres` (not `munkres-js`) via npmjs.com/package; pin exact version; verified no postinstall (RESEARCH audit) |
| T-08-03 | Information disclosure (confident-but-wrong) | cost matrix / munkres | mitigate | D-07 anchor force (-Infinity), large-finite sentinels (not Infinity-everywhere), coverage-fail demotion; full confident-but-wrong guard completed by the Wave-2 accept bar |
| T-08-04 | Denial of service | candidate prune | mitigate | rbush k≤30 prune (D-03) + elapsedMs captured for the Wave-3 2s budget demotion; warn when unpruned set >30 |
</threat_model>

<verification>
- `node --test parser/__tests__/global-solver.test.mjs` exits 0.
- `npm run test:gate` exits 0 (solver not yet wired; hard red-lines untouched).
- `node_modules/munkres` present, no postinstall, version matches developer approval.
</verification>

<success_criteria>
- munkres installed under the approved name/version (blocking checkpoint honored).
- solveGlobalGraphAlignment assigns posts→DXF nodes globally via munkres (SOLVE-01).
- Output matches the walker coords shape; inputs are never mutated; elapsedMs captured.
- All tolerances are scale-derived (no absolute-meter constants).
</success_criteria>

<output>
Create `.planning/phases/08-global-pdf-dxf-solver/08-01-SUMMARY.md` when done (record the approved munkres name+version).
</output>
