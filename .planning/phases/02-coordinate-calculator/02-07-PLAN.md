---
phase: 02-coordinate-calculator
plan: 07
type: execute
wave: 1
depends_on: ["02-06"]
files_modified:
  - parser/geo/label-lsq-calibrator.js
  - parser/coordinate-calculator.js
autonomous: true
requirements: [COORD-03]
must_haves:
  truths:
    - "Posts 9, 10, 11 on João Born page 3 each measure < 10m vs reference"
    - "Valmor G-1: 11/11 posts < 5m and max < 5m (no regression)"
    - "João Born posts 15-34: at least 22/34 posts < 5m (session-7 gains preserved)"
    - "parser/__tests__/coordinate-calculator.test.mjs: 20/20 passing"
  artifacts:
    - path: "parser/geo/label-lsq-calibrator.js"
      provides: "refineAnchorPageByDownstreamChord (fixed) + refineAnchorPageBySplitRegion (new export)"
      contains: "export function refineAnchorPageBySplitRegion"
    - path: "parser/coordinate-calculator.js"
      provides: "Split-region call wired after existing anchor refit block"
      contains: "refineAnchorPageBySplitRegion"
  key_links:
    - from: "parser/coordinate-calculator.js"
      to: "refineAnchorPageByDownstreamChord"
      via: "import + call at lines 1349-1375 (multiSheetRoute gated)"
      pattern: "refineAnchorPageByDownstreamChord\\("
    - from: "parser/coordinate-calculator.js"
      to: "refineAnchorPageBySplitRegion"
      via: "import + call immediately after existing refit block, same multiSheetRoute gate"
      pattern: "refineAnchorPageBySplitRegion\\("
---

<objective>
Drop posts 9, 10, and 11 on João Born page 3 from current 15–19m errors to under 10m, without regressing Valmor G-1 (11/11 < 5m, max < 5m) or João Born posts 15–34 (session-7 gains: ≥22/34 < 5m).

Purpose: Posts 9/10/11 are the only remaining sub-10m blockers for João Born. Field crews require ≤10m locating accuracy (D-P911-01). Two compounding problems block this target:
1. `refineAnchorPageByDownstreamChord` emits ZERO `[anchor-refit]` log lines on João Born — it is failing one of 8+ silent `return false` guards. The page-3 scale/theta correction (~-4%, -3.7°) is never applied, leaving a ~7m gap between current performance and the Procrustes optimum for posts 10/11.
2. Even at the Procrustes optimum, post 9's global-transform floor is 12.34m — only a local (split-region) transform can push it below 10m.

Output: Two layered fixes — (a) the silent failure in `refineAnchorPageByDownstreamChord` is diagnosed and fixed (posts 10/11 reach ~8m), then (b) a new `refineAnchorPageBySplitRegion` applies separate 2-point similarity fits to two sub-regions of the anchor page (post 9 reaches < 10m).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coordinate-calculator/02-POSTS9-11-CONTEXT.md
@.planning/phases/02-coordinate-calculator/.continue-here.md
@.planning/debug/joao-born-coords-off.md

<interfaces>
<!-- Key contracts extracted from the codebase. Executor uses these directly. -->

From parser/geo/utm-calibrator.js:
```javascript
export function latLonToUtm(lat_deg, lon_deg);   // → { easting, northing, zone }
export function utmToLatLon(easting, northing, zone);  // → { lat, lon }
export function projectPost(px, py, pageTransform);  // → { lat, lon }
```

From parser/geo/label-lsq-calibrator.js (already in working tree):
```javascript
// Top of file imports — DO NOT remove existing imports:
import { latLonToUtm, rotatePdfPoint, utmFromPdfPoint } from './utm-calibrator.js';

// Exported (existing):
export function refineAnchorPageByDownstreamChord(transforms, sortedPosts, distMap, post1Gps, warnings);  // → boolean
export function labelDistanceRmse(transforms, sortedPosts, distMap);  // → number|null
export function augmentCrossPageDistances(sorted, distMap);  // → { map, filled }
export function refinePageOriginsByLabelLsq(...);  // existing — DO NOT touch
```

Transform shape (from utm-calibrator.js):
```javascript
{ origin_e, origin_n, x_scale_sf, y_scale_sf, theta, affine, zone }
```

Silent return-false guards inside refineAnchorPageByDownstreamChord (label-lsq-calibrator.js lines 426–513) currently emitting NO log:
- line 434: `sorted.length < 4`
- line 438: `anchorPage == null || !transforms.has(anchorPage)`
- line 440: `tAnchor.affine`
- line 443: `anchorPagePosts.length < 4`
- line 450-457: `!firstDownstream || firstDownstream.pageNum === anchorPage || firstDownstream.lat == null || firstDownstream.lon == null`
- line 459: `labelKtoK1 == null || labelKtoK1 <= 0`
- line 470: `chordLen < labelKtoK1`
- line 484: `det < 1`
- line 495-501: `!Number.isFinite(newScale) || !Number.isFinite(newTheta) || newScale <= 0`

Outer gate in parser/coordinate-calculator.js lines 1349-1354:
```javascript
if (
  multiSheetRoute &&
  pageTransforms.size > 0 &&
  sorted[0]?.lat != null &&
  augDistMapForSeams?.size      // ← can be empty if line 993 didn't run
) { ... refineAnchorPageByDownstreamChord(...) ... }
```

2-point similarity fit formula (reuse exactly as in refineAnchorPageByDownstreamChord lines 481-490):
```
dx = postB.x - postA.x
dy = postB.y - postA.y
det = dx*dx + dy*dy             // require det >= 1
dE  = eB_target - eA_target
dN  = nB_target - nA_target
u   = (dx*dE - dy*dN) / det
v   = (dy*dE + dx*dN) / det
newScale = hypot(u, v)
newTheta = atan2(v, u)
// origin pinned so postA projects exactly to (eA_target, nA_target):
c = cos(newTheta); s = sin(newTheta)
rxA = c*postA.x + s*postA.y
ryA = -s*postA.x + c*postA.y
newOriginE = eA_target - rxA * newScale
newOriginN = nA_target + ryA * newScale
```
</interfaces>

<harness_invariants>
Reference targets (D-P911-03 — non-negotiable, verify after every change):
- Valmor G-1: 11/11 < 5m, max < 5m
- João Born: ≥22/34 < 5m (posts 15-34 session-7 gains)
- João Born posts 9, 10, 11: each < 10m (the goal)
- parser/__tests__/coordinate-calculator.test.mjs: 20/20 passing

Run harness (Windows PowerShell):
  node debug-run-calc.mjs
Run tests:
  node parser/__tests__/coordinate-calculator.test.mjs
</harness_invariants>

</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 1): Instrument every silent return-false guard in refineAnchorPageByDownstreamChord and the outer call gate</name>
  <files>
    parser/geo/label-lsq-calibrator.js
    parser/coordinate-calculator.js
  </files>

  <read_first>
    - parser/geo/label-lsq-calibrator.js lines 1-50 (imports + helpers)
    - parser/geo/label-lsq-calibrator.js lines 420-560 (full refineAnchorPageByDownstreamChord function)
    - parser/geo/label-lsq-calibrator.js lines 560-590 (labelDistanceRmse)
    - parser/coordinate-calculator.js lines 1-48 (imports)
    - parser/coordinate-calculator.js lines 810-830 (augDistMapForSeams initialization on line 817)
    - parser/coordinate-calculator.js lines 985-1000 (the conditional block that updates augDistMapForSeams on line 993)
    - parser/coordinate-calculator.js lines 1340-1375 (outer call gate + reproject block)
    - .planning/debug/joao-born-coords-off.md (full file — pay attention to session_7_remaining_work and session_5_finding for the post 9/10/11 Procrustes floors)
    - .planning/phases/02-coordinate-calculator/02-POSTS9-11-CONTEXT.md (D-P911-04 through D-P911-06)
  </read_first>

  <action>
    Add diagnostic `warnings.push` calls to EVERY silent `return false` path in `refineAnchorPageByDownstreamChord` (parser/geo/label-lsq-calibrator.js lines 426–513) and to the outer gate in `parser/coordinate-calculator.js` lines 1349–1354. Each diagnostic message uses the prefix `[anchor-refit-diag]` (function body) or `[anchor-refit-gate]` (outer gate). Include the variable values that caused the early return so the harness output uniquely identifies the failing guard.

    Inside `refineAnchorPageByDownstreamChord` (label-lsq-calibrator.js), insert one `warnings.push(...)` immediately before each of the following early returns. Use these exact message strings:

    1. Before `return false;` at line 434 (sorted.length < 4):
       `[anchor-refit-diag] sorted.length ${sorted.length} < 4 — abort`
    2. Before `return false;` at line 438 (anchorPage missing or transform missing):
       `[anchor-refit-diag] post1.pageNum=${anchorPage} transforms.has=${transforms.has(anchorPage)} — abort`
    3. Before `return false;` at line 440 (tAnchor.affine):
       `[anchor-refit-diag] anchor page ${anchorPage} tAnchor.affine=true — abort`
    4. Before `return false;` at line 443 (anchorPagePosts.length < 4):
       `[anchor-refit-diag] anchorPagePosts.length ${anchorPagePosts.length} < 4 on page ${anchorPage} — abort`
    5. Inside the `if (!firstDownstream || ...)` block at lines 450–456, BEFORE the `return false;` at line 456, push a single line that records each sub-condition:
       `[anchor-refit-diag] firstDownstream=${firstDownstream?.number ?? 'null'} pageNum=${firstDownstream?.pageNum ?? 'null'} anchorPage=${anchorPage} lat=${firstDownstream?.lat ?? 'null'} lon=${firstDownstream?.lon ?? 'null'} — abort`
    6. Before `return false;` at line 459 (labelKtoK1 missing or non-positive):
       `[anchor-refit-diag] labelKtoK1 (${lastOnAnchor.number}->${firstDownstream.number})=${labelKtoK1} — abort (distMap.size=${distMap.size})`
    7. Before `return false;` at line 470 (chordLen < labelKtoK1):
       `[anchor-refit-diag] chordLen=${chordLen.toFixed(2)} < labelKtoK1=${labelKtoK1.toFixed(2)} (post1=${post1.number} -> postK1=${firstDownstream.number}) — abort`
    8. Before `return false;` at line 484 (det < 1):
       `[anchor-refit-diag] det=${det.toFixed(4)} < 1 (dx=${dx.toFixed(3)} dy=${dy.toFixed(3)}) — abort`
    9. Inside the `if (!Number.isFinite(newScale) || ...)` block at lines 495–501, BEFORE the `return false;` at line 500:
       `[anchor-refit-diag] newScale=${newScale} newTheta=${newTheta} — abort (non-finite or non-positive scale)`

    In `parser/coordinate-calculator.js`, immediately BEFORE the `if (` at line 1349 (the outer gate), add a diagnostic that logs whether the gate fires AND whether augDistMapForSeams was updated:

    ```
    warnings.push(
      `[anchor-refit-gate] multiSheetRoute=${multiSheetRoute} pageTransforms.size=${pageTransforms.size} sorted[0].lat=${sorted[0]?.lat ?? 'null'} augDistMapForSeams.size=${augDistMapForSeams?.size ?? 0}`
    );
    ```

    Do NOT modify the actual control flow — only add `warnings.push(...)` lines. Do NOT remove or alter the existing success-path log at lines 503-505, 509-511, 544-546, or 550-555. Do NOT change `labelDistanceRmse` or any other function.

    Run `node debug-run-calc.mjs > .planning/debug/anchor-refit-diag.txt` from the working directory and inspect the output. Find the FIRST `[anchor-refit-gate]` line, then the FIRST `[anchor-refit-diag]` line that follows it. The diagnostic message identifies which guard fires on João Born page 3. Record the finding in a brief one-line summary appended to `.planning/debug/joao-born-coords-off.md` under a new `session_8_diagnosis` heading (format: `session_8_diagnosis: 'Guard X fired: <exact diagnostic message>'`).

    Per D-P911-05, the strongest candidates are guards 5 (`firstDownstream.lat == null`) and 6 (`labelKtoK1 == null`). If guard 5 fires, the cross-page label chain did not project the first downstream post. If guard 6 fires, the cross-page label is missing from `augDistMapForSeams`. If the outer `[anchor-refit-gate]` log shows `augDistMapForSeams.size=0`, the gate at line 1353 (the `augDistMapForSeams?.size` check) blocks the call entirely — the line-993 update did not run.
  </action>

  <verify>
    <automated>
      cmd: node debug-run-calc.mjs
      expect: stdout contains exactly one `[anchor-refit-gate]` line for the João Born route AND (if the gate fires) at least one `[anchor-refit-diag]` line identifying the specific guard that returned false. Tests still pass: `node parser/__tests__/coordinate-calculator.test.mjs` exits with code 0 (20/20).
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '\[anchor-refit-diag\]' parser/geo/label-lsq-calibrator.js` returns at least 9 (one push per silent guard).
    - `grep -c '\[anchor-refit-gate\]' parser/coordinate-calculator.js` returns at least 1.
    - Running `node debug-run-calc.mjs` writes at least one of `[anchor-refit-diag]` or `[anchor-refit-gate]` to stdout/warnings for the João Born run.
    - `.planning/debug/joao-born-coords-off.md` contains a new `session_8_diagnosis` block with the exact diagnostic string that identifies the failing guard.
    - Existing success-path log strings remain unchanged: `grep -c '\[anchor-refit\] Page' parser/geo/label-lsq-calibrator.js` is unchanged from baseline (4 matches: 2 guards + 1 revert + 1 success).
    - `node parser/__tests__/coordinate-calculator.test.mjs` exits 0 (20/20 passing) — diagnostics must not change behavior.
    - Valmor harness output: max < 5m, 11/11 < 5m. João Born harness output: max remains 18.97m, ≥22/34 < 5m. (No behavior change — pushes only.)
  </acceptance_criteria>

  <done>
    All 9+ silent return-false paths in `refineAnchorPageByDownstreamChord` push a `[anchor-refit-diag]` warning before returning. The outer gate in coordinate-calculator.js pushes a `[anchor-refit-gate]` warning before evaluating the conditional. Harness output for João Born identifies the exact guard that fires. The diagnosis is recorded in joao-born-coords-off.md as `session_8_diagnosis`. Tests remain 20/20 and Valmor/João Born error magnitudes are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2 (Wave 1): Apply the targeted fix for the identified guard so `[anchor-refit] Page 3: refined scale ...` fires and posts 10/11 each measure < 10m</name>
  <files>
    parser/geo/label-lsq-calibrator.js
    parser/coordinate-calculator.js
  </files>

  <read_first>
    - The `session_8_diagnosis` entry just added to `.planning/debug/joao-born-coords-off.md` (identifies the failing guard)
    - parser/geo/label-lsq-calibrator.js lines 420-560 (refineAnchorPageByDownstreamChord with diagnostics in place)
    - parser/coordinate-calculator.js lines 810-830 (where `augDistMapForSeams = distMap` is set)
    - parser/coordinate-calculator.js lines 970-1000 (the conditional block that updates `augDistMapForSeams` on line 993 — note the enclosing `if` that guards whether the update runs)
    - parser/coordinate-calculator.js lines 1340-1375 (outer gate + call + reproject)
    - .planning/phases/02-coordinate-calculator/02-POSTS9-11-CONTEXT.md (D-P911-06 expected outcome)
  </read_first>

  <action>
    Read `session_8_diagnosis` from `.planning/debug/joao-born-coords-off.md`. The diagnostic identifies which silent guard fires. Apply the fix below corresponding to the identified guard. Then remove ALL `[anchor-refit-diag]` and `[anchor-refit-gate]` diagnostic pushes added in Task 1 (they were temporary). Keep the EXISTING success/failure log lines (lines 503-505, 509-511, 544-546, 550-555) intact.

    Fix lookup table by guard ID (from Task 1 numbering):

    - **Guard 5 fires (`firstDownstream.lat/lon == null`)**: The first downstream post lacks a projected GPS. Locate where `sorted[lastIdx + 1]` is computed and ensure that the post's `lat`/`lon` were populated by the upstream label chain. If the post is on a page that was not projected, walk forward through `sorted` from `lastIdx + 1` until a post with non-null `lat`/`lon` is found; use that as `firstDownstream` and sum the label distances from `lastOnAnchor.number` to `firstDownstream.number` (use `distMap` for each consecutive label) as `labelKtoK1`. Guard against missing intermediate labels (revert to `return false` only after pushing `[anchor-refit] Page ${anchorPage}: cannot reach first projected downstream post — skipped.` with a real warning, not a silent return).

    - **Guard 6 fires (`labelKtoK1 == null`)**: The cross-page label from `lastOnAnchor.number` to `firstDownstream.number` is missing from `distMap` (which is `augDistMapForSeams` at the call site). The line-993 update that calls `augmentCrossPageDistances` did not include this label. The fix is in `parser/coordinate-calculator.js`: if the outer-gate `[anchor-refit-gate]` log showed `augDistMapForSeams.size > 0` but the function still hits this guard, call `augmentCrossPageDistances(sorted, augDistMapForSeams)` IMMEDIATELY BEFORE the `refineAnchorPageByDownstreamChord` call at line 1355 (inside the same `if` block) and assign the returned `.map` back to `augDistMapForSeams`. Push `[anchor-refit-prep] augmented N cross-page label(s) before anchor refit.` with the count from `.filled`.

    - **Outer gate fails with `augDistMapForSeams.size=0`**: The line-993 update never ran because its enclosing conditional was false. The fix is in `parser/coordinate-calculator.js`: replace `augDistMapForSeams?.size` on line 1353 with a guard that ALSO accepts the base `distMap` when augmentation didn't happen: change the condition to `(augDistMapForSeams?.size || distMap?.size)`. Additionally, immediately before calling `refineAnchorPageByDownstreamChord`, run `const ensured = augmentCrossPageDistances(sorted, augDistMapForSeams && augDistMapForSeams.size > 0 ? augDistMapForSeams : distMap);` and pass `ensured.map` as the `distMap` argument. Push `[anchor-refit-prep] ensured cross-page labels: augmented ${ensured.filled} label(s) for anchor refit.`.

    - **Any other guard fires**: Document the finding under `session_8_diagnosis` extension and apply the minimal change needed to unblock the function for this specific input shape WITHOUT widening the rotation/scale guards (`MAX_ANCHOR_REFINE_THETA_DEG = 6`, `MAX_ANCHOR_REFINE_SCALE_FRAC = 0.06`) and WITHOUT changing the RMSE tolerance (`ANCHOR_REFINE_RMSE_TOLERANCE_M = 0.5`).

    After applying the fix, run `node debug-run-calc.mjs` and confirm the success line `[anchor-refit] Page 3: refined scale 0.354610->0.<refined>` appears in the João Born portion of the output (the refined scale should be ~0.339-0.342, the refined theta ~-3 to -4°, matching the Procrustes optimum from joao-born-coords-off.md line 444-447). Confirm Valmor output still contains no `[anchor-refit] Page` line (multiSheetRoute=false for 2-sheet Valmor).

    Remove the 9 `[anchor-refit-diag]` pushes from `refineAnchorPageByDownstreamChord` and the `[anchor-refit-gate]` push from coordinate-calculator.js. The success/failure logs at lines 503-505, 509-511, 544-546, 550-555 of label-lsq-calibrator.js remain.

    Append a `session_8_fix:` block to `.planning/debug/joao-born-coords-off.md` documenting (a) which guard was the culprit, (b) the minimal change applied, (c) post-by-post errors for posts 9/10/11 before and after the fix, and (d) confirmation that Valmor and posts 15-34 are unchanged.
  </action>

  <verify>
    <automated>
      cmd: node debug-run-calc.mjs
      expect: stdout for the João Born run contains exactly one line matching the pattern `[anchor-refit] Page 3: refined scale 0\.\d+->0\.\d+` (the success log from label-lsq-calibrator.js line 550-555). For the Valmor run, no `[anchor-refit] Page` line appears. Posts 10 and 11 each report < 10m. Valmor 11/11 < 5m max < 5m. Tests pass: `node parser/__tests__/coordinate-calculator.test.mjs` exits 0.
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '\[anchor-refit-diag\]' parser/geo/label-lsq-calibrator.js` returns 0 (diagnostics removed).
    - `grep -c '\[anchor-refit-gate\]' parser/coordinate-calculator.js` returns 0 (diagnostic removed).
    - `grep -c '\[anchor-refit\] Page' parser/geo/label-lsq-calibrator.js` returns 4 (success + theta-skip + scale-skip + RMSE-revert — original strings preserved).
    - Running `node debug-run-calc.mjs` on João Born produces a line matching `[anchor-refit] Page 3: refined scale 0\.3[0-9]+->0\.3[0-9]+`.
    - João Born harness output: post 10 error < 10.0m AND post 11 error < 10.0m (per D-P911-06 expected outcome: ~8m).
    - Valmor harness output: max < 5m, 11/11 posts < 5m (no regression).
    - João Born posts 15-34: at least 22 posts < 5m (session-7 invariant).
    - `node parser/__tests__/coordinate-calculator.test.mjs` exits 0 (20/20 passing).
    - `.planning/debug/joao-born-coords-off.md` contains a new `session_8_fix:` block with before/after numbers for posts 9, 10, 11.
  </acceptance_criteria>

  <done>
    The silent failure is identified and fixed. `refineAnchorPageByDownstreamChord` successfully runs for João Born page 3 and emits the `[anchor-refit] Page 3: refined scale ...` log line. Posts 10 and 11 each measure < 10m. Post 9 likely still ≥ 10m (its global-transform floor is 12.34m — Task 3 will address that). Valmor untouched (multiSheetRoute=false). Posts 15-34 invariant preserved. Tests 20/20. All Task-1 diagnostics removed.
  </done>
</task>

<task type="auto">
  <name>Task 3 (Wave 2): Implement refineAnchorPageBySplitRegion in label-lsq-calibrator.js</name>
  <files>
    parser/geo/label-lsq-calibrator.js
  </files>

  <read_first>
    - parser/geo/label-lsq-calibrator.js lines 1-50 (imports — must add `utmToLatLon` if not already present)
    - parser/geo/label-lsq-calibrator.js lines 420-560 (refineAnchorPageByDownstreamChord — the prior-art 2-point similarity fit to mirror)
    - parser/geo/label-lsq-calibrator.js lines 560-590 (labelDistanceRmse — for RMSE guard)
    - parser/geo/utm-calibrator.js lines 60-65 (`utmToLatLon` signature)
    - parser/geo/utm-calibrator.js lines 750-770 (`projectPost` signature for reference)
    - .planning/phases/02-coordinate-calculator/02-POSTS9-11-CONTEXT.md (D-P911-07 through D-P911-12, plus "Claude's Discretion" thresholds)
    - .planning/debug/joao-born-coords-off.md (post 9 Procrustes floor = 12.34m, post 10 = 8.01m, post 11 = 8.88m — split-region must beat these globally)
  </read_first>

  <action>
    Add a NEW exported function `refineAnchorPageBySplitRegion` to `parser/geo/label-lsq-calibrator.js`. The function lives next to (after) `refineAnchorPageByDownstreamChord` and follows the same patterns (named ESM export, mutable `warnings` accumulator, snapshot/trial/revert with `labelDistanceRmse` guard).

    First, update the import at the top of the file from:
    ```
    import { latLonToUtm, rotatePdfPoint, utmFromPdfPoint } from './utm-calibrator.js';
    ```
    to ALSO import `utmToLatLon`:
    ```
    import { latLonToUtm, utmToLatLon, rotatePdfPoint, utmFromPdfPoint } from './utm-calibrator.js';
    ```

    Then add the function with this exact signature and behavior:

    ```
    export function refineAnchorPageBySplitRegion(
      transforms,
      sortedPosts,
      distMap,
      post1Gps,
      warnings,
    ): boolean
    ```

    Behavior (D-P911-07 through D-P911-12):

    1. **Activation guards** (return false silently for shape, push `[split-region]` warning for non-activation):
       - `sorted = [...sortedPosts].sort((a, b) => a.number - b.number)`
       - `post1 = sorted[0]`, `anchorPage = post1.pageNum`
       - If `anchorPage == null` or `!transforms.has(anchorPage)`: push `[split-region] no anchor page transform — skipped.`, return false.
       - `anchorPagePosts = sorted.filter(p => p.pageNum === anchorPage)`
       - If `anchorPagePosts.length < 6` (D-P911-08): push `[split-region] anchor page has ${anchorPagePosts.length} posts (<6) — skipped.`, return false.
       - `tAnchor = transforms.get(anchorPage)`; if `tAnchor.affine`: push `[split-region] anchor page affine — skipped.`, return false.

    2. **Forward-chain GPS estimation from post 1** (D-P911-08, D-P911-09):
       Compute "forward-chain GPS" for each anchor-page post by walking forward from post 1 using `distMap` (label distances) and the corrected page-3 UTM-orientation. For each anchor-page post P at index i (i > 0):
       - Get the label `m = distMap.get(`${anchorPagePosts[i-1].number}->${P.number}`)`. If missing, skip this post (record as "unchained").
       - Derive the UTM bearing from PDF using tAnchor: compute the PDF direction vector (P.x - prev.x, P.y - prev.y), apply `rotatePdfPoint` with `tAnchor.theta`, and take `atan2(rx, -ry)` (compass from grid-North; matches the utm-calibrator sign convention where +y in PDF = south = decreasing northing). Walk: `eForward = prevForwardE + m * sin(bearing)`, `nForward = prevForwardN - m * cos(bearing)`. Seed with post 1: `e0, n0` from `latLonToUtm(post1Gps.lat, post1Gps.lon)`.
       - Store the forward-chain UTM in a per-post map keyed by post.number.

    3. **Per-post residual computation** (D-P911-09):
       For each anchor-page post (excluding post 1), compute `residual = hypot(forwardE - projectedE, forwardN - projectedN)` where projectedE/N come from `utmAtPost(post, tAnchor, tAnchor)` (apply the current transform). The first post has residual 0 by construction. Posts that could not be forward-chained are skipped.

    4. **Activation check at midpoint** (D-P911-08):
       Compute the mid-page post = `anchorPagePosts[Math.floor(anchorPagePosts.length / 2)]`. If its residual is < 8m: push `[split-region] midpoint residual ${mid.toFixed(2)}m < 8m threshold — skipped.`, return false. (This prevents firing on well-calibrated pages like Valmor; D-P911-08 starting threshold = 8m.)

    5. **Break-post K detection** (D-P911-09 + discretion rules):
       Find the post K with maximum residual that satisfies: residual > 2 × median(residuals across anchor-page posts) AND at least 3 posts on each side of K within `anchorPagePosts`. If max residual ≤ 2 × median: push `[split-region] residual spike not detected (max ${max.toFixed(2)}m / median ${med.toFixed(2)}m) — skipped.`, return false. If K's position would leave < 3 posts on one side, shift K by ±1 to meet the min-3 constraint. The break post K MUST satisfy `i_K >= 3 && (anchorPagePosts.length - 1 - i_K) >= 3`. Otherwise: push `[split-region] cannot satisfy ≥3 posts/region (length ${anchorPagePosts.length}) — skipped.`, return false.

    6. **Region 1 anchor points** (D-P911-10):
       - Anchor A: `post1` (PDF: post1.x, post1.y; UTM target: `e0, n0` from step 2 seed).
       - Anchor B: break post K (PDF: K.x, K.y; UTM target: K's forward-chain UTM `forwardE_K, forwardN_K` from step 2).
       - Apply the 2-point similarity formula (the exact formula in the `<interfaces>` block) with postA = post1, postB = K, targets (e0, n0) and (forwardE_K, forwardN_K).
       - Compute `region1Transform = { ...tAnchor, origin_e: newOriginE, origin_n: newOriginN, x_scale_sf: newScale, y_scale_sf: newScale, theta: newTheta }`.

    7. **Region 2 anchor points** (D-P911-10):
       - Last anchor-page post L = `anchorPagePosts[anchorPagePosts.length - 1]`. Forward-chain UTM for L is `forwardE_L, forwardN_L` from step 2.
       - Anchor A: break post K (PDF: K.x, K.y; UTM target: `forwardE_K, forwardN_K` — same as region-1's anchor B so the boundary post is pinned identically on both sides; continuity constraint).
       - Anchor B: last post L (PDF: L.x, L.y; UTM target: `forwardE_L, forwardN_L`).
       - Apply 2-point similarity to compute `region2Transform`.

    8. **Sanity guards on per-region transforms**:
       Reject the split-region attempt if EITHER region produces non-finite scale/theta, scale ≤ 0, theta change > 6° relative to tAnchor.theta, OR scale change > 6% relative to tAnchor.x_scale_sf. Same thresholds as `MAX_ANCHOR_REFINE_THETA_DEG` and `MAX_ANCHOR_REFINE_SCALE_FRAC`. On rejection: push `[split-region] region1/region2 transform exceeded ±6°/±6% guard — skipped.`, return false.

    9. **Apply transforms and update posts** (self-contained — no caller reprojection needed):
       For each anchor-page post P (split by sequence index):
       - If P is in region 1 (index ≤ K_index): apply `region1Transform` via the same math as `utmAtPost` to compute `(e, n)`, then `utmToLatLon(e, n, tAnchor.zone)` to get `(lat, lon)`. Update `P.lat = lat; P.lon = lon;` directly.
       - If P is in region 2 (index > K_index): apply `region2Transform` the same way.
       - For the break post K (boundary), use `region1Transform` — by construction both regions project K to the same UTM point.
       (The function mutates `post.lat`/`post.lon` directly on the anchor-page posts so the caller does not need to reproject — this is the key self-contained design from the planning context.)

    10. **RMSE guard with snapshot-and-revert** (D-P911-11):
        Before steps 6-9, snapshot the current transforms map AND a deep copy of each anchor-page post's lat/lon. Compute `rmseBefore = labelDistanceRmse(transforms, sorted, distMap)`. After applying the split-region updates, compute `rmseAfter = labelDistanceRmse(transforms, sorted, distMap)`. If `rmseAfter > rmseBefore + 0.5` (same ANCHOR_REFINE_RMSE_TOLERANCE_M as the existing function), revert: restore the snapshot transforms and restore each anchor-page post's saved lat/lon. Push `[split-region] label RMSE worsened ${rmseBefore.toFixed(2)}->${rmseAfter.toFixed(2)}m (>0.5m tolerance) — reverted.`, return false.

        Note: because the function writes to `post.lat/post.lon` directly (it does NOT store per-region transforms in the `transforms` Map), `labelDistanceRmse(transforms, sorted, distMap)` after the write will still use the GLOBAL anchor-page transform (unchanged). To make the RMSE guard meaningful, the function instead computes RMSE over the post lat/lon directly:
        - Define a local helper `labelDistanceRmseFromLatLon(sortedPosts, distMap)`: iterate consecutive pairs, convert each post's lat/lon to UTM via `latLonToUtm`, compute the haversine-equivalent UTM chord distance `hypot(dE, dN)`, take the residual against the label, and return the RMSE. Compute before/after using this helper. Revert on regression > 0.5m.

    11. **Success log** (D-P911-11):
        On success, push: `[split-region] Page ${anchorPage}: K=${K.number} (index ${K_index}/${anchorPagePosts.length-1}), region1 scale ${region1Scale.toFixed(4)} θ ${(region1ThetaDeg).toFixed(2)}°, region2 scale ${region2Scale.toFixed(4)} θ ${(region2ThetaDeg).toFixed(2)}°; lat/lon RMSE ${rmseBefore.toFixed(2)}→${rmseAfter.toFixed(2)} m.`
        Return true.

    DO NOT modify `refineAnchorPageByDownstreamChord`, `refinePageOriginsByLabelLsq`, `labelDistanceRmse`, or any other existing export. The new function is purely additive.

    Add JSDoc above the function summarizing the inputs and the return contract (boolean: true = split-region applied successfully; false = guards blocked or revert fired).
  </action>

  <verify>
    <automated>
      cmd: node parser/__tests__/coordinate-calculator.test.mjs
      expect: exit code 0 (20/20 passing). The new function is purely additive — it is NOT wired into coordinate-calculator.js yet, so tests must still pass on their existing inputs.
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'export function refineAnchorPageBySplitRegion' parser/geo/label-lsq-calibrator.js` returns exactly 1.
    - `grep 'utmToLatLon' parser/geo/label-lsq-calibrator.js` returns the import line at the top of the file.
    - `node -e "import('./parser/geo/label-lsq-calibrator.js').then(m => { if (typeof m.refineAnchorPageBySplitRegion !== 'function') { process.exit(1); } else { console.log('export OK'); } })"` prints `export OK` and exits 0.
    - `node parser/__tests__/coordinate-calculator.test.mjs` exits 0 (20/20). The function is not wired yet, so harness results are unchanged from Task 2.
    - The function body contains the literal strings `[split-region]` (for all warning prefixes) — `grep -c '\[split-region\]' parser/geo/label-lsq-calibrator.js` returns at least 6 (one per guard + one success log).
    - The function uses the 2-point similarity formula identical to `refineAnchorPageByDownstreamChord` lines 481-490 (verify by reading the source — `det = dx*dx + dy*dy`, `u = (dx*dE - dy*dN) / det`, `v = (dy*dE + dx*dN) / det`, `newScale = hypot(u, v)`, `newTheta = atan2(v, u)`).
    - The function does NOT write to the `transforms` Map; it writes directly to `post.lat` / `post.lon` on anchor-page posts (`grep 'post.lat = ' parser/geo/label-lsq-calibrator.js | wc -l` returns ≥ 1 inside the new function).
    - `refineAnchorPageByDownstreamChord` is byte-unchanged: `git diff parser/geo/label-lsq-calibrator.js` shows only additive lines for the new function plus the one-line import modification — no edits between line 420 and line 557.
  </acceptance_criteria>

  <done>
    `refineAnchorPageBySplitRegion` is exported from `label-lsq-calibrator.js`. It is self-contained (mutates post.lat/post.lon directly), gated by anchor-page post count (≥6), midpoint residual (>8m), and RMSE non-regression (≤+0.5m). It reuses the 2-point similarity formula and the ±6°/±6% sanity guards from `refineAnchorPageByDownstreamChord`. It is not yet wired into `coordinate-calculator.js` — Task 4 wires it. Tests remain 20/20.
  </done>
</task>

<task type="auto">
  <name>Task 4 (Wave 2): Wire refineAnchorPageBySplitRegion into coordinate-calculator.js after the existing anchor refit block</name>
  <files>
    parser/coordinate-calculator.js
  </files>

  <read_first>
    - parser/coordinate-calculator.js lines 30-37 (import block from `./geo/label-lsq-calibrator.js`)
    - parser/coordinate-calculator.js lines 1340-1378 (existing anchor refit block — split-region call goes IMMEDIATELY AFTER line 1375 and BEFORE line 1377 connections-build comment)
    - parser/geo/label-lsq-calibrator.js (the new `refineAnchorPageBySplitRegion` function from Task 3)
    - .planning/phases/02-coordinate-calculator/02-POSTS9-11-CONTEXT.md (D-P911-12 multiSheetRoute gate; D-P911-03 invariants)
  </read_first>

  <action>
    1. Add `refineAnchorPageBySplitRegion` to the import from `./geo/label-lsq-calibrator.js` in `parser/coordinate-calculator.js` lines 32-36. The updated import block:

    ```
    import {
      augmentCrossPageDistances,
      fillAdjacentMissingDistances,
      refineAnchorPageByDownstreamChord,
      refineAnchorPageBySplitRegion,
      refinePageOriginsByLabelLsq,
    } from "./geo/label-lsq-calibrator.js";
    ```

    2. Immediately after the existing anchor refit block's closing `}` on line 1375 and BEFORE the comment `// ── Build connections array` on line 1377, insert a new split-region call block. The block uses the SAME outer guard as the existing block (`multiSheetRoute && pageTransforms.size > 0 && sorted[0]?.lat != null && augDistMapForSeams?.size`) per D-P911-12. The block does NOT reproject anchor-page posts — `refineAnchorPageBySplitRegion` writes lat/lon directly inside its body.

    Insert exactly this block:

    ```
      // ── Split-region calibration (multi-sheet routes only; D-P911-07..12) ─────
      // After the global anchor refit, post 9 on João Born page 3 still sits at its
      // Procrustes floor (~12m). A split-region transform applies separate similarity
      // fits to two sub-regions of the anchor page (post 1..K and K+1..14), where K is
      // the break post detected by per-post residual spike. Self-contained: writes
      // post.lat/post.lon directly, no caller reprojection needed.
      if (
        multiSheetRoute &&
        pageTransforms.size > 0 &&
        sorted[0]?.lat != null &&
        augDistMapForSeams?.size
      ) {
        refineAnchorPageBySplitRegion(
          pageTransforms,
          sorted,
          augDistMapForSeams,
          { lat: startLat, lon: startLon },
          warnings,
        );
      }
    ```

    3. Run the harness: `node debug-run-calc.mjs`. Verify the FOUR invariants from must_haves:
       - João Born posts 9, 10, 11: each error < 10m. (Task 2 already brings 10/11 to ~8m; this task brings post 9 from ~12m to < 10m.)
       - Valmor 11/11 < 5m, max < 5m (multiSheetRoute=false for 2-sheet route — split-region never fires on Valmor).
       - João Born posts 15-34: ≥22/34 < 5m (session-7 invariant).
       - Tests: `node parser/__tests__/coordinate-calculator.test.mjs` exits 0.

    4. If the split-region call reverts (logged as `[split-region] ... reverted.`) without dropping post 9 below 10m, inspect the diagnostic output to determine whether:
       (a) the activation midpoint threshold (8m) is too high — lower to 6m and retry, OR
       (b) the break-post detection picked the wrong K — log K and per-post residuals to verify against the post-9-as-worst expectation, OR
       (c) the 2-point similarity per region is producing a scale/theta that exceeds the ±6%/±6° guard — investigate by logging the computed scale/theta of both regions.
       Adjust ONE hyperparameter at a time (do not widen the safety guards in steps 8 or 10 of Task 3) and re-run the harness. Document the tuning in `session_8_split_region:` in `.planning/debug/joao-born-coords-off.md`.

    5. After all four invariants pass, append a final `session_8_result:` block to `.planning/debug/joao-born-coords-off.md` containing the per-post errors for João Born posts 9, 10, 11, the Valmor max error, the João Born <5m count out of 34, and the test result (20/20).

    Do NOT modify the existing anchor refit block (lines 1349-1375). Do NOT touch any logic outside the new insertion. Do NOT widen the `multiSheetRoute` guard or any other gate.
  </action>

  <verify>
    <automated>
      cmd: node debug-run-calc.mjs && node parser/__tests__/coordinate-calculator.test.mjs
      expect: stdout for the João Born run contains `[anchor-refit] Page 3: refined scale ...` (Task 2 result) AND `[split-region] Page 3: K=...` (Task 4 result). Valmor stdout contains no `[anchor-refit] Page` and no `[split-region] Page` lines (multiSheetRoute=false). João Born posts 9, 10, 11 each report < 10m. Valmor max < 5m, 11/11 < 5m. João Born ≥22/34 < 5m. Test runner exits 0 with 20/20 passing.
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'refineAnchorPageBySplitRegion' parser/coordinate-calculator.js` returns exactly 2 (one in the import, one in the call).
    - The split-region call is inside an `if (multiSheetRoute && pageTransforms.size > 0 && sorted[0]?.lat != null && augDistMapForSeams?.size)` block (verify via `grep -B2 -A8 'refineAnchorPageBySplitRegion(' parser/coordinate-calculator.js`).
    - João Born harness output: post 9 error < 10.0m, post 10 error < 10.0m, post 11 error < 10.0m.
    - Valmor harness output: max error < 5.0m, 11 of 11 posts < 5m.
    - João Born harness output: at least 22 of 34 posts < 5m (session-7 gain preserved).
    - `node parser/__tests__/coordinate-calculator.test.mjs` exits 0 with output containing `20/20` (or equivalent "all tests passing" indicator from the existing harness).
    - Valmor harness output contains NO `[split-region] Page` line (because `multiSheetRoute` is false for 2-sheet routes).
    - João Born harness output contains exactly ONE `[split-region] Page 3:` line — either a success log `[split-region] Page 3: K=... region1 ... region2 ...` or a non-activation/revert log. The success case must be observed at least once during tuning iterations; otherwise post 9 will not drop below 10m.
    - `.planning/debug/joao-born-coords-off.md` contains a `session_8_result:` block listing per-post errors for posts 9, 10, 11 along with Valmor and posts-15-34 confirmation.
  </acceptance_criteria>

  <done>
    `refineAnchorPageBySplitRegion` is wired into the coordinate-calculator pipeline behind the same `multiSheetRoute` gate as the existing anchor refit. On João Born it activates, picks a break post K, and applies per-region similarity transforms, dropping post 9 below 10m. On Valmor the gate blocks the call (2-sheet route) so its 11/11 < 5m invariant is mechanically preserved. Posts 15-34 invariant preserved (split-region only mutates anchor-page posts). Tests 20/20. The 4 non-negotiable invariants in `must_haves.truths` are all true.
  </done>
</task>

</tasks>

<verification>
End-to-end checks (all must pass before marking this plan complete):

1. **Goal achieved**: `node debug-run-calc.mjs` shows João Born posts 9, 10, 11 each with error < 10m.
2. **Valmor non-regression**: `node debug-run-calc.mjs` shows Valmor max < 5m and 11/11 posts < 5m.
3. **João Born 15-34 non-regression**: `node debug-run-calc.mjs` shows at least 22 of 34 João Born posts < 5m (session-7 baseline).
4. **Tests green**: `node parser/__tests__/coordinate-calculator.test.mjs` exits 0 with 20/20 passing.
5. **Diagnostic cleanup**: `grep -c '\[anchor-refit-diag\]' parser/geo/label-lsq-calibrator.js` = 0 and `grep -c '\[anchor-refit-gate\]' parser/coordinate-calculator.js` = 0.
6. **Anchor refit active**: João Born stdout contains `[anchor-refit] Page 3: refined scale`.
7. **Split-region active**: João Born stdout contains `[split-region] Page 3: K=...` success line.
8. **Multi-sheet gate**: Valmor stdout contains neither `[anchor-refit] Page` nor `[split-region] Page` lines.

Run all checks from the working tree root in PowerShell:
```
node debug-run-calc.mjs
node parser/__tests__/coordinate-calculator.test.mjs
```
</verification>

<success_criteria>
- João Born posts 9, 10, 11: each < 10m (D-P911-01 hard requirement).
- Valmor G-1: 11/11 < 5m, max < 5m (D-P911-03 invariant).
- João Born posts 15-34: ≥22/34 < 5m (D-P911-03 session-7 invariant).
- `parser/__tests__/coordinate-calculator.test.mjs`: 20/20 passing.
- `refineAnchorPageByDownstreamChord` emits `[anchor-refit] Page 3: refined scale ...` (no longer silent on João Born).
- `refineAnchorPageBySplitRegion` exported from `label-lsq-calibrator.js` and called from `coordinate-calculator.js` behind the `multiSheetRoute` gate.
- All temporary diagnostic warnings (`[anchor-refit-diag]`, `[anchor-refit-gate]`) removed before final commit.
- `.planning/debug/joao-born-coords-off.md` updated with `session_8_diagnosis`, `session_8_fix`, and `session_8_result` blocks documenting the investigation, the fix, and the per-post outcomes.
</success_criteria>

<output>
Create `.planning/phases/02-coordinate-calculator/02-07-SUMMARY.md` when done. Include:
- The guard that was firing silently (session_8_diagnosis quote)
- The minimal fix applied in Task 2
- Per-post errors for posts 9/10/11 before and after each task
- Confirmation of Valmor + posts-15-34 invariants
- Final test result (20/20)
- Files changed and net line counts
</output>
