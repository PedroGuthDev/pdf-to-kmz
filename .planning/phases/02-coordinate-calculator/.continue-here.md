---
context: phase
phase: 02-coordinate-calculator
task: coordinate-accuracy-fix
status: in_progress
last_updated: 2026-05-18T12:00:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] **CONSTRAINT: Post 01 anchor** — Hybrid transform must keep post 01 within ~5 m of user GPS when `origin_e_pk = e1 - post1.x * scale_x_pk` and viewport offset uses `(box_K.x - rect_pk.x) * page2Scale`, **not** `(box_K.x - x1_p2) * page2Scale`. Mixing models broke anchor (~276 m error).

- [ ] **CONSTRAINT: Do not use isotropic UTM-only scale on detail pages** — Measured ~0.3546 m/pt on pages 3–4 alone is **worse** than hybrid (debug-compare: ~33 m avg vs ~23 m). Keep **Y** from viewport ratio × page-2 scale.

- [ ] **CONSTRAINT: Pages without viewport boxes** — Only pages **3** and **4** have paired Padrão thumbnails on page 2. Posts on other pages (e.g. **08 on page 8**) get `lat/lon: null` unless `repairPostsOnUncalibratedPages` runs (interpolate between neighbors on a calibrated page).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Transform-only tuning | Raising thresholds when GPS is wrong but PDF x,y are wrong | advisory | Fix parser positions first; use `debug-run-calc.mjs` per change |
| Poste text as route numbers | Reading 10-150 (U) digits as post 01–11 | blocking | Route digits: **TEXTO** / **Numero_Poste** OCG only; Poste = graphics + postType |
| Pure UTM isotropic replace | Replacing viewport Y with grid scale only | blocking | Use `detailPageXScale` + `detailPageYScale` in `utm-calibrator.js` |

<current_state>

**Goal:** GPS for all 11 posts within practical accuracy vs `coordenadas postes.txt` when user supplies post 01 coordinates.

**Done this session (resumed Claude `4c5e13d3-…` in Cursor):**

- Node `parsePdf` produces **11/11 posts** without `debug_results.txt` (route-order fallback, descending X)
- Skip Tesseract on Node (pdf.js+canvas rasterizes blank pages — OCR cannot run until render fixed)
- `debug-run-calc.mjs` uses parser positions when ≥11 posts (browser dump comparison only)

**Latest metrics (`node debug-run-calc.mjs`, parser-only):**

| Post | Error vs reference |
|------|-------------------|
| 01 | 0.04 m ✓ |
| 02–04 | ~5–12 m ~ |
| 05 | ~26 m ~ |
| 06 | 4.29 m ✓ |
| 07–09 | ~7–12 m ~ |
| 10–11 | (re-run for full table) |
| **Max** | **26.24 m**, **0/11 null GPS** |

**Tests:** `node parser/__tests__/coordinate-calculator.test.mjs` — 20/20 pass.

</current_state>

<completed_work>

- Diagnosed split: projection model vs parser PDF coordinates
- Implemented hybrid X/Y calibration and fixed origin math
- Post 08 GPS via page-4 interpolation between 07 and 09
- Added `debug-run-calc.mjs` for repeatable comparison

</completed_work>

<remaining_work>

1. **Parser accuracy (main)** — tighten cable/Poste snap; fix Node pdf.js canvas render for browser-parity OCR; posts 05/10–11 still ~8–26 m off
2. **Optional coord tuning** — ALT-1 was best in `debug-compare.mjs`; further Y refinement via distance labels if parser coords improve
3. **Browser UAT** — `index.html` + sample PDF + reference compare UI
4. **Commit** when satisfied (not done yet)

</remaining_work>

<decisions_made>

- Hybrid scale: UTM grid for **X**, viewport ratio for **Y**
- Cross-page origin: viewport box **corner delta** on page 2, anchored at post 01
- Rejected: single isotropic UTM scale per detail page (worse on Palhoça sample)

</decisions_made>

<blockers>

None technical — remaining work is accuracy iteration and optional UAT.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable resume state
2. `coordenadas postes.txt` — ground-truth GPS
3. `debug_results.txt` — last browser/parser dump (post PDF x,y per page)
4. `parser/geo/utm-calibrator.js` — `buildPageTransforms`, `detailPageXScale`, `detailPageYScale`
5. `parser/coordinate-calculator.js` — `repairPostsOnUncalibratedPages`, `calculateCoordinates`
6. `.planning/phases/02-coordinate-calculator/02-CONTEXT.md` — D-REV calibration decisions

## Resume commands

```bash
# Cursor / any agent
# Read .planning/HANDOFF.json then:

node debug-run-calc.mjs

# Claude Code CLI (prior session)
claude --resume 4c5e13d3-7418-4809-b43c-3cd25d75ab54
```

## GSD resume

```
/gsd-resume-work .planning/HANDOFF.json
```
