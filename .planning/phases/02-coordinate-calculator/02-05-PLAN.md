---
phase: 02-coordinate-calculator
plan: 05
type: execute
wave: 3
depends_on: [02-04]
files_modified:
  - parser/coordinate-calculator.js
  - parser/geo/utm-calibrator.js
  - index.html
autonomous: false
requirements: [COORD-01, COORD-02, COORD-03, COORD-04, COORD-05]

must_haves:
  truths:
    - "Each post's PDF (x, y) is snapped to the nearest Cabo_Projetado polyline vertex on its own page within a 30 PDF-pt threshold before any UTM projection runs (D-ACC-01, D-ACC-02, D-ACC-05)."
    - "No two posts ever snap to the same polyline vertex — one-to-one greedy assignment by globally shortest edge prevents collisions at branch junctions (D-ACC-03)."
    - "If no vertex is within the threshold, the OCR centroid is kept and a warning is pushed to warnings[] naming the post number, page, and nearest vertex distance (D-ACC-02 fallback)."
    - "buildPageTransforms() uses a single isotropic per-page UTM scale (X = Y) derived from each detail page's own UTM grid, falling back to viewport ratio × overview scale only when the page has no UTM grid paths (D-ACC-06)."
    - "calculateCoordinates() accepts an optional opts.lastPostGps { lat, lon }; when provided, a 2D similarity transform (translation + rotation + uniform scale) constrained by post-1 and the last post is solved and applied instead of the anchor-and-grid transform (D-ACC-07)."
    - "After GPS is computed, every labeled segment is compared via haversine vs label_meters; a warning is emitted when |delta| > max(5, 0.10 * label_meters). Labels are NEVER fed back into the scale math (D-ACC-08)."
    - "index.html exposes an optional second-anchor textbox (#gpsInputLast) that, when filled, is parsed and passed as opts.lastPostGps. When empty, calculation falls back to single-anchor behavior."
    - "Running `node debug-run-calc.mjs` against the Palhoça sample reports a smaller max-error than the 49.52 m pre-change baseline (target: <5 m on most posts; explicit numeric result recorded in commit message)."
  artifacts:
    - path: "parser/coordinate-calculator.js"
      provides: "snapPostsToPolyline() pre-step, optional 2nd-anchor branch, label sanity-check warnings"
      exports: ["calculateCoordinates", "parseCoordinateInput", "validateBrazilBounds", "detectRouteTopology", "detectGaps", "snapPostsToPolyline"]
      contains: "snapPostsToPolyline"
    - path: "parser/geo/utm-calibrator.js"
      provides: "Isotropic per-page UTM scale (detailPageScale), viewport-ratio fallback retained"
      exports: ["latLonToUtm", "utmToLatLon", "computeScaleFactor", "buildPageTransforms", "projectPost", "haversineMeters", "gpsBearing", "destinationPoint"]
      contains: "detailPageScale"
    - path: "index.html"
      provides: "Optional 2nd-anchor input wired into calculateCoordinates opts.lastPostGps"
      contains: "gpsInputLast"
  key_links:
    - from: "calculateCoordinates()"
      to: "snapPostsToPolyline()"
      via: "first statement after early-return guard, before topology/gap detection"
      pattern: "snapPostsToPolyline\\s*\\("
    - from: "snapPostsToPolyline()"
      to: "cableSegments[i].ops"
      via: "iterate M/L PathOps; ignore/flatten C bezier; group by pageNum"
      pattern: "op\\.type\\s*===\\s*['\"]M['\"]|op\\.type\\s*===\\s*['\"]L['\"]"
    - from: "buildPageTransforms()"
      to: "detailPageScale()"
      via: "single isotropic scale call per page (replaces detailPageXScale + detailPageYScale)"
      pattern: "detailPageScale"
    - from: "calculateCoordinates()"
      to: "opts.lastPostGps"
      via: "6th parameter is the opts object; when opts.lastPostGps present, similarity solver runs"
      pattern: "opts\\.lastPostGps|lastPostGps"
    - from: "index.html"
      to: "calculateCoordinates()"
      via: "opts object built from #gpsInput + optional #gpsInputLast"
      pattern: "gpsInputLast"
---

<objective>
Implement accuracy-fix decisions D-ACC-01 through D-ACC-09 as a single end-to-end vertical slice that takes the Palhoça sample from the current 49.52 m max-error baseline toward the <5 m target.

Three coordinated changes ship together because they only work as a set:
1. `snapPostsToPolyline()` replaces OCR-circle centroids with the nearest `Cabo_Projetado` polyline vertex (the actual pole position).
2. `buildPageTransforms()` collapses the hybrid X/Y scale into a single isotropic per-page UTM-grid scale (only safe once positions are accurate — the .continue-here.md block on isotropic was for the *old* positions).
3. Optional second anchor (last post GPS) lets the user constrain the global affine with two real-world points, post-computation label-vs-haversine sanity warnings expose any residual drift, and a browser UAT confirms the result on the live sample PDF.

Purpose: Source of error per CONTEXT.md diagnosis is parser-reported (x, y), not the transform. Fix the positions, simplify the scale, optionally add a second constraint, surface label warnings — in that order, in one slice.

Output: Updated `coordinate-calculator.js`, simplified `utm-calibrator.js`, wired `index.html`, and a documented improvement in `node debug-run-calc.mjs` max-error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/CLAUDE.md
@C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/ROADMAP.md
@C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/REQUIREMENTS.md
@C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/02-CONTEXT.md
@C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/02-RESEARCH.md
@C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/.continue-here.md
@C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/02-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from current codebase. -->
<!-- Do not re-explore the codebase for these — they are authoritative. -->

From parser/coordinate-calculator.js (currently at HEAD):
- `export function calculateCoordinates(posts, distances, startLat, startLon, cableSegments = [], utmCalibrationData = null)`
  — 6th arg is currently named `utmCalibrationData`. **This task renames it to `opts`** with shape `{ utmGridPathsPerPage, viewportBoxes, pageDimensions, lastPostGps? }`. Existing call sites in `index.html` and `debug-run-calc.mjs` already pass an object literal — extending the shape is backward-compatible.
- `function repairPostsOnUncalibratedPages(posts, calibratedPages, warnings)` — keep as-is; runs AFTER snap.
- `function applyDistanceLabelGpsChain(...)` — keep but ensure it remains a refinement; the post-snap positions already feed the UTM projection.
- The mutable `warnings[]` array is the project's standard accumulator (D-REV-16 patterns).

From parser/geo/utm-calibrator.js (currently at HEAD):
- `function detailPageXScale(pageNum, box_K, pageDim_K, overviewScaleFactor, utmGridPathsPerPage, warnings)` — returns m/pt using page UTM grid if available, else viewport-width-ratio × overview scale.
- `function detailPageYScale(box_K, pageDim_K, overviewScaleFactor)` — returns viewport-height-ratio × overview scale (NO UTM grid path; this is the hybrid asymmetry to eliminate).
- `export function buildPageTransforms(post1, pageDimensions, viewportBoxes, scaleFactor, zone, warnings = [], utmGridPathsPerPage = null)` — current hybrid implementation. Replace with isotropic version per D-ACC-06.
- Transform shape returned in the Map is `{ origin_e, origin_n, x_scale_sf, y_scale_sf, zone }`. After D-ACC-06 the two scale fields become equal (x_scale_sf === y_scale_sf); for backward compatibility keep both keys (avoid touching `projectPost`).

From parser/cable-builder.js (input contract):
- `cableSegments[i] = { id, ops: PathOp[], startPoint, endPoint, pageNum? }`
  — `ops` contains `{ type: 'M', x, y } | { type: 'L', x, y } | { type: 'C', x1,y1,x2,y2,x3,y3 } | { type: 'C2', x1,y1,x2,y2 } | { type: 'Z' }`.
  — `pageNum` is attached by `pdf-parser.js`. Use it directly for same-page grouping.
  — Coordinates are already flipY-applied.

From parser/post-assembler.js (template for one-to-one greedy assignment — apply same pattern):
- `applyPosteHintPositions` builds `candidates = [{ ri, hi, d }]`, sorts by ascending d, then walks the list keeping a `usedResult`/`usedHint` Set guard. Replicate this exactly for the snap step (rename to `usedPost`/`usedVertex`).

From parser/cable-builder.js (vertex iteration reference):
- `minDistancePointToPathOps(px, py, ops)` shows how to walk `ops` and handle `M`, `L`, `C`, `C2`, `Z`. For the snap step we only need vertex *positions*, so collect after each `M`/`L` (and the endpoints of `C`/`C2` if encountered). Bezier interior samples are NOT vertices and must NOT be added to the candidate vertex list.

From index.html (currently at HEAD):
- `#coordForm` section, line ~99–106: contains `#gpsInput` and `#calcBtn`. Insert the optional second-anchor input directly below `#gpsInput` with id `gpsInputLast` and matching placeholder text.
- `calcBtn` handler line ~386 reads `gpsInput.value`, calls `parseCoordinateInput`, then `calculateCoordinates(postsCopy, distances, lat, lon, cableSegments, utmCalibrationData)`. Update to also read `#gpsInputLast`, parse with the same helper, and extend the opts object with `lastPostGps` when valid.

From debug-run-calc.mjs (test harness):
- Imports `calculateCoordinates`, `haversineMeters`, calls `calculateCoordinates(parserPosts, distances, start.lat, start.lon, parsed.cableSegments ?? [], { utmGridPathsPerPage, viewportBoxes, pageDimensions })`.
- After the change, the call must still pass without adding `lastPostGps` (single-anchor path). Add a commented-out example showing how to pass the optional 2nd anchor.
- `REFERENCE` array at line 14–26 is the ground truth. Use post 01 `lat/lon` as `startLat/startLon` and post 11 `lat/lon` as the optional `lastPostGps` when testing the 2nd-anchor branch manually.

Ground-truth GPS file:
- `coordenadas postes.txt` rows 1–11 mirror REFERENCE above. Post 01 = `-27.6594603999238, -48.699240275151034`. Post 11 = `-27.659066208413993, -48.702999429619396`.
</interfaces>

<anti_patterns>
<!-- From .planning/phases/02-coordinate-calculator/.continue-here.md and CONTEXT.md. Do NOT regress these. -->

- Poste-layer text strings are NOT post numbers — only the TEXTO / Numero_Poste OCG carries route digits. The snap step must use post (x, y) from the OCR pipeline as the *query* point; the *target* polyline is `Cabo_Projetado` cableSegments only.
- The previous ban on "pure UTM isotropic scale" applied to the *pre-snap* OCR-centroid positions. With polyline-vertex positions (D-ACC-01), the empirical justification for the hybrid model disappears. D-ACC-06 is valid ONLY because D-ACC-01 ships in the same plan — do not commit the isotropic change without the snap change.
- Do NOT tune transform thresholds (e.g. `POSTE_POSITION_MAX_PT`, viewport-rect filters) to mask position errors. Fix positions first.
- Do NOT feed distance labels into the scale math. Labels are sanity-check only (D-ACC-08); the existing scale comes from UTM grid spacing.
- Do NOT touch `buildCableSegments`, `detectBranches`, `assemblePostsFromOcr`, or `applyPosteHintPositions`. Phase 1's parser contract is unchanged (D-ACC-05).
</anti_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add snapPostsToPolyline() and wire it into calculateCoordinates()</name>
  <files>parser/coordinate-calculator.js</files>
  <read_first>
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/parser/coordinate-calculator.js (current calculateCoordinates flow, warnings[] usage)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/parser/post-assembler.js (lines 185–219, applyPosteHintPositions — template pattern to copy)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/parser/cable-builder.js (lines 30–102, minDistancePointToPathOps — vertex iteration reference)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/02-CONTEXT.md (D-ACC-01 through D-ACC-05, plus `<specifics>` section)
  </read_first>
  <action>
Add a new named export `snapPostsToPolyline(posts, cableSegments, warnings, threshold = 30)` to `parser/coordinate-calculator.js`. Per D-ACC-01, D-ACC-02, D-ACC-03, D-ACC-05:

1. Build a vertex pool grouped by pageNum:
   - For each `cableSegments[i]`, read `seg.pageNum`. For each `op` in `seg.ops`:
     - If `op.type === 'M'` or `op.type === 'L'`: push `{ x: op.x, y: op.y, pageNum, segIdx: i }` into `verticesByPage[pageNum]`.
     - If `op.type === 'C'`: push the endpoint `{ x: op.x3, y: op.y3, pageNum, segIdx: i }` (bezier interior samples are NOT vertices).
     - If `op.type === 'C2'`: push the endpoint `{ x: op.x2, y: op.y2, pageNum, segIdx: i }`.
     - Skip `op.type === 'Z'`.
   - Resulting structure: `Map<number, Array<{ x, y, pageNum, segIdx }>>` keyed by pageNum, with each vertex's index also tracked in a flat list so a single Set can mark used vertices uniquely (the same `(pageNum, vertexIndex)` pair).

2. Build candidate edges (post → vertex distances) using a 30 PDF-pt default threshold:
   - For each post `p` with `p.pageNum` present, iterate vertices in `verticesByPage[p.pageNum] ?? []`.
   - Compute `d = Math.hypot(v.x - p.x, v.y - p.y)`. If `d <= threshold`, push `{ postIdx, vertexId, d }` where `vertexId` is a stable per-page index (use `${pageNum}:${arrayIndex}`).
   - If a post's pageNum is `null`/`undefined`, leave it untouched and continue.

3. Run one-to-one greedy assignment (D-ACC-03), copying the exact pattern in `post-assembler.js:applyPosteHintPositions`:
   - Sort candidates ascending by `d`.
   - Initialise `usedPost = new Set()`, `usedVertex = new Set()`.
   - Walk candidates in order: if `!usedPost.has(postIdx) && !usedVertex.has(vertexId)`, mutate `posts[postIdx].x = v.x; posts[postIdx].y = v.y;` and mark both Sets.

4. Emit warnings (push into the shared `warnings[]` array):
   - For each post that has at least one vertex on its page but the nearest one was outside `threshold`: `warnings.push(\`[coordinate-calculator] snap: post ${num} (page ${pageNum}) kept OCR position — nearest cable vertex was ${dMin.toFixed(2)} pt away (> ${threshold} pt threshold).\`);`
   - For each post whose page has zero polyline vertices: `warnings.push(\`[coordinate-calculator] snap: post ${num} (page ${pageNum}) had no Cabo_Projetado vertices on its page — OCR position retained.\`);`
   - No warning is emitted for the success case.

5. Wire the call into `calculateCoordinates`:
   - Rename the 6th parameter from `utmCalibrationData = null` to `opts = null`.
   - Add `const opts_ = opts || {};` and destructure where needed: `const { utmGridPathsPerPage, viewportBoxes, pageDimensions, lastPostGps } = opts_;`
   - All current property accesses on `utmCalibrationData` change to use the destructured locals (or the `opts_` object).
   - Add `snapPostsToPolyline(sorted, cableSegments, warnings);` as the FIRST mutation after `const sorted = [...posts].sort(...)` and before `const postMap = new Map(...)` so postMap reflects snapped positions. This is the "pre-step at top of calculateCoordinates" per D-ACC-05.

6. Export `snapPostsToPolyline` so unit tests can hit it directly: `export function snapPostsToPolyline(...) { ... }`.

Do NOT modify `assemblePostsFromOcr`, `applyPosteHintPositions`, `buildCableSegments`, or anything in Phase 1. Posts arrive with OCR-derived `(x, y)`; the snap step rewrites those in-place after sort.
  </action>
  <verify>
    <automated>node -e "import('./parser/coordinate-calculator.js').then(m => { if (typeof m.snapPostsToPolyline !== 'function') { console.error('snapPostsToPolyline not exported'); process.exit(1); } const posts = [{ number: 1, x: 100, y: 100, pageNum: 3 }, { number: 2, x: 200, y: 200, pageNum: 3 }]; const cable = [{ ops: [{ type: 'M', x: 105, y: 102 }, { type: 'L', x: 205, y: 198 }], pageNum: 3 }]; const warns = []; m.snapPostsToPolyline(posts, cable, warns); if (posts[0].x !== 105 || posts[0].y !== 102) { console.error('post 1 not snapped:', posts[0]); process.exit(1); } if (posts[1].x !== 205 || posts[1].y !== 198) { console.error('post 2 not snapped:', posts[1]); process.exit(1); } if (warns.length !== 0) { console.error('unexpected warnings on success path:', warns); process.exit(1); } console.log('snap OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
    <automated>node -e "import('./parser/coordinate-calculator.js').then(m => { const posts = [{ number: 1, x: 100, y: 100, pageNum: 3 }, { number: 2, x: 110, y: 100, pageNum: 3 }]; const cable = [{ ops: [{ type: 'M', x: 105, y: 100 }], pageNum: 3 }]; const warns = []; m.snapPostsToPolyline(posts, cable, warns); const snapped = posts.filter(p => p.x === 105 && p.y === 100); if (snapped.length !== 1) { console.error('one-to-one violated:', posts); process.exit(1); } if (warns.length === 0) { console.error('expected fallback warning for unsnapped post'); process.exit(1); } console.log('one-to-one OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `parser/coordinate-calculator.js` exports `snapPostsToPolyline` (named export visible via `import { snapPostsToPolyline } from './parser/coordinate-calculator.js'`).
    - Both `node -e` snippets in `<verify>` exit 0 and print `snap OK` and `one-to-one OK` respectively.
    - `calculateCoordinates`'s 6th parameter is named `opts` (grep `parser/coordinate-calculator.js` for `function calculateCoordinates` shows the new signature `(posts, distances, startLat, startLon, cableSegments = [], opts = null)`).
    - `snapPostsToPolyline(sorted, cableSegments, warnings)` is called inside `calculateCoordinates` before `detectRouteTopology(sorted)` (verified by reading the function body — the snap call appears textually above the topology call).
    - Existing call from `index.html` (`calculateCoordinates(postsCopy, ..., cableSegments, utmCalibrationData)`) still works because `opts` accepts the same object shape — `node debug-run-calc.mjs` does not throw and still prints a comparison table (max error may differ from baseline; this task does not yet constrain the value).
    - `parser/__tests__/coordinate-calculator.test.mjs` continues to pass: `node parser/__tests__/coordinate-calculator.test.mjs` exits 0.
  </acceptance_criteria>
  <done>
`snapPostsToPolyline` is exported, vertex extraction handles M/L plus C/C2 endpoints only (no bezier interior), one-to-one greedy assignment prevents duplicate snaps, fallback warnings name post number, page, and nearest distance, and the function runs as the first mutation inside `calculateCoordinates` so all downstream code (topology, gaps, projection) sees the snapped positions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Collapse hybrid X/Y scale into isotropic per-page UTM scale</name>
  <files>parser/geo/utm-calibrator.js</files>
  <read_first>
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/parser/geo/utm-calibrator.js (lines 208–323, detailPageXScale / detailPageYScale / buildPageTransforms — current hybrid implementation)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/02-CONTEXT.md (D-ACC-06 plus the "superseded decisions" block — hybrid Y is GONE)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/.continue-here.md (the ban on isotropic was for *pre-snap* positions — D-ACC-01 + D-ACC-06 ship together, so the ban no longer applies)
  </read_first>
  <action>
Replace the hybrid X/Y scale in `parser/geo/utm-calibrator.js` with a single isotropic per-page scale (D-ACC-06):

1. Delete `detailPageYScale` entirely. Rename `detailPageXScale` to `detailPageScale` and keep its signature `(pageNum, box_K, pageDim_K, overviewScaleFactor, utmGridPathsPerPage, warnings)`. The body is unchanged — it already prefers the page's own UTM grid via `computeScaleFactor(paths, warnings)` and falls back to `(box_K.w / pageDim_K.w) * overviewScaleFactor`. Rename the X-specific fallback comment to "viewport-width ratio fallback when the page has no UTM grid (D-ACC-06)".

2. In `buildPageTransforms`:
   - Replace the two-call block:
     ```
     const scale_x_pk = detailPageXScale(post1.pageNum, rect_pk, pageDim_pk, scaleFactor, utmGridPathsPerPage, warnings);
     const scale_y_pk = detailPageYScale(rect_pk, pageDim_pk, scaleFactor);
     ```
     with a single call `const scale_pk = detailPageScale(post1.pageNum, rect_pk, pageDim_pk, scaleFactor, utmGridPathsPerPage, warnings);` and use `scale_pk` for both axes when anchoring post #1:
     ```
     const origin_e_pk = e1 - post1.x * scale_pk;
     const origin_n_pk = n1 + post1.y * scale_pk;
     ```
   - Inside the per-viewport loop, replace `detailPageXScale(...)` and `detailPageYScale(...)` with a single `detailPageScale(...)` call producing `scale_K`. Set both `x_scale_sf` and `y_scale_sf` to `scale_K` so `projectPost` (untouched) keeps working.
   - Keep the viewport-corner offset math unchanged: `origin_e = origin_e_pk + (box_K.x - rect_pk.x) * scaleFactor;` and `origin_n = origin_n_pk - (box_K.y - rect_pk.y) * scaleFactor;`.
   - The console.debug line stays; update the printed labels from `x_sf=` / `y_sf=` to `iso_sf=` and print only `scale_K.toFixed(6)`.

3. The exported transform shape MUST remain `{ origin_e, origin_n, x_scale_sf, y_scale_sf, zone }` so `projectPost` and any caller that destructures both keys continues to work; both fields now equal `scale_K`.

4. Update the JSDoc header on `buildPageTransforms` to remove the "Hybrid scales" language and replace with "Isotropic per-page UTM scale: prefer each detail page's own UTM grid; fall back to viewport-width ratio × page-2 overview scale only when the page has no UTM grid paths (D-ACC-06)."

5. Do NOT change `computeScaleFactor`, `latLonToUtm`, `utmToLatLon`, `projectPost`, `haversineMeters`, `gpsBearing`, `destinationPoint`.
  </action>
  <verify>
    <automated>node -e "import('./parser/geo/utm-calibrator.js').then(m => { if (typeof m.buildPageTransforms !== 'function') { console.error('export missing'); process.exit(1); } const post1 = { x: 200, y: 200, pageNum: 3, lat: -27.6594603999238, lon: -48.699240275151034 }; const pageDim = new Map([[3, { w: 800, h: 600 }]]); const boxes = [{ pageNum: 3, rect: { x: 100, y: 100, w: 400, h: 300 } }]; const warns = []; const transforms = m.buildPageTransforms(post1, pageDim, boxes, 0.3528, 22, warns, null); const t = transforms.get(3); if (!t) { console.error('no transform for page 3'); process.exit(1); } if (t.x_scale_sf !== t.y_scale_sf) { console.error('x_scale_sf !== y_scale_sf — not isotropic:', t); process.exit(1); } console.log('isotropic OK x_sf=', t.x_scale_sf, 'y_sf=', t.y_scale_sf); }).catch(e => { console.error(e); process.exit(1); })"</automated>
    <automated>node -e "import('fs').then(fs => { const src = fs.readFileSync('parser/geo/utm-calibrator.js', 'utf8'); if (src.includes('detailPageYScale')) { console.error('detailPageYScale still present — must be deleted'); process.exit(1); } if (!src.includes('detailPageScale')) { console.error('detailPageScale not introduced'); process.exit(1); } console.log('refactor OK'); })"</automated>
    <automated>node parser/__tests__/coordinate-calculator.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `detailPageYScale` no longer appears anywhere in `parser/geo/utm-calibrator.js` (grep returns zero matches).
    - `detailPageScale` is defined and is called exactly twice inside `buildPageTransforms` (once for post #1's page, once per viewport box).
    - The transform Map values still have BOTH `x_scale_sf` and `y_scale_sf` keys, and they are equal (`t.x_scale_sf === t.y_scale_sf`) for every entry — proven by the first `node -e` snippet above.
    - All existing exports (`latLonToUtm`, `utmToLatLon`, `computeScaleFactor`, `buildPageTransforms`, `projectPost`, `haversineMeters`, `gpsBearing`, `destinationPoint`) remain available.
    - `node parser/__tests__/coordinate-calculator.test.mjs` exits 0 (existing 20/20 must still pass).
  </acceptance_criteria>
  <done>
Hybrid X/Y scale removed. Each detail page now uses a single isotropic UTM scale derived from its own UTM grid (with viewport-width-ratio fallback). Transform shape preserved for backward compat. Existing tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Optional 2nd-anchor (similarity transform) in calculateCoordinates</name>
  <files>parser/coordinate-calculator.js</files>
  <behavior>
    - Given `opts.lastPostGps = { lat, lon }` and a successfully-computed pageTransform projection, the function MUST refine all post lat/lon by applying a 2D similarity transform (translation + rotation + uniform scale) so that:
      - Post #1's final (lat, lon) equals the user-provided `startLat`/`startLon` exactly (to within 1e-9 degrees).
      - The last post (highest `number` among all posts) has its final (lat, lon) equal to `opts.lastPostGps.lat`/`opts.lastPostGps.lon` (to within 1e-9 degrees).
    - When `opts.lastPostGps` is omitted, missing required keys, or fails Brazil bounds, the function MUST fall back to the existing anchor-and-grid behavior unchanged AND push a single warning naming why the 2nd anchor was rejected (only when the input was supplied but invalid).
    - The 2D similarity is solved in the local UTM plane (zone from post #1), not in lat/lon directly:
      1. Convert post #1 GPS and last-post target GPS to UTM (easting1, northing1) and (estingN, northingN) — use the zone from post #1.
      2. Convert each post's *projected* (lat, lon) to UTM in the same zone.
      3. Compute the similarity (s, theta, tx, ty) that maps `{projected_utm[0], projected_utm[N-1]}` to `{(e1, n1), (eN, nN)}`.
      4. Apply that similarity to every post's projected UTM, then convert back to lat/lon via `utmToLatLon`.
    - Posts with `lat == null` are skipped during the similarity transform (kept as null).
  </behavior>
  <read_first>
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/parser/coordinate-calculator.js (full file — calculateCoordinates flow, especially the projection loop and the trailing connections-building block)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/parser/geo/utm-calibrator.js (latLonToUtm / utmToLatLon — the only conversions to use)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/02-CONTEXT.md (D-ACC-07 and Claude's Discretion note on closed-form similarity)
  </read_first>
  <action>
1. Write a node-runnable test file `parser/__tests__/two-anchor.test.mjs` (ESM, top-level await OK) that exercises the behavior above:
   - Build 3 synthetic posts (numbers 1, 2, 3) with known projected lat/lon roughly along an east-west line near Palhoça.
   - Manually rotate/scale them by a known amount to simulate "off by 30°, 1.05x" projection drift.
   - Pass `startLat/startLon` of post 1 ground truth + `opts.lastPostGps` of post 3 ground truth.
   - Assert that the returned `posts` array has post 1 and post 3 lat/lon equal to the ground truth within 1e-7 degrees, and post 2 lat/lon "closer" to its ground truth than the unrefined input (use a `haversineMeters` reduction check).
   - Run the test and confirm it fails (the feature does not exist yet).

2. Implement the 2nd-anchor branch inside `calculateCoordinates`:
   - After the existing per-post projection loop (the one that calls `projectPost(post.x, post.y, transform)` and writes `post.lat`/`post.lon`), add an `if (opts_.lastPostGps && typeof opts_.lastPostGps.lat === 'number' && typeof opts_.lastPostGps.lon === 'number')` block.
   - Validate Brazil bounds on `opts_.lastPostGps` using the existing `validateBrazilBounds` helper; if it fails, push a warning `\`[coordinate-calculator] 2nd anchor outside Brazil bounds — using single-anchor projection.\`` and skip the refinement.
   - Otherwise compute the similarity transform:
     - `const post1 = sorted[0];` (post 1 by sort order — guaranteed to be `number === sorted[0].number`; the function uses `sorted = [...posts].sort((a,b) => a.number - b.number)`).
     - `const lastPost = sorted[sorted.length - 1];`
     - Both must have non-null `lat`/`lon` from the projection step; if either is null, push a warning and skip.
     - Compute `const { easting: e1g, northing: n1g, zone } = latLonToUtm(startLat, startLon);` (ground-truth post 1 UTM, used as authoritative zone).
     - `const { easting: eNg, northing: nNg } = latLonToUtm(opts_.lastPostGps.lat, opts_.lastPostGps.lon);`
     - `const { easting: e1p, northing: n1p } = latLonToUtm(post1.lat, post1.lon);` (projected post 1).
     - `const { easting: eNp, northing: nNp } = latLonToUtm(lastPost.lat, lastPost.lon);` (projected last post).
     - Compute the similarity that maps `(e1p, n1p) -> (e1g, n1g)` and `(eNp, nNp) -> (eNg, nNg)`:
       - `const dxP = eNp - e1p, dyP = nNp - n1p;`
       - `const dxG = eNg - e1g, dyG = nNg - n1g;`
       - `const denomP = dxP * dxP + dyP * dyP;`
       - If `denomP < 1e-9`, push a warning `\`[coordinate-calculator] 2nd anchor coincides with post 1 in projection space — similarity undefined.\`` and skip.
       - `const cosScale = (dxP * dxG + dyP * dyG) / denomP;` (this is `s * cos(theta)`)
       - `const sinScale = (dxP * dyG - dyP * dxG) / denomP;` (this is `s * sin(theta)`)
       - Apply to every post: `const { easting: ep, northing: np } = latLonToUtm(p.lat, p.lon);` then `const e = e1g + cosScale * (ep - e1p) - sinScale * (np - n1p);` and `const n = n1g + sinScale * (ep - e1p) + cosScale * (np - n1p);` then `const { lat, lon } = utmToLatLon(e, n, zone); p.lat = lat; p.lon = lon;`.
       - Push a confirmation warning: `\`[coordinate-calculator] 2nd anchor applied — similarity refined: scale=${Math.hypot(cosScale, sinScale).toFixed(5)}, rot=${(Math.atan2(sinScale, cosScale) * 180 / Math.PI).toFixed(2)}°.\``
   - This block runs BEFORE `applyDistanceLabelGpsChain` and BEFORE the connections array is built, so haversine distances in the connections array reflect the refined positions.

3. Re-run the test file; confirm it now passes.

4. Update `debug-run-calc.mjs`: add a commented-out example at the bottom showing `lastPostGps: { lat: REFERENCE[10].lat, lon: REFERENCE[10].lon }` inside the opts argument (as a single-line code comment), so a developer can uncomment to exercise the 2nd-anchor path. Do not enable it by default.
  </action>
  <verify>
    <automated>node parser/__tests__/two-anchor.test.mjs</automated>
    <automated>node parser/__tests__/coordinate-calculator.test.mjs</automated>
    <automated>node debug-run-calc.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `parser/__tests__/two-anchor.test.mjs` exists and exits 0 when run with `node`.
    - The test asserts both endpoints are pinned to within 1e-7 degrees of ground truth AND that the middle post's error decreased after refinement.
    - `parser/__tests__/coordinate-calculator.test.mjs` continues to exit 0 (single-anchor regression coverage).
    - `node debug-run-calc.mjs` exits 0 and prints the comparison table — current invocation does NOT pass `lastPostGps`, so single-anchor behavior must remain unchanged for that script.
    - A commented-out `lastPostGps` example exists in `debug-run-calc.mjs` near the `calculateCoordinates` call.
    - When `opts.lastPostGps` is provided but outside Brazil bounds, the warnings list includes the rejection message (verifiable via a small ad-hoc `node -e` if helpful, but not strictly required as a separate test).
  </acceptance_criteria>
  <done>
calculateCoordinates accepts `opts.lastPostGps`, pins both anchors exactly, refines middle posts by similarity, falls back cleanly when the 2nd anchor is absent or invalid, and is covered by a dedicated test that runs in node.
  </done>
</task>

<task type="auto">
  <name>Task 4: Post-projection label-vs-haversine sanity warnings</name>
  <files>parser/coordinate-calculator.js</files>
  <read_first>
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/parser/coordinate-calculator.js (the connections-building block — labels live in distMap; haversineMeters is already imported)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/02-CONTEXT.md (D-ACC-08, D-ACC-09)
  </read_first>
  <action>
After the connections array is fully built inside `calculateCoordinates` (i.e., after the loop that pushes both branch-junction and forward connections — directly before `return { posts: sorted, connections };`), add a label sanity-check pass:

1. For each `c` in `connections` where `c.gap === false`:
   - Look up the label: `const labelM = distMap.get(\`${c.from}->${c.to}\`);` (the existing `distMap` already covers both directions).
   - Skip if `labelM == null || labelM <= 0`.
   - Find the two posts: `const a = postMap.get(c.from), b = postMap.get(c.to);` (postMap is already built at the top of the function — make sure it reflects post-snap, post-similarity GPS, so build/rebuild it AFTER all GPS mutations or use `sorted` directly).
   - Skip if either post has `lat == null` or `lon == null`.
   - Compute `const hav = haversineMeters(a.lat, a.lon, b.lat, b.lon);`
   - Compute `const tolerance = Math.max(5, 0.10 * labelM);` (D-ACC-08).
   - If `Math.abs(hav - labelM) > tolerance`, push `warnings.push(\`[coordinate-calculator] label sanity-check: segment ${c.from}->${c.to} label=${labelM.toFixed(1)}m vs haversine=${hav.toFixed(1)}m (delta=${(hav - labelM).toFixed(1)}m, tol=${tolerance.toFixed(1)}m).\`);`

2. Do NOT modify `c.meters` based on the label — `meters` continues to flow from the existing logic (D-REV-14 / D-REV-15 / D-ACC-09: label is sanity-check only).

3. Append the new `warnings` to the returned object: change the return statement to `return { posts: sorted, connections, warnings };` — the harness (`debug-run-calc.mjs`) already reads `warnings` from the return (`const { posts, warnings = [] } = calculateCoordinates(...)` on line 104). Verify by grep that no other caller would break.

4. `postMap` is built early (line ~343, `const postMap = new Map(sorted.map(p => [p.number, p]));`). Because `sorted` is mutated in-place by the snap step, projection step, and 2nd-anchor step, the existing `postMap` references point to the same objects — no rebuild needed. Add a `// postMap entries are live refs into sorted; lat/lon are current.` comment to make this explicit.
  </action>
  <verify>
    <automated>node -e "import('./parser/coordinate-calculator.js').then(m => { const posts = [{ number: 1, x: 0, y: 0, pageNum: 3, lat: -27.6594, lon: -48.6992 }, { number: 2, x: 100, y: 0, pageNum: 3, lat: -27.6594, lon: -48.5992 }]; const distances = [{ from: 1, to: 2, meters: 50 }]; const result = m.calculateCoordinates(posts, distances, -27.6594, -48.6992, [], null); if (!Array.isArray(result.warnings)) { console.error('warnings not returned as array'); process.exit(1); } const found = result.warnings.find(w => /label sanity-check/i.test(w)); if (!found) { console.error('expected label sanity warning (50m label vs ~10km haversine):', result.warnings); process.exit(1); } console.log('sanity-check warning OK:', found); }).catch(e => { console.error(e); process.exit(1); })"</automated>
    <automated>node parser/__tests__/coordinate-calculator.test.mjs</automated>
    <automated>node debug-run-calc.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `calculateCoordinates` returns `{ posts, connections, warnings }`. The `warnings` array is the same instance pushed into throughout the function.
    - When a labeled segment's haversine differs from the label by more than `max(5, 0.10 * label)`, a warning string matching `/label sanity-check: segment \d+->\d+ label=.+vs haversine=.+delta=.+tol=.+/` is in the array.
    - When the haversine is within tolerance, no warning is emitted for that segment.
    - `c.meters` values in the connections array are NOT mutated by this task (`debug-run-calc.mjs` output for connection meters is unchanged compared to before this task, modulo position changes from Task 1).
    - `node debug-run-calc.mjs` exits 0 and may print one or more new "label sanity-check" warnings under the "Warnings (first 8)" section depending on actual position accuracy.
  </acceptance_criteria>
  <done>
Label sanity-check runs over labeled, non-gap connections. Warnings are emitted past the `max(5m, 10% of label)` tolerance. Labels remain sanity-only — never fed into scale or meters math. Return shape extended with `warnings`.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire optional 2nd-anchor input into index.html</name>
  <files>index.html</files>
  <read_first>
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/index.html (lines 99–106 for the #coordForm section, lines 386–463 for the calcBtn handler)
    - C:/Users/INFORMAC PAULO LOPES/Documents/Projetos/pdf-to-kmz/.planning/phases/02-coordinate-calculator/02-CONTEXT.md (D-ACC-07 plus the "Anchor input format for 2nd anchor" note in `<specifics>`)
  </read_first>
  <action>
1. In `index.html` inside `<section id="coordForm">` (line ~99), directly below the existing `<input type="text" id="gpsInput" ...>` element and before `<button id="calcBtn" ...>`, insert:
   ```
   <br>
   <label for="gpsInputLast" style="display: inline-block; margin-top: 8px;">Last Post Coordinates (optional, improves accuracy):</label>
   <input type="text" id="gpsInputLast" placeholder="-27.659066, -48.702999" style="width: 300px; padding: 6px;">
   ```
   Use the same styling pattern as the existing input. The label text MUST contain the literal string "Last Post Coordinates (optional, improves accuracy):" so a grep can confirm the field exists.

2. In the `calcBtn` click handler (line ~386):
   - Read the optional value: `const lastInputVal = document.getElementById('gpsInputLast').value;`
   - Parse it with the existing helper: `const lastParsed = lastInputVal && lastInputVal.trim() ? parseCoordinateInput(lastInputVal) : null;`
   - If `lastInputVal` was non-empty but `lastParsed === null`, show a warning in `coordWarning` (`'Last post coordinate format invalid — proceeding with single anchor.'`) and continue without setting `lastPostGps`.
   - When building the opts object passed as the 6th argument, extend it:
     ```
     const opts = {
       utmGridPathsPerPage: currentParseData.utmGridPathsPerPage,
       viewportBoxes: currentParseData.viewportBoxes,
       pageDimensions: currentParseData.pageDimensions,
       ...(lastParsed ? { lastPostGps: lastParsed } : {}),
     };
     ```
   - Update the call to read `opts` instead of `utmCalibrationData`. The variable name `utmCalibrationData` at line ~417 should be renamed to `opts` to mirror the function signature; this rename is local to this handler.

3. Surface any new warnings: the existing `#warningsList` is populated from `parsed.warnings` after `parsePdf`. Add a small block after `calculateCoordinates` returns that appends `result.warnings` (the new array returned by Task 4) to `#warningsList` as `<li>` items, prefixed with a `[calc]` tag so they're visually distinct from parser warnings:
   ```
   const calcWarnings = result && Array.isArray(result.warnings) ? result.warnings : [];
   for (const w of calcWarnings) {
     const li = document.createElement('li');
     li.textContent = '[calc] ' + w;
     document.getElementById('warningsList').appendChild(li);
   }
   ```
   Place this block immediately after the `console.log("[pdf-to-kmz] Generated connections:", connections);` line. Make sure `result` is the destructured object (rename `const { posts: calculatedPosts, connections } = calculateCoordinates(...);` to `const result = calculateCoordinates(...); const { posts: calculatedPosts, connections } = result;`).

4. Do NOT change the second `calculateCoordinates` invocation inside the `compareBtn` handler (line ~512). That comparison block already operates on the parsed `currentParseData` and does not need the 2nd anchor — leave it on single-anchor for now. Add a one-line code comment `// 2nd-anchor input not wired into the compare flow yet (manual UAT uses calcBtn).` above that call.
  </action>
  <verify>
    <automated>node -e "import('fs').then(fs => { const html = fs.readFileSync('index.html', 'utf8'); const checks = [{ name: 'gpsInputLast input', re: /id=\"gpsInputLast\"/ }, { name: 'last-post label', re: /Last Post Coordinates \(optional, improves accuracy\):/ }, { name: 'lastPostGps wired into opts', re: /lastPostGps:\s*lastParsed/ }, { name: 'calcWarnings rendered', re: /'\[calc\] '\s*\+\s*w/ }]; let failed = 0; for (const c of checks) { if (!c.re.test(html)) { console.error('MISSING:', c.name); failed++; } else { console.log('OK:', c.name); } } process.exit(failed ? 1 : 0); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains an `<input>` with `id="gpsInputLast"` inside `<section id="coordForm">`.
    - The label "Last Post Coordinates (optional, improves accuracy):" appears verbatim in `index.html`.
    - The `calcBtn` handler builds an `opts` object containing `lastPostGps: lastParsed` ONLY when `lastParsed` is non-null.
    - When `gpsInputLast` is left empty, the `opts` object has no `lastPostGps` key (use spread-with-conditional, not `lastPostGps: null`).
    - The handler appends each `result.warnings` entry to `#warningsList` as a `<li>` prefixed with `[calc] `.
    - The `node -e` grep verification above prints all four "OK:" lines and exits 0.
  </acceptance_criteria>
  <done>
UI accepts an optional second GPS anchor, parses it with the same helper as post 1, passes it through to `calculateCoordinates` only when valid, and surfaces calculation warnings (label sanity, snap fallback, 2nd-anchor confirmation) in the existing warnings list.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Browser UAT against Palhoça sample — confirm accuracy improvement</name>
  <what-built>
Vertex-snap pre-step, isotropic per-page UTM scale, optional 2nd-anchor similarity refinement, and label sanity-check warnings — all wired through `index.html` and `debug-run-calc.mjs`. Pre-change baseline: max error 49.52 m on the Palhoça sample (`coordenadas postes.txt`, posts 01–11).
  </what-built>
  <how-to-verify>
1. Run the Node end-to-end check first (fastest signal):
   ```
   node debug-run-calc.mjs
   ```
   Expected output:
   - The "Comparison vs reference" block prints err values for all 11 posts.
   - Final line shows `Max error: <X>m  null GPS: 0/11`.
   - X SHOULD be smaller than the 49.52 m pre-change baseline. Record the actual value.
   - The "Warnings (first 8)" section may now include `[coordinate-calculator] snap:` lines (for any post with no nearby vertex) and `label sanity-check:` lines (for any segment where label and haversine disagree by > max(5m, 10% of label)). These warnings are diagnostic, not failures.

2. Open `index.html` in Chrome 137+ via a static server (e.g. `npx serve` or `python -m http.server`).

3. Upload `INFOVIAS_PJC INTERNET_Palhoça_RUA VALMOR FRANCISCO_v1.pdf` from the repo root.

4. Wait for the parser to finish (warnings list populates, coordForm becomes visible).

5. Single-anchor test:
   - In "Post #1 Coordinates": paste `-27.6594603999238, -48.699240275151034`
   - Leave "Last Post Coordinates" empty.
   - Click "Calculate Route".
   - Scroll to the "Comparison vs Reference (coordenadas postes.txt)" section and click "Compare".
   - Expected: 11 rows, all with non-null lat/lon, max error reported below the table. Record the max error.

6. Two-anchor test:
   - In "Last Post Coordinates" paste `-27.659066208413993, -48.702999429619396`.
   - Click "Calculate Route" again.
   - Click "Compare" again.
   - Expected: post 01 and post 11 have err ≈ 0.00 m (pinned exactly). Middle posts (02–10) should have smaller errors than the single-anchor run. Record the new max error.
   - In the warnings list, expect at least one `[calc] [coordinate-calculator] 2nd anchor applied — similarity refined: scale=..., rot=...°` line.

7. Sanity check the warnings list for unexpected regressions:
   - No "no Cabo_Projetado vertices on its page" warnings should fire for posts 01–11 (they are all on detail pages with cable paths).
   - "label sanity-check" warnings are expected IF the residual error per segment exceeds the tolerance — these are informational.

8. Report back with three numbers:
   - **Baseline:** 49.52 m (from CONTEXT.md)
   - **Single-anchor max error after change:** ___ m
   - **Two-anchor max error after change:** ___ m
And whether the result is "good enough to ship" or "needs another iteration".
  </how-to-verify>
  <resume-signal>
Type one of:
- `approved <single>m <two>m` — accept current accuracy, record numbers in the commit.
- `iterate <reason>` — request another planning iteration (e.g. "snap threshold too tight", "isotropic still off on page 4"). Spawn a follow-up plan.
- `block <reason>` — something is broken (null GPS, throw, missing UI element).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → browser file input | Untrusted PDF bytes are parsed by pdfjs and walked by our extractor. |
| user → GPS textbox | User-pasted strings are parsed by `parseCoordinateInput` and bounded by `validateBrazilBounds`. |
| parser → calculator | Internal trust boundary — calculator must tolerate missing/malformed `cableSegments`, `viewportBoxes`, `pageDimensions` without throwing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | `snapPostsToPolyline` vertex pool | mitigate | Guard against missing `pageNum` on cable segments (skip rather than crash); cap the candidate-edge list size implicitly by the 30 pt threshold so a degenerate PDF with millions of vertices cannot OOM. |
| T-02-05-02 | Denial of Service | `calculateCoordinates(opts.lastPostGps)` | mitigate | Validate `lastPostGps.lat`/`lon` are finite numbers before `latLonToUtm`; reject NaN/Infinity via `validateBrazilBounds` and the explicit `typeof === 'number'` guard. |
| T-02-05-03 | Information Disclosure | User GPS in warnings | accept | Warnings printed via `console.warn` may include partial coords. Tool runs client-side only; no transmission. |
| T-02-05-04 | Tampering | Bezier control-point misuse | mitigate | Snap step explicitly uses ONLY `M`/`L` and the *endpoints* of `C`/`C2`; bezier interior samples are forbidden as snap targets. Documented in Task 1 action. |
| T-02-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — all changes are to existing source files (`coordinate-calculator.js`, `utm-calibrator.js`, `index.html`). RESEARCH.md confirms no new external dependencies; pdfjs / Tesseract already loaded by Phase 1. |
</threat_model>

<verification>
Phase-level checks after all tasks complete:
- `node debug-run-calc.mjs` exits 0 with `null GPS: 0/11` and a max-error value lower than 49.52 m.
- `node parser/__tests__/coordinate-calculator.test.mjs` exits 0 (existing 20/20).
- `node parser/__tests__/two-anchor.test.mjs` exits 0 (new test from Task 3).
- `index.html` loads in Chrome 137+ without console errors when opened against the Palhoça sample.
- The `[calc] 2nd anchor applied — similarity refined: ...` warning appears in the browser warnings list when both anchor inputs are filled.
</verification>

<success_criteria>
Plan 02-05 is complete when:

1. `snapPostsToPolyline` is exported from `parser/coordinate-calculator.js` and runs as the first mutation inside `calculateCoordinates`.
2. `parser/geo/utm-calibrator.js` no longer contains `detailPageYScale`; `detailPageScale` is the single isotropic-scale function.
3. `calculateCoordinates`'s 6th parameter is `opts` and accepts an optional `lastPostGps`; when provided, the function pins both anchors exactly and refines middle posts via a 2D similarity transform.
4. After GPS is computed, labeled non-gap connections are sanity-checked vs haversine and warnings are emitted past the `max(5m, 10% of label)` tolerance — without mutating connection `meters`.
5. `index.html` contains the optional `#gpsInputLast` input, parses it with `parseCoordinateInput`, and passes a valid result through as `opts.lastPostGps`; calculator warnings render into `#warningsList`.
6. A new `parser/__tests__/two-anchor.test.mjs` covers the 2nd-anchor branch and passes.
7. Existing test (`parser/__tests__/coordinate-calculator.test.mjs`) still passes.
8. Browser UAT (Task 6) records single-anchor and two-anchor max errors against the Palhoça sample, with the human verification gate either approved or directed to a follow-up plan.
</success_criteria>

<output>
Create `.planning/phases/02-coordinate-calculator/02-05-SUMMARY.md` when done, including:
- The recorded baseline (49.52 m), single-anchor max error, and two-anchor max error from Task 6.
- Whether the <5 m target was hit and on which posts.
- Any per-post `[snap]` fallbacks (posts that kept OCR position because no vertex was within 30 pt).
- Any `[label sanity-check]` warnings and what they indicate about residual drift.
- The decision from the human verifier (approve / iterate / block).
</output>
