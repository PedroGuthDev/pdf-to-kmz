---
phase: 06-dxf-ingestion-region-lookup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parser/dwg/region-library.js
  - parser/__tests__/dxf-ingestion.test.mjs
  - parser/__tests__/fixtures/siriu-bbox-golden.json
  - parser/__tests__/fixtures/mm-scale.dxf
autonomous: true
requirements: [DXF-01, DXF-02, DXF-03]
must_haves:
  truths:
    - "Re-ingesting siriu.dxf produces the same bboxLatLon as before any change (no drift)"
    - "A re-ingested valid SC DXF stores crs.confidence === 'high'"
    - "A mm-scale DXF that fails the divide-by-1000 retry throws 'DXF unit mismatch suspected' and stores nothing"
    - "A DXF whose extmin/extmax corners fall outside Brazil's bbox throws and stores nothing"
  artifacts:
    - path: "parser/dwg/region-library.js"
      provides: "Envelope/unit-mismatch + Brazil-bbox validation inside addRegion; crs.confidence field"
      contains: "DXF unit mismatch suspected"
    - path: "parser/__tests__/dxf-ingestion.test.mjs"
      provides: "SC-1 golden-bbox regression + SC-2 mm-fail + DXF-03 Brazil-bbox tests"
      contains: "DXF unit mismatch suspected"
    - path: "parser/__tests__/fixtures/siriu-bbox-golden.json"
      provides: "Pre-change Siriu bboxLatLon golden reference for drift detection"
      contains: "minLat"
  key_links:
    - from: "parser/dwg/region-library.js addRegion"
      to: "parser/geo/utm-calibrator.js utmToLatLon"
      via: "corner UTM->WGS84 conversion before Brazil-bbox check"
      pattern: "utmToLatLon"
---

<objective>
Harden `region-library.js::addRegion()` with the two fail-loud validation guards that make DXF ingestion trustworthy: coordinate-range unit-mismatch detection (mm->m divide-by-1000 retry, D-01/D-02) and Brazil-bounding-box corner validation (D-09), plus the `crs.confidence` field (D-08). Capture the pre-change Siriu `bboxLatLon` as a golden fixture FIRST so any coordinate drift introduced by this phase is detectable.

Purpose: Closes DXF-01 (CRS resolved at ingest with confidence), DXF-02 (mm retry + loud "DXF unit mismatch suspected"), DXF-03 (Brazil-bbox corner validation, fail loud out of range). After this slice a user who ingests a valid SC DXF gets a `crs.confidence:'high'` record; a user who ingests a mm-scale or out-of-country DXF gets a loud error instead of a silently-wrong region.
Output: Validation logic added in-place in `addRegion()` (strangler-fig, D-03 — no new ingestion module), the new test file, the Siriu golden fixture, and a synthetic mm-scale DXF fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dxf-ingestion-region-lookup/06-CONTEXT.md
@.planning/phases/06-dxf-ingestion-region-lookup/06-RESEARCH.md
@.planning/phases/06-dxf-ingestion-region-lookup/06-PATTERNS.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

From parser/dwg/region-library.js (current addRegion flow, lines 50-89):
- parseDxfText(dxfText) returns { posts, cableEdges, primaryCableEdges, extmin, extmax }
  where extmin/extmax are { x, y } in raw DXF UTM metres.
- DEFAULT_CRS = { datum: "SIRGAS-2000", zone: 22, hemisphere: "S" }  (line 11)
- bboxUtm = { minE: extmin.x, maxE: extmax.x, minN: extmin.y, maxN: extmax.y }
- normalizeBboxLatLon(a, b) takes two {lat,lon} and returns {minLat,maxLat,minLon,maxLon}
- record fields: { id, name, uploadedAt, crs, bboxUtm, bboxLatLon, posts, cableEdges, primaryCableEdges, rbushDump, sourceDxf, parserVersion }
- createRegionLibrary(idbFactory = null) factory; addRegion(name, dxfBlob) where dxfBlob.text() yields the DXF string.

From parser/geo/utm-calibrator.js:
- utmToLatLon(easting, northing, zone) returns { lat, lon }  (Snyder TM inverse, Math.* only)

VERIFIED real extents (use these, NOT the CONTEXT "~730000" figure which is WRONG):
- Siriu:   E 642260-742091, N 6812580-6930560
- Palhoca: E 642260-761089, N 6812580-6996762
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Siriu golden bbox fixture (drift baseline) BEFORE touching addRegion</name>
  <read_first>
    - parser/dwg/region-library.js (current addRegion, to run it unchanged)
    - parser/__tests__/region-pairing.test.mjs (fixture-load + fake-indexeddb idiom to mirror)
  </read_first>
  <files>parser/__tests__/fixtures/siriu-bbox-golden.json</files>
  <action>Write a one-off capture step (inline node script or a `node -e` invocation documented in the SUMMARY) that imports `createRegionLibrary` from `parser/dwg/region-library.js` UNCHANGED, imports `IDBFactory` from `fake-indexeddb`, reads `siriu.dxf` from the repo root via `readFileSync(new URL("../../siriu.dxf", import.meta.url))`, calls `addRegion("siriu-golden", {text: async () => dxfText})`, and writes the resulting `record.bboxLatLon` (the `{minLat,maxLat,minLon,maxLon}` object) to `parser/__tests__/fixtures/siriu-bbox-golden.json`. This file is the SC-1 drift baseline and MUST be captured from the current code before any validation edits land. Do NOT round or reformat the numbers — write them as `JSON.stringify(record.bboxLatLon)` so equality is exact.</action>
  <verify>
    <automated>node -e "const b=require('./parser/__tests__/fixtures/siriu-bbox-golden.json'); if(typeof b.minLat!=='number'||typeof b.maxLon!=='number'){process.exit(1)}; console.log('golden bbox ok',b)"</automated>
  </verify>
  <done>parser/__tests__/fixtures/siriu-bbox-golden.json exists and contains numeric minLat/maxLat/minLon/maxLon captured from the unmodified addRegion path.</done>
  <acceptance_criteria>
    - parser/__tests__/fixtures/siriu-bbox-golden.json exists on disk
    - JSON.parse of the file yields numeric minLat, maxLat, minLon, maxLon
    - The file was produced by the UNMODIFIED addRegion (no validation edits committed before this task)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit-mismatch + Brazil-bbox validation and crs.confidence to addRegion</name>
  <read_first>
    - parser/dwg/region-library.js (addRegion lines 50-89 — the exact insertion point between parse/index and record assembly)
    - parser/geo/utm-calibrator.js (utmToLatLon signature, returns {lat,lon})
    - parser/dwg/dxf-loader.js (lines 1-12 — the "DO NOT scale $INSUNITS" invariant that D-01 coordinate-range detection preserves)
    - .planning/phases/06-dxf-ingestion-region-lookup/06-RESEARCH.md (Code Examples: envelope + Brazil-bbox; Pitfall 1 + Pitfall 2)
  </read_first>
  <files>parser/dwg/region-library.js</files>
  <behavior>
    - Test: valid SC DXF (Siriu) ingests -> record.crs.confidence === "high" and record.bboxLatLon equals the golden fixture exactly.
    - Test: synthetic mm-scale DXF whose extmax.x ~ 642,260,000 fails the divide-by-1000 retry (642,260,000/1000 = 642,260 IS in envelope -> actually passes; so the fixture must use a value that is out of envelope BOTH raw AND after /1000, e.g. extmax.x = 9,999,999) -> addRegion throws Error with message exactly "DXF unit mismatch suspected" and nothing is stored.
    - Test: a DXF whose extmin/extmax corners convert to lat/lon outside Brazil (lat/lon outside -33.8..5.3 / -73.0..-34.8) -> addRegion throws and nothing is stored.
    - Test: confidence:'low' (successful mm->m retry) is asserted UNREACHABLE for the real corpus — per CONTEXT discretion, treat any extmax outside-envelope-then-in-envelope-after-/1000 as confidence:'low' in code but document in SUMMARY that no corpus file triggers it.
  </behavior>
  <action>Inside `addRegion()`, between the `parseDxfText` result destructure and the `record` assembly, insert: (1) a module-level constant `ZONE_22S = { minE: 600000, maxE: 800000, minN: 6700000, maxN: 7100000 }` and predicate `inZone22S(e, n)` checking e/n within those bounds — derived from VERIFIED Siriu/Palhoca extents (E 642260-761089, N 6812580-6996762) with margin, NOT the CONTEXT "~730000" figure; (2) the unit-retry block: if `!inZone22S(extmax.x, extmax.y)` then if `inZone22S(extmax.x/1000, extmax.y/1000)` set `scale = 1/1000` and `confidence = "low"`, else `throw new Error("DXF unit mismatch suspected")` (exact string, D-02 — never store on failed retry); (3) apply `scale` to extmin.x/y and extmax.x/y AND to every post coordinate before indexing/storing (default scale = 1, confidence = "high"); (4) module-level `BRAZIL = { minLat: -33.8, maxLat: 5.3, minLon: -73.0, maxLon: -34.8 }` and `inBrazil(p)` over `p.lat`/`p.lon`; compute `ll0 = utmToLatLon(scaledExtmin.x, scaledExtmin.y, crs.zone)` and `ll1` likewise, and if `!inBrazil(ll0) || !inBrazil(ll1)` throw `new Error("DXF coordinates outside Brazil - wrong UTM zone or datum suspected")`; (5) add `confidence` to the `crs` object (`crs = { ...DEFAULT_CRS, confidence }`). Preserve the "DO NOT scale $INSUNITS" invariant — detection is coordinate-range only, never reads $INSUNITS (D-01). Do NOT introduce a new module; all logic stays in region-library.js (D-03). The constants/predicates may live at module top of region-library.js.</action>
  <verify>
    <automated>node --test parser/__tests__/dxf-ingestion.test.mjs</automated>
  </verify>
  <done>addRegion stores crs.confidence:'high' for valid SC DXFs with bboxLatLon identical to the golden fixture; throws "DXF unit mismatch suspected" on double-failed unit retry; throws on out-of-Brazil corners; no region record is written on either throw path.</done>
  <acceptance_criteria>
    - Ingesting siriu.dxf returns record.crs.confidence === "high"
    - record.bboxLatLon deep-equals parser/__tests__/fixtures/siriu-bbox-golden.json
    - addRegion throws Error whose .message === "DXF unit mismatch suspected" when both raw and /1000 extmax are outside ZONE_22S
    - After a thrown unit-mismatch, listRegions() does NOT contain the rejected region id
    - addRegion throws when utmToLatLon of either extmin/extmax corner is outside BRAZIL bounds
    - Module exposes ZONE_22S = {minE:600000,maxE:800000,minN:6700000,maxN:7100000}; no read of $INSUNITS appears in addRegion
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write dxf-ingestion test suite (SC-1 golden, SC-2 mm-fail, DXF-03 Brazil) + mm fixture</name>
  <read_first>
    - parser/__tests__/region-pairing.test.mjs (lines 1-26 — `import "fake-indexeddb/auto"` FIRST, node:test, fixture reads via new URL)
    - parser/dwg/region-library.js (the addRegion contract under test)
    - parser/__tests__/fixtures/siriu-bbox-golden.json (golden reference from Task 1)
  </read_first>
  <files>parser/__tests__/dxf-ingestion.test.mjs, parser/__tests__/fixtures/mm-scale.dxf</files>
  <action>Create `parser/__tests__/fixtures/mm-scale.dxf` — a minimal valid DXF (HEADER with $EXTMIN/$EXTMAX whose extmax.x/y are out of the zone-22S envelope BOTH raw AND after divide-by-1000, e.g. extmax = 9,999,999 / 9,999,999; plus a couple of INSERT entities so parseDxfText returns posts). Create `parser/__tests__/dxf-ingestion.test.mjs` mirroring region-pairing.test.mjs: `import "fake-indexeddb/auto"` MUST be the first import (addRegion touches IndexedDB), then `node:test`, `node:assert/strict`, `readFileSync`, and `createRegionLibrary` from `../dwg/region-library.js`. Tests: (a) SC-1 — ingest siriu.dxf (read from `new URL("../../siriu.dxf", import.meta.url)`), assert `record.crs.confidence === "high"` and `record.bboxLatLon` deep-equals the golden JSON; (b) SC-2 — `assert.rejects(() => lib.addRegion("mm", blob), /DXF unit mismatch suspected/)` using the mm-scale fixture, then assert `(await lib.listRegions())` does NOT contain id "mm"; (c) DXF-03 — a fixture or in-test extmin/extmax that converts outside Brazil throws (use `assert.rejects` with a message matching `/outside Brazil/`). Use a fresh `createRegionLibrary(new IDBFactory())` per test to isolate state.</action>
  <verify>
    <automated>node --test parser/__tests__/dxf-ingestion.test.mjs</automated>
  </verify>
  <done>node --test parser/__tests__/dxf-ingestion.test.mjs passes with at least the SC-1 golden-bbox, SC-2 mm-fail, and DXF-03 Brazil-bbox test cases green; the mm-scale fixture exists.</done>
  <acceptance_criteria>
    - node --test parser/__tests__/dxf-ingestion.test.mjs exits 0
    - Test file imports "fake-indexeddb/auto" as its first import
    - SC-1 test deep-equals record.bboxLatLon against the golden fixture
    - SC-2 test uses assert.rejects(..., /DXF unit mismatch suspected/)
    - parser/__tests__/fixtures/mm-scale.dxf exists with extmax out of envelope both raw and after /1000
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded DXF -> addRegion | Untrusted file input crosses into ingestion/storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | addRegion coordinate validation | mitigate | Envelope + Brazil-bbox corner checks fail loud; never store a silently-wrong region (D-02/D-09) |
| T-06-02 | Denial of Service | parseDxfText on malformed DXF | accept | parseSync throw is caught at the caller and surfaced as user error (existing pattern); huge-file DoS handled by Worker isolation in plan 06-03 |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No packages added this phase (RESEARCH Package Legitimacy Audit: N/A); nothing to verify |
</threat_model>

<verification>
- `node --test parser/__tests__/dxf-ingestion.test.mjs` green.
- Existing `npm run test:gate` still green (no Siriu coordinate drift — golden bbox unchanged).
- `parser/__tests__/fixtures/siriu-bbox-golden.json` captured from pre-change code.
</verification>

<success_criteria>
- DXF-01: valid SC DXF ingests with crs.confidence:'high'.
- DXF-02: mm-scale DXF failing the /1000 retry throws exactly "DXF unit mismatch suspected" and stores nothing.
- DXF-03: out-of-Brazil corners throw and store nothing.
- SC-1: re-ingested Siriu bboxLatLon equals the golden fixture (zero drift).
</success_criteria>

<output>
Create `.planning/phases/06-dxf-ingestion-region-lookup/06-01-SUMMARY.md` when done
</output>
