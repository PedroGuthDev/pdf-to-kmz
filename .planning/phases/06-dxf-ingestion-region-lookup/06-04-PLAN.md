---
phase: 06-dxf-ingestion-region-lookup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - parser/dwg/coordinate-calculator-dwg.js
  - browser/main.js
  - parser/__tests__/calc-user-warnings.test.mjs
  - parser/__tests__/coordinate-calculator-dwg-no-region.test.mjs
autonomous: false
gap_closure: true
requirements: [DXF-05]
must_haves:
  truths:
    - "When post-1 GPS falls outside all ingested region bboxes, #calcNotices (main workflow) shows a Portuguese message that no DXF region covers the GPS"
    - "The no-region notice includes the nearest ingested region name and distance in km"
    - "dwg-region-miss structured warnings appear in #calcNotices, not only in hidden #debugSection #warningsList"
    - "Calculation still completes with pdf-fallback; backend dwgNoRegion shape is unchanged"
  artifacts:
    - path: "parser/dwg/coordinate-calculator-dwg.js"
      provides: "formatNoRegionUserNotice + collectCalcUserNotices exporting Portuguese no-region copy"
      contains: "formatNoRegionUserNotice"
    - path: "browser/main.js"
      provides: "calcBtn handler calls collectCalcUserNotices → showCalcNotices"
      pattern: "collectCalcUserNotices"
    - path: "parser/__tests__/calc-user-warnings.test.mjs"
      provides: "unit tests for no-region notice text"
  key_links:
    - from: "browser/main.js calcBtn handler"
      to: "collectCalcUserNotices(result)"
      via: "showCalcNotices(collectCalcUserNotices(result))"
      pattern: "collectCalcUserNotices"
    - from: "collectCalcUserNotices"
      to: "result.dwgNoRegion.nearest"
      via: "formatNoRegionUserNotice"
      pattern: "dwgNoRegion"
---

<objective>
Close UAT Test 4 gap: surface existing `dwgNoRegion` / `dwg-region-miss` signals in the main `#calcNotices` panel (Portuguese, nearest-region hint). Backend already emits structured data; this plan fixes the UI surfacing layer only — no ingestion or cascade rewrite.

Purpose: Satisfies Phase 6 SC-3 / DXF-05 — GPS outside all regions must fail loud with nearest-region hint visible to the user, not buried in hidden developer tools.
Output: `collectCalcUserNotices(result)` helper, wired in `browser/main.js`, with unit tests locking Portuguese copy.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dxf-ingestion-region-lookup/06-UAT.md
@.planning/debug/no-region-warning-missing.md
@.planning/phases/06-dxf-ingestion-region-lookup/06-02-SUMMARY.md

<interfaces>
From parser/dwg/coordinate-calculator-dwg.js (existing — do not change shapes):

```javascript
export function noRegionError(lat, lon, regions) {
  // returns { code: "NO_REGION", nearest: { name, distanceKm } | null }
}

export function buildCalcUserWarnings(result) {
  // returns string[] — currently generic pdf-fallback text; misses dwgNoRegion specifics
}

export function formatDwgWarning(w) {
  // case "dwg-region-miss": `DWG: nenhuma região cobre o GPS do poste 1 (${lat}, ${lon}). Usando só PDF.`
}
```

From browser/main.js calcBtn handler (lines ~785-802):

```javascript
const calcUserNotices =
  Array.isArray(result.userWarnings) && result.userWarnings.length > 0
    ? result.userWarnings
    : buildCalcUserWarnings(result);
showCalcNotices(calcUserNotices);
// calcWarnings → #warningsList inside hidden #debugSection only
```

From index.html: `#calcNotices` is in the main calc section (visible); `#warningsList` is inside `#debugSection` (display:none until dev tools toggled).

Gap root cause (UAT Test 4): structured signals exist on `result` but main UI never renders `dwgNoRegion.nearest` or promotes `dwg-region-miss` to `#calcNotices`.

Out of scope (Phase 9 / deferred): false-positive bbox match when wrong DXF bbox still contains post-1 GPS; full CONF-01 taxonomy; hard-block KMZ emission; `hardBlock` flag.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add collectCalcUserNotices with no-region Portuguese copy</name>
  <files>parser/__tests__/calc-user-warnings.test.mjs, parser/dwg/coordinate-calculator-dwg.js</files>
  <behavior>
    - formatNoRegionUserNotice({ code:"NO_REGION", nearest:{ name:"Florianópolis test", distanceKm:412.3 } }) returns a string containing "nenhuma região", "Florianópolis test", and "412.3" (or "412,3" if locale — prefer "412.3 km" with dot decimal per existing formatDwgWarning meter style)
    - buildCalcUserWarnings({ dwgStatus:"pdf-fallback", dwgNoRegion:{ code:"NO_REGION", nearest:{ name:"X", distanceKm:10 } }, posts:[{source:"pdf"}] }) includes the no-region notice and does NOT duplicate the old generic-only pdf-fallback sentence when dwgNoRegion is present
    - collectCalcUserNotices merges userWarnings + formatDwgWarning for dwg-region-miss from warnings without duplicate strings
    - collectCalcUserNotices on a pdf-fallback result without dwgNoRegion still returns the existing generic precision notice (regression)
  </behavior>
  <action>
    RED: Create `parser/__tests__/calc-user-warnings.test.mjs` (node:test + node:assert/strict) importing `formatNoRegionUserNotice`, `buildCalcUserWarnings`, `collectCalcUserNotices` — run tests; they must fail first.

    GREEN: In `coordinate-calculator-dwg.js`:

    1. Add exported `formatNoRegionUserNotice(dwgNoRegion)` — Portuguese user copy per D-09 idiom (plain language, no `%`):
       - When `nearest?.name` and finite `nearest.distanceKm`: `"Nenhuma região DXF cobre o GPS do poste 1. Região mais próxima: {name} (~{distanceKm.toFixed(1)} km). Coordenadas calculadas só pelo PDF."`
       - When nearest missing: `"Nenhuma região DXF cobre o GPS do poste 1. Coordenadas calculadas só pelo PDF."`

    2. Update `buildCalcUserWarnings(result)`:
       - If `result.dwgNoRegion?.code === "NO_REGION"`, push `formatNoRegionUserNotice(result.dwgNoRegion)` first.
       - When that no-region notice is present, skip the generic `"Precisão limitada: coordenadas calculadas só pelo PDF — o pareamento DXF não concluiu..."` block (avoid duplicate/confusing messages). Keep partial-dwg and dwg-pdf-walk notices unchanged.

    3. Add exported `collectCalcUserNotices(result)`:
       - Start from `result.userWarnings` if non-empty, else `buildCalcUserWarnings(result)`.
       - Dedupe via Set.
       - For each `result.warnings` entry with `kind === "dwg-region-miss"`, append `formatDwgWarning(w)` if not already present (covers regionId-miss variant too).
       - Return string array.

    Do NOT modify `noRegionError`, cascade miss paths, or `formatDwgWarning` switch cases beyond what tests require. Phase 9 owns fuller taxonomy.
  </action>
  <verify>
    <automated>node --test parser/__tests__/calc-user-warnings.test.mjs</automated>
  </verify>
  <done>All calc-user-warnings tests pass; exported helpers available for browser import</done>
</task>

<task type="auto">
  <name>Task 2: Wire browser calc handler to collectCalcUserNotices</name>
  <files>browser/main.js, parser/__tests__/coordinate-calculator-dwg-no-region.test.mjs</files>
  <action>
    In `browser/main.js`:
    - Add `collectCalcUserNotices` to the existing import from `../parser/dwg/coordinate-calculator-dwg.js` (alongside `buildCalcUserWarnings`, `formatDwgWarning`).
    - Replace the calcBtn handler block (lines ~785-789) with:
      `showCalcNotices(collectCalcUserNotices(result));`
    - Leave the `#warningsList` append loop (dev tools) unchanged — dual surfacing is OK; main fix is `#calcNotices`.

    Extend `parser/__tests__/coordinate-calculator-dwg-no-region.test.mjs`:
    - Import `collectCalcUserNotices`.
    - On the existing cascade-miss test stub (`lookupByGps → null`, São Paulo GPS), assert `collectCalcUserNotices(result)` returns at least one string matching `/nenhuma região/i` and containing the seeded region name `"Florianópolis test"`.

    Rebuild if project uses esbuild bundle (`npm run build`) — verify import resolves in bundled output (collectCalcUserNotices must be exported and tree-shaken into bundle).
  </action>
  <verify>
    <automated>node --test parser/__tests__/coordinate-calculator-dwg-no-region.test.mjs parser/__tests__/calc-user-warnings.test.mjs</automated>
  </verify>
  <done>Browser imports collectCalcUserNotices; integration test proves no-region copy reaches collectCalcUserNotices output; dev-tools warnings loop untouched</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT Test 4 manual re-check</name>
  <files>.planning/phases/06-dxf-ingestion-region-lookup/06-UAT.md</files>
  <action>After Tasks 1–2 pass automated tests, pause for human UAT Test 4 re-check per steps below. Update 06-UAT.md Test 4 to pass if approved.</action>
  <verify>
    <automated>node --test parser/__tests__/calc-user-warnings.test.mjs parser/__tests__/coordinate-calculator-dwg-no-region.test.mjs</automated>
  </verify>
  <done>User confirms #calcNotices shows Portuguese no-region message with nearest-region hint; Test 4 marked pass in 06-UAT.md</done>
  <what-built>No-region / region-mismatch warnings now appear in `#calcNotices` (main workflow) with nearest-region name and distance when GPS is outside all ingested DXF bboxes.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in the browser.
    2. Upload a DXF that does NOT cover the Siriu route (or any DXF whose bbox excludes Siriu post-1 GPS).
    3. Parse the Siriu PDF and enter Siriu post-1 GPS (or São Paulo coords `-23.55, -46.63` if no Siriu DXF loaded).
    4. Click "Calcular rota".
    5. Confirm `#calcNotices` panel appears below the calc button with Portuguese text stating no region covers the GPS, including nearest region name and ~distance km.
    6. Confirm calculation still completes (pdf-fallback) and KMZ can still be generated.
    7. Optional: open developer tools panel — `[calc] dwg-region-miss` may also appear there; main requirement is visible `#calcNotices`.
  </how-to-verify>
  <resume-signal>Type "approved" if Test 4 passes, or describe what is still missing</resume-signal>
</task>

</tasks>

<verification>
- `node --test parser/__tests__/calc-user-warnings.test.mjs` green
- `node --test parser/__tests__/coordinate-calculator-dwg-no-region.test.mjs` green (includes collectCalcUserNotices assertion)
- Manual UAT Test 4 re-check passes (checkpoint Task 3)
- No changes to ingestion pipeline, region-library, or cascade logic
</verification>

<success_criteria>
- UAT Gap Test 4 truth satisfied: user sees Portuguese no-region warning with nearest-region hint in main UI when GPS is outside all regions
- DXF-05 user-visible surfacing complete for Phase 6 scope; Phase 9 may extend taxonomy without rework of this wiring
</success_criteria>

<output>
After completion, create `.planning/phases/06-dxf-ingestion-region-lookup/06-04-SUMMARY.md` and update `06-UAT.md` Test 4 result to pass if checkpoint approved.
</output>
